# ثبت ادغام G15 در main — مقاوم‌سازی تراکنش‌های Core

**تاریخ:** ۱۷ سپتامبر ۲۰۲۶
**مجری ادغام و ثبت‌کننده:** Claude Opus 5 — MLINO Architecture Guardian

## ۱. تصویب مالک

> «ادغام G15 در main مجاز است.»

## ۲. ادغام انجام‌شده

| مورد | مقدار |
|---|---|
| **merge commit** | **`9ae0d79b096590b61df2756e75ac2ddae11f959f`** · tree ‏`f595e645` · والدها `a7f7be9` و `def9039` |
| ادغام آزمایشی | ✅ tree برابر است · تفاوت با آزمایش پیشین (`7f23b013`) فقط سه فایل AI_HANDOFF است |
| دامنه | ۴۹ فایل، +۶٬۵۴۲ و −۲۸ خط، بدون تعارض |
| **پذیرش پس از ادغام** | ✅ در main، `$transaction(` فقط در `implementation/core/transaction.ts` است · پوشه‌ی migrations دست‌نخورده (۷ migration و فایل lock) |
| hashهای کلیدی در main | `transaction.ts` ‏`a94e91b2…` · `errors.ts` ‏`76b0ee84…` · `error-adapter.ts` ‏`e4f549d3…` · spec ‏`9599cfcf…` |
| اثر runtime | بدون تغییر انتظار می‌رود: پوشه‌ی `core` در image ‏read-api کپی نمی‌شود · بدون schema و migration |

**وضعیت Docker در زمان ادغام:** Docker Desktop خاموش است، پس وضعیت زنده‌ی DB سنجیده نشد. G15 هیچ تماسی با DB محلی نداشت. **پس از روشن شدن Docker این موارد را می‌سنجم:** ۷ migration، `publications`=0، ستون و CHECK موجود، و پاسخ 401 از read-api.

**G15 بسته شد:** تشخیص، CCR، پیاده‌سازی، ۱۰ اجرای سبز و ادغام.

---

## ۳. گام بزرگ بعدی: G14b

طبق بخش ۱۳ سند نهایی قرارداد خواندن V2، دو گام باقی است:
- **G14b:** تولیدکننده‌ی export امضاشده در V1
- **G14c:** انتقال مصرف‌کننده‌ی V2 به `public-business.v1`

**پیشنهاد نگهبان: ابتدا G14b-1، یک سند طراحی کوتاه (فقط سند).** قرارداد داده‌ای از قبل نهایی است، ولی این پرسش‌های عملیاتی هنوز تصمیم ندارند:
1. **امضا:** الگوریتم، و اینکه کلید خصوصی کجا نگهداری و چگونه چرخانده شود. مالک باید سیاست کلید را تصویب کند.
2. **شکل اجرا:** اسکریپت دستی، کار زمان‌بندی‌شده، یا سرویس. و اینکه خروجی کجا نوشته شود.
3. **چرخه:** S23-B حداکثر ۵ دقیقه · چرخه‌ی دوره‌ای یا invalidation فوری هنگام لغو انتشار.
4. **منبع داده:** فقط `published_content` (S19-A1) به‌همراه دروازه‌های زنده‌ی S16-A، S18-A، S20-A و lifecycle.
5. **قالب `business_hours` و `terms`** که طبق OQ-4 A′ به G14b سپرده شد.
6. **آزمون‌ها و شواهد:** فقط روی DB یک‌بارمصرف.

**پاسخ پیشنهادی مالک:**
> «G14b-1 (سند طراحی export امضاشده) مجاز است.»

## ۴. وضعیت دروازه‌ها

| دروازه | موضوع | وضعیت |
|---|---|---|
| G14a | snapshot محتوای منتشرشده | ✅ کامل و اعمال‌شده روی DB محلی |
| **G15** | **مقاوم‌سازی تراکنش** | ✅ **در main ادغام شد (`9ae0d79`)** |
| **G14b-1** | **سند طراحی export امضاشده** | ⏳ **تصمیم مالک** |
| G14b-2 و G14c | پیاده‌سازی export و مصرف‌کننده‌ی V2 | ⏳ تصویب جدا |

من کلاد هستم

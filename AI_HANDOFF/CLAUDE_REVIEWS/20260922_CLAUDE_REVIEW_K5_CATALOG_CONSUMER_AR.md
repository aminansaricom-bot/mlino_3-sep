# بازبینی نگهبان — K5: نمایش کاتالوگ و ویترین دوربین روی گوشی

**تاریخ:** ۲۲ سپتامبر ۲۰۲۶
**بازبین:** Claude Opus 5 — نگهبان معماری MLINO
**commitها:** `2718564` تا `ce02693` روی `47b1733` · به‌علاوه‌ی یک commit اصلاحی نگهبان `9e2143b`. **نگهبان شاخه‌ی `codex/v2-catalog-ui` را در `9e2143b` با lease خالی منتشر کرد.**

## حکم: `APPROVED_NEXT_STEP`؛ یک ایراد کوچک را نگهبان خودش بست

## ۱. راستی‌آزمایی

| مورد | نتیجه |
|---|---|
| **دامنه** | ✅ فقط `nginx.conf` و `src/**` · بدون تغییر در `package.json`، lockfile، Dockerfile یا کد نسخه‌ی یک |
| **ساخت** | ✅ موفق |
| **آزمون‌ها** | ✅ **۲۸۸ از ۲۸۸** (پس از اصلاح F1، از یک checkout تازه) |
| **جداسازی کاتالوگ (K5-G1)** | ✅ خرابی کاتالوگ در `catch` مهار می‌شود و وضعیت کسب‌وکار دست نمی‌خورد؛ به‌روزرسانی کاتالوگ پس از کسب‌وکار انجام می‌شود |
| **سنجش تصویر (K5-G2)** | ✅ بایت‌ها با سقف ۱٫۵ مگابایت دریافت و اثرانگشت، قالب، پویانمایی و ابعاد سنجیده می‌شوند؛ blob فقط از بایت‌های سنجیده ساخته می‌شود؛ حافظه‌ی آفلاین در هر خواندن دوباره سنجیده و در صورت خرابی پاک می‌شود |
| **گره خوردن (K5-G3)** | ✅ ناهمخوانی فقط کاتالوگ همان کسب‌وکار را پنهان می‌کند و پیوند پیشنهادهای ناموجود حذف می‌شود |

## ۲. آزمون nginx واقعی

پیکربندی را با گواهی موقت، خروجی ساخت، یک فایل کاتالوگ و یک تصویر PNG واقعی در کانتینر `nginx:alpine` اجرا کردم. `nginx -t` موفق بود.

| درخواست | نتیجه |
|---|---|
| فایل کاتالوگ موجود | ✅ 200 · JSON |
| فایل کاتالوگ غایب | ✅ **404 واقعی nginx**، نه صفحه‌ی برنامه |
| تصویر معتبر | ✅ 200 · `image/png` · `immutable` · `nosniff` |
| پیشوند پوشه‌ی نادرست · پسوند نادرست · حروف بزرگ · اثرانگشت نادرست · `.bak` | ✅ همه 404 |
| هر مسیر دیگر زیر `/public-export/`، از جمله خود پوشه | ✅ 404، **یافته‌ی پیش از دستور بسته شد** |
| POST روی تصویر | ✅ 403 |
| مسیرهای `..` | ✅ nginx آن‌ها را پیش از تطبیق به همان فایل‌های عمومی مجاز نرمال می‌کند؛ هیچ فایلی بیرون پوشه در دسترس نیست |
| مسیرهای برنامه‌ی تک‌صفحه‌ای | ✅ مثل قبل |

## ۳. آزمایش حذف

| حذف | نتیجه |
|---|---|
| بررسی گره خوردن به شناسه‌ی انتشار کسب‌وکار | ✅ آزمون جداسازی سازمان‌ها شکست خورد |
| بررسی اثرانگشت تصویر | ✅ دو آزمون شکست خوردند |

## ۴. یافته

| # | یافته | اقدام |
|---|---|---|
| **F1** | در این ویندوز `core.autocrlf=true` است و checkout پایان‌خط fixture مُهرشده را از LF به CRLF تغییر می‌داد. پس آزمون اثرانگشت در هر checkout تازه **شکست می‌خورد** و فقط در worktree خود کدکس سبز بود. ساخت Docker از روی checkout ویندوز هم همین مشکل را داشت | ✅ **نگهبان بست:** commit `9e2143b` فقط یک فایل `mlino2/app/.gitattributes` افزود تا fixtureهای مُهرشده بدون تبدیل پایان‌خط بمانند. از checkout تازه، اثرانگشت `dc945f4a…` و ۲۸۸ از ۲۸۸ |

**یادداشت:** شاخه‌ی محلی کدکس در `ce02693` مانده تا worktree او آلوده نشود. commit نگهبان فقط روی سرور است.

## ۵. تأییدنشده

دوربین واقعی، کشیدن انگشت روی گوشی واقعی و حافظه‌ی آفلاین در مرورگر گوشی. این‌ها در آزمون روی گوشی پس از K6 سنجیده می‌شوند.

## ۶. تصمیم مالک

| # | تصمیم | توضیح |
|---|---|---|
| ۱ | **ادغام K5 در شاخه‌ی نسخه‌ی دوم** | بدون تعارض |
| ۲ | **K6** | محصول و تصویر آزمایشی ساختگی برای کسب‌وکارهای ونک، روشن کردن کاتالوگ در سازنده، و ساخت دوباره‌ی کانتینر آزمایشی برای دیدن روی گوشی |

**پاسخ پیشنهادی مالک:**
> «ادغام K5 در شاخه‌ی نسخه‌ی دوم مجاز است؛ مرحله‌ی K6 (داده‌ی آزمایشی کاتالوگ و آزمون روی گوشی) مجاز است.»

من کلاد هستم

# بازبینی نگهبان معماری — G15-2: پیاده‌سازی مقاوم‌سازی تراکنش در Core

**تاریخ:** ۱۷ سپتامبر ۲۰۲۶
**بازبین:** Claude Opus 5 — MLINO Architecture Guardian
**commitها:** `538342b` (کد) و `def9039` (شواهد و گزارش)، روی `1dc6818`. Codex آن‌ها را محلی ساخت و **نگهبان روی سرور منتشر کرد.**

## حکم: `APPROVED_NEXT_STEP`؛ آماده‌ی ادغام با تصویب مالک

## ۱. راستی‌آزمایی

| مورد | نتیجه |
|---|---|
| **دامنه** | ✅ دقیقاً فایل‌های مجاز: CCR (فقط وضعیت و تصمیم‌ها)، `transaction.ts` تازه، `errors.ts`، `error-adapter.ts`، ده سرویس، spec تازه، شواهد، گزارش و Handoff الحاقی · **بدون schema، migration، Dockerfile، tsconfig، package یا HTTP** · worktree تمیز |
| **پذیرش مسیر تراکنش** | ✅ `git grep '\$transaction('` در `implementation/core` **فقط یک تطابق** دارد: `transaction.ts:22` |
| **سرویس‌ها** | ✅ diff هر ده سرویس **فقط** یک import و جایگزینی فراخوانی است · ترتیب قفل‌ها، `.catch(mapCoreDatabaseError)` و منطق کسب‌وکار دست‌نخورده |
| **نشت** | ✅ هیچ مقدار محرمانه‌ای commit نشده · هشت تطابق grep فقط الگوهای پاک‌سازی و رمز تصادفی container آزمایشی در اسکریپت‌اند |
| **TypeScript** | ✅ `tsc --noEmit` با کد ۰ |
| **ادغام آزمایشی** | ✅ tree ‏`7f23b013`، بدون تعارض، ۴۹ فایل |

## ۲. بازبینی کد

| جنبه | نتیجه |
|---|---|
| **helper** | ✅ `runCoreTransaction(db, fn)` گزینه‌ها را از `coreTransactionOptions()` می‌گیرد · پیش‌فرض `maxWait=5000` و `timeout=10000` مطابق S2 · `CORE_TX_MAX_WAIT_MS` و `CORE_TX_TIMEOUT_MS` قابل تنظیم‌اند · مقدار نامعتبر، شامل صفر، منفی، غیرعددی و غیرصحیح، **خطا می‌دهد و بی‌صدا به پیش‌فرض برنمی‌گردد** (fail-closed) · **هیچ retry ندارد** (S3-A) |
| **قرارداد خطا** | ✅ `TRANSACTION_RETRYABLE` به `CoreErrorCode` افزوده شد (S1-A) · در adapter، کدهای `P2028`، `P2034`، `40001` و `40P01` به آن نگاشت می‌شوند · نگاشت‌های پیشین دست‌نخورده‌اند و خطای ناشناخته همچنان `INTERNAL_ERROR` است |
| **آزمون‌ها** | ✅ **T1:** گزینه‌های پیش‌فرض و override و رد مقدار نامعتبر · ✅ **T2 قطعی و مستقل از بار میزبان:** یک کلاینت با `connection_limit=1`، یک تراکنش که تنها اتصال را نگه می‌دارد، و `maxWait=200` · انتظار `TRANSACTION_RETRYABLE`، نه `INTERNAL_ERROR` · آزاد کردن و قطع اتصال در `finally` · ✅ **T3:** جدول نگاشت به‌همراه نگه‌داشتن نگاشت‌های trigger و یکتایی |
| **نام آزمون‌ها** | ✅ هر چهار نام گزارش واقعاً در spec وجود دارد |

## ۳. ده اجرای پذیرش

| اجرا | ۱ | ۲ | ۳ | ۴ | ۵ | ۶ | ۷ | ۸ | ۹ | ۱۰ |
|---|---|---|---|---|---|---|---|---|---|---|
| بار CPU | ۴٪ | ۲۹٪ | ۱۳٪ | **۵۲٪** | ۲۰٪ | ۴۰٪ | **۶۶٪** | ۱۵٪ | ۲۷٪ | ۴۲٪ |
| نتیجه | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |

- هر ده اجرا روی **container تازه‌ی یک‌بارمصرف** روی 5499 انجام شد، هر بار **۲۹ از ۲۹ suite و ۳۶۸ از ۳۶۸ آزمون** سبز، و `ten-run-verification.log` نتیجه‌ی ۱۰ از ۱۰ را ثبت کرده است.
- **مهم‌ترین شاهد:** اجراهای ۴ و ۷ با بار **۵۲ و ۶۶ درصد** سبز شدند. همان محدوده‌ی باری که در G14a-2c شش شکست P2028 می‌داد. یعنی اصلاح در عمل کار می‌کند.
- `connection_limit=10` فقط در URL آزمون است (S4-A) و تنظیم runtime دست‌نخورده مانده.
- هر ده container حذف شده‌اند و فهرست volumeها تغییر نکرده است.
- **گزارش صادقانه است:** یک اجرای پذیرش پیشین به‌خاطر خطای cleanup کد خروجی ۱ گرفته بود؛ Codex آن را به‌جای پنهان‌کردن ثبت کرد، اسکریپت را اصلاح کرد و ده اجرا را از نو انجام داد. اجرای اختیاری «زیر فشار مصنوعی» انجام نشد که مطابق دستور اختیاری بود.

**یادداشت محیطی:** در لحظه‌ی این بازبینی Docker Desktop روی سیستم اجرا نیست، پس وضعیت زنده‌ی DB را نسنجیدم. این تغییر ربطی به G15 ندارد و G15 هیچ تماسی با DB محلی نداشته است. پس از ادغام و هنگام روشن‌بودن Docker، وضعیت را دوباره می‌سنجم.

## ۴. تصمیم مالک: ادغام G15 در main

| مورد | مقدار |
|---|---|
| شاخه | `codex/core-g15-transaction-robustness` @ `def9039` |
| tree ادغام آزمایشی | `7f23b013`، بدون تعارض |
| دامنه | ۴۹ فایل: CCR ‏APPROVED، helper، قرارداد خطا، ده سرویس، spec تازه، شواهد g15-1 و g15-2، گزارش‌ها |
| اثر runtime | **هیچ.** پوشه‌ی `core` در image ‏read-api کپی نمی‌شود · بدون schema و migration · بدون تغییر در تنظیم runtime |

**پاسخ پیشنهادی مالک:**
> «ادغام G15 در main مجاز است.»

پس از آن نگهبان ادغام را با `--no-ff` و push محافظت‌شده انجام می‌دهد، ثبت می‌کند، و هنگام روشن‌بودن Docker وضعیت زنده را می‌سنجد.

## ۵. وضعیت دروازه‌ها

| دروازه | موضوع | وضعیت |
|---|---|---|
| G14a | snapshot محتوای منتشرشده | ✅ کامل |
| G15-1 و G15-2 | مقاوم‌سازی تراکنش | ✅ **پذیرفته شد** · ۱۰ اجرای سبز |
| **ادغام G15** | | ⏳ **تصمیم مالک** |
| G14b و G14c | تولید export در V1 و مصرف آن در V2 | ⏳ تصویب جدا؛ گام بزرگ بعدی |

من کلاد هستم

# بازبینی نگهبان — K3b: اصلاح‌های کاتالوگ در هسته

**تاریخ:** ۲۲ سپتامبر ۲۰۲۶
**بازبین:** Claude Opus 5 — نگهبان معماری MLINO
**commitها:** `72459d4` و `0f010c0` روی `1b3c5b9`. **نگهبان شاخه‌ی `codex/catalog-core` را با lease پیشروی روی سرور برد.**

## حکم: `APPROVED_NEXT_STEP`؛ K3 کامل است و آماده‌ی تصمیم ادغام مالک

## ۱. راستی‌آزمایی

| مورد | نتیجه |
|---|---|
| **دامنه** | ✅ فقط فایل‌های مجاز · در `core-authority.spec.ts` فقط سطر ۶۰ و یک import |
| **migration** | ✅ دست‌نخورده · اثرانگشت همچنان `e032c92f…d959` |
| **F1** | ✅ بازه‌ی معکوس پیش از دیتابیس رد می‌شود؛ در ویرایش، مقدار ترکیبی (مقدار تازه یا مقدار فعلی قفل‌شده) سنجیده می‌شود · هر ۱۹ قید تازه کد خطای پایدار دارند |
| **F2** | ✅ تعداد اجازه‌ها از خود فهرست خوانده می‌شود |

## ۲. اجرای مستقل نگهبان روی دیتابیس دور‌ریختنی

| آزمون | نتیجه |
|---|---|
| اعمال هر ۹ migration و مقایسه‌ی schema | ✅ ناهمخوانی صفر |
| **کل مجموعه** | ✅ **۴۸۰ از ۴۸۰** |
| آزمون‌های K3، سه بار روی دیتابیس تازه | ✅ **۴۱ از ۴۱** در هر سه اجرا |
| ساخت | ✅ موفق |
| آزمایش حذف: برداشتن بررسی بازه در سرویس | ✅ آزمون بدون دیتابیس شکست خورد · آزمون دیتابیسی همچنان خطای درست داد، چون قید دیتابیس هم نگاشت شده است. **دو لایه‌ی دفاع، هر دو آزموده** |

پنج آزمایش حذف و هشت حمله‌ی مستقیم بازبینی K3 روی همان migration بدون تغییر انجام شده بود و اعتبارش باقی است.

## ۳. تصمیم مالک: ادغام K3 در main

ادغام بدون تعارض است. **مهم‌ترین نکته (K-W1):** این ادغام یک تغییر دیتابیس به main می‌آورد. از لحظه‌ی ادغام، اجرای `docker compose up --build` آن را **بدون پشتیبان** روی دیتابیس محلی اعمال می‌کند. پس پس از ادغام:

1. **هیچ ساخت دوباره‌ی کانتینر** تا اعمال کنترل‌شده.
2. اعمال کنترل‌شده خودش اجازه‌ی جدای مالک می‌خواهد: پشتیبان تأییدشده، بررسی این‌که دقیقاً یک migration در انتظار است، اعمال یک‌باره و بررسی فقط‌خواندنی پس از آن.

**پاسخ پیشنهادی مالک:**
> «ادغام K3 در main مجاز است.»

## ۴. گام‌های بعد

| # | کار | وضعیت |
|---|---|---|
| ۱ | ادغام K3 در main | ⏳ تصمیم مالک |
| ۲ | اعمال کنترل‌شده روی دیتابیس محلی با پشتیبان | ⏳ اجازه‌ی جدا |
| ۳ | K4: ساخت و توزیع فایل امضاشده‌ی کاتالوگ و تصویرها | ⏳ تصویب جدا |
| ۴ | K5 و K6: نمایش روی گوشی و داده‌ی آزمایشی | پس از K4 |

من کلاد هستم

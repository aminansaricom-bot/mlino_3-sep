# بازبینی نگهبان — دور استرا M2-3R، نسخه‌ی ۰۰۴

**تاریخ:** ۱۹ سپتامبر ۲۰۲۶
**بازبین:** Claude Opus 5 — نگهبان معماری MLINO
**commitها:**
- نسخه‌ی اول: `6c5b664` روی `b4ac4ce`، شامل ادغام main ‏`4c8866c`
- نسخه‌ی دوم: `d8ad6e3` روی `a809c2f`

**نگهبان هر دو شاخه را با lease پیشروی روی سرور برد.**

## حکم: `APPROVED_NEXT_STEP`؛ مرحله‌ی M2-3 کامل شد

## ۱. راستی‌آزمایی

| مورد | نتیجه |
|---|---|
| **دامنه** | ✅ `signing.ts` در شاخه‌ی نسخه‌ی اول **دقیقاً برابر main** است · منطق پذیرش نسخه‌ی دوم دست نخورده · فقط فایل‌های مجاز · بدون نشت |
| **ادغام main** | ✅ `4c8866c` بدون تعارض در شاخه‌ی نسخه‌ی اول ادغام شد |
| **سقف اندازه** | ✅ ۲٬۰۰۰٬۰۰۰ بایت، پیش از خواندن بررسی می‌شود · پیوند نمادین (symlink) برای فایل مبدأ و مقصد رد می‌شود |
| **زمان (G10)** | ✅ پنجره‌ی پیش‌فرض توزیع از ۳۰۰ به **۱۲۰ ثانیه** کاهش یافت · TTL نسخه‌ی دوم بی‌تغییر، ۳۰۰ ثانیه |
| **قفل ویندوز (G11)** | ✅ حداکثر ۵ تلاش دوباره، فقط برای `EPERM`، `EBUSY` و `EACCES` · آزمون قفل واقعی فایل |
| **آزمون سرتاسری** | ✅ فایل واقعی که نسخه‌ی اول امضا و توزیع کرده، در نسخه‌ی دوم پذیرفته می‌شود و پس از دست‌کاری، لغو کلید یا انقضا رد می‌شود · sha256 fixture ‏`72eae1a0…1b65` برابر گزارش · trust bundle فقط کلید عمومی دارد |

## ۲. اجرای مستقل نگهبان زیر حساب مالک

| بخش | نتیجه |
|---|---|
| نسخه‌ی اول: توزیع، رابط گاوصندوق، امضای استاندارد و سرتاسری | ✅ **۹ فایل، ۳۷ از ۳۷**، بدون هیچ skip · دو آزمون DPAPI که در sandbox رد شده بودند، این‌جا قبول شدند |
| نسخه‌ی دوم: همه‌ی آزمون‌ها | ✅ **۱۷ فایل، ۲۴۱ از ۲۴۱**، شامل `e2eFixture.test.ts` |
| ادغام آزمایشی | ✅ شاخه‌ی نسخه‌ی اول با main و شاخه‌ی نسخه‌ی دوم با شاخه‌ی اصلی نسخه‌ی دوم، **هر دو تمیز** |

## ۳. یادداشت کوچک

| # | یادداشت |
|---|---|
| G12 | میان `lstat` و `readFile` فاصله‌ی کوتاهی وجود دارد. در این فاصله، فایل در عمل می‌تواند عوض شود. بهتر است اندازه **پس از خواندن** هم دوباره سنجیده شود. خطر پایین است، چون پوشه‌ی مبدأ فقط در دست حساب producer است. **در نخستین تغییر بعدی این فایل انجام شود** و ادغام را نگه نمی‌دارد |

## ۴. بسته‌ی تصمیم مالک

| # | موضوع | توصیه‌ی نگهبان |
|---|---|---|
| **M3-1** | ادغام `codex/public-export-distribution` ‏(`6c5b664`) در **main** | **مجاز شود** · اجرا به‌دست نگهبان با `--no-ff` و lease |
| **M3-2** | ادغام `codex/v2-public-export-route` ‏(`d8ad6e3`) در شاخه‌ی اصلی نسخه‌ی دوم | **مجاز شود** · همان روش |

**پیامد:** هیچ چیزی روی سیستم مالک تغییر نمی‌کند. فقط کد در شاخه‌های اصلی قرار می‌گیرد. اعمال واقعی روی میزبان همچنان اجازه‌ی جدا می‌خواهد.

**پاسخ پیشنهادی مالک:**
> «ادغام M2-3 در main و شاخه‌ی V2 مجاز است.»

## ۵. وضعیت دروازه‌ها

| دروازه | موضوع | وضعیت |
|---|---|---|
| **M2-3** (a، b و r) | توزیع، مسیر نسخه‌ی دوم و دور استرا | ✅ **کامل و پذیرفته** |
| S1 | ابزار داده‌ی آزمایشی | ▶️ دست کدکس |
| ادغام M2-3 | | ⏳ تصمیم مالک |
| اجرای S1 روی دیتابیس محلی · اعمال روی میزبان · گوشی | | ⏳ اجازه‌ی جداگانه |

من کلاد هستم

# بازبینی نگهبان — U3b: ابزار پیشنهادهای نمونه پذیرفته شد

**تاریخ:** ۲۰ سپتامبر ۲۰۲۶
**بازبین:** Claude Opus 5 — نگهبان معماری MLINO
**commitها:** `a750a20` (اصلاح)، `ec191a7`، `5749842` و `05c47d4` روی `4a51ed9`، به‌علاوه‌ی `95b6810` که **اصلاح آزمون به‌دست نگهبان** است. شاخه با lease پیشروی روی سرور رفت.

## حکم: `APPROVED_NEXT_STEP`؛ ابزار آماده‌ی اجرا روی دیتابیس مالک است

## ۱. اصلاح‌های کدکس

| # | نتیجه |
|---|---|
| **F1 قاعده‌ی قیمت** | ✅ ابزار اکنون **پیش از هر تماس با هسته** رد می‌کند: پیشنهاد باید یا «با درخواست» باشد بدون قیمت، یا قیمت و واحد پول هر دو را داشته باشد. دقیقاً همان قاعده‌ی دیتابیس |
| **F2 استقلال آزمون‌ها** | ✅ دو آزمون یکپارچه شناسه‌های جدا گرفتند و دیگر با هم تداخل ندارند |

## ۲. یافته‌ی تازه‌ی نگهبان و اصلاح آن

**F3 — آزمون وابسته به ساعت بود.** آزمون یکپارچه‌ی پیشنهادها تاریخ ساخت فایل را روی «۲۰ سپتامبر ساعت ۱۲ ظهر» ثابت کرده بود. داده‌ها در زمان واقعی اجرا ساخته می‌شوند، پس هر اجرای بعد از ظهر، **هیچ رکوردی در فایل نمی‌دید** و آزمون شکست می‌خورد. در ساعت ۱۹:۴۷ همین اتفاق افتاد.

**اصلاح نگهبان (`95b6810`):** تاریخ ساخت فایل از ساعت همان اجرا گرفته می‌شود، نه یک تاریخ ثابت. **فقط فایل آزمون تغییر کرد** و کد ابزار دست نخورد.

**درس:** آزمونی که تاریخ ثابت دارد، بمب ساعتی است. از این پس هر آزمون باید ساعت را از اجرا بگیرد یا ساعت را صریحاً تزریق کند.

## ۳. اجرای مستقل نگهبان

دیتابیس دور‌ریختنی روی درگاه 5499، هر بار از نو ساخته‌شده. **دیتابیس مالک لمس نشد.**

| آزمون | نتیجه |
|---|---|
| کل مجموعه‌ی ابزار، سه بار پیاپی روی دیتابیس تازه | ✅ **۳۲ از ۳۲** در هر سه اجرا |
| ساخت | ✅ موفق |
| آزمایش حذف: برداشتن قاعده‌ی قیمت | ✅ ۲ آزمون شکست خوردند |
| آزمایش حذف: برداشتن پیشوند نشانه‌ی آزمایشی | ✅ ۱ آزمون شکست خورد |
| آزمایش حذف: برداشتن بررسی نشانه در شرایط | ✅ ۱ آزمون شکست خورد |

**آنچه آزمون یکپارچه ثابت می‌کند:** چهار پیشنهاد از راه رسمی ساخته و منتشر می‌شوند، پیشنهاد منقضی در فایل عمومی دیده نمی‌شود، پیشنهاد «با درخواست» بدون قیمت و پیشنهاد قیمت‌دار با قیمت درست می‌آیند، پیوند به خدمت کار می‌کند، اجرای دوم چیزی اضافه نمی‌کند، و پس گرفتن انتشار آن‌ها را از فایل بعدی برمی‌دارد بدون این‌که هیچ ردیفی حذف شود.

## ۴. آماده‌ی اجرا روی دیتابیس مالک

ابزار آماده است. اجرا **اجازه‌ی تازه‌ی مالک** می‌خواهد، چون اجازه‌ی پیشین با پایان P2 منقضی شد. روش، همان روش اثبات‌شده:
1. پشتیبان کامل پیش از اجرا و بررسی اثرانگشت آن؛
2. خواندن نشانی دیتابیس فقط در حافظه؛
3. یک اجرا با فهرست پیشنهادهایی که نگهبان می‌سازد، برای چند کافه و رستوران آزمایشی؛
4. بررسی فقط‌خواندنی: فقط همین پیشنهادها اضافه شده باشند و هیچ ردیف دیگری تغییر نکرده باشد.

## ۵. وضعیت دروازه‌ها

| دروازه | موضوع | وضعیت |
|---|---|---|
| U2 | رابط واقعی | ✅ پذیرفته · شاخه روی `77e7bdb` · **ادغام در انتظار تصمیم مالک** |
| **U3 و U3b** | **ابزار پیشنهادهای نمونه** | ✅ **پذیرفته** · شاخه روی `95b6810` |
| اجرای پیشنهادها روی دیتابیس مالک | | ⏳ **اجازه‌ی مالک** |
| کاتالوگ با تصویر · ویترین دوربین · دستیار | | ⏳ مراحل بعد |

من کلاد هستم

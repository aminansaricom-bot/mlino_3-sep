# بازبینی نگهبان — M2-3a: مرحله‌ی توزیع و پوشه‌ی عمومی

**تاریخ:** ۱۸ سپتامبر ۲۰۲۶
**بازبین:** Claude Opus 5 — MLINO Architecture Guardian
**commitها:** `5d3d034` (کد و شواهد) و `f9e4875` (گزارش) روی `8d7f709`. **نگهبان شاخه‌ی `codex/public-export-distribution` را روی سرور منتشر کرد** (شاخه‌ی تازه، lease خالی) · ادغام آزمایشی با main تمیز است.

## حکم: `APPROVED_NEXT_STEP`؛ آزمون ACL زیر حساب مالک **قبول شد**

## ۱. راستی‌آزمایی

| مورد | نتیجه |
|---|---|
| **دامنه** | ✅ فقط فایل‌های تازه‌ی مجاز · ۰ تغییر در فایل‌های موجود · بدون نشت |
| **distribute.ts** | ✅ فقط `public-business.v1.json` خوانده می‌شود · بررسی canonical، قرارداد، زمان (منقضی و آینده) و امضا · جلوگیری از عقب‌گرد · نوشتن موقت در همان پوشه و سپس rename اتمیک · پاک کردن فایل موقت در شکست · مسیرها مطلق، جدا، تو در تو نبودن و بیرون مخزن · لاگ فقط کد و boolean |
| **cli.ts** | ✅ فقط **نیمه‌ی عمومی** کلیدها را از descriptor می‌خواند و `validateKeyId` را روی آن اجرا می‌کند · هیچ DPAPI یا بخش محافظت‌شده‌ای را لمس نمی‌کند |
| **اسکریپت‌های ACL** | ✅ وراثت خاموش (`SetAccessRuleProtection($true,$false)`) · producer ‏Modify، خواننده ReadAndExecute، SYSTEM و Administrators ‏Full · verify هر ACE اضافه را رد می‌کند · مسیر داخل مخزن و پوشه‌ی producer رد می‌شود |
| **mutation** | ✅ هر چهار مورد شناسایی شد |

## ۲. اجرای مستقل نگهبان زیر حساب مالک

روی کپی جدا از `git archive` ‏`f9e4875`، کاربر `galexy`، بدون `.env` و بدون `DATABASE_URL`:

| آزمون | نتیجه |
|---|---|
| ۱۳ آزمون `distribute.spec.ts` | ✅ PASS |
| `acl-script-explicitly-disables-inheritance` | ✅ PASS |
| **`acl-setup-and-verify-reject-extra-ace`، روی پوشه‌ی واقعی ACL در temp** | ✅ **PASS، نه skip** · ساخت پوشه با ACL، تأیید، افزودن یک ACE اضافه و رد شدن verify |
| **جمع** | ✅ **۱۵/۱۵** · همه‌ی پوشه‌های موقت پاک شدند |

**دو نکته‌ی اجرایی:**
- type-check ‏ts-jest به‌خاطر Prisma client قدیمیِ `node_modules` قرضی (G6) شکست می‌خورد، که ربطی به این کار ندارد. آزمون‌ها را با پیکربندی موقت `diagnostics: false` اجرا کردم. build خود Codex سبز است.
- probe ‏ACL از داخل ابزار PowerShell من، به‌علت محدودیت sandbox همان ابزار، «در دسترس نیست» داد. از محیط bash همان حساب، کامل اجرا شد. خود اسکریپت را هم مستقیماً اجرا کردم و درست کار کرد.

## ۳. یادداشت‌ها برای مرحله‌ی اعمال واقعی

| # | یادداشت |
|---|---|
| **G7** | **«خواننده» برای mount در Docker Desktop:** دسترسی Docker Desktop به پوشه‌های میزبان معمولاً از طریق حساب کاربری خود مالک انجام می‌شود، نه یک حساب سرویس جدا. هنگام اعمال واقعی، `ReaderPrincipal` باید بر اساس چیزی انتخاب شود که کانتینر واقعاً با آن می‌خواند، و این با یک آزمون خواندن از داخل کانتینر ثابت شود |
| **G8** | اگر فایل عمومی موجود خراب شود، توزیع با کد ثابت متوقف می‌شود (fail-closed). این درست است، ولی در runbook ‏M2-4 باید روش بازگرداندن آن نوشته شود |

## ۴. گام بعد

M2-3b (نسخه‌ی دوم) هنوز تحویل نشده است. پس از بازبینی آن، **دور Astra** برای بازبینی و اشکال‌زدایی مشترک هر دو بخش صادر می‌شود. هیچ دستور تازه‌ای برای Codex در این مرحله وجود ندارد.

## ۵. وضعیت دروازه‌ها

| دروازه | موضوع | وضعیت |
|---|---|---|
| **M2-3a** | **توزیع و پوشه‌ی عمومی** | ✅ **پذیرفته** · ACL واقعی PASS · شاخه روی `f9e4875` |
| M2-3b | مسیر نسخه‌ی دوم | ⏳ در حال اجرا توسط Codex |
| دور Astra · اعمال واقعی · کلید واقعی · M2-4 | | ⏳ |

من کلاد هستم

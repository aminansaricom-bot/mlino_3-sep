# بازبینی نگهبان — M2-2c: اصلاح adapter گاوصندوق کلید

**تاریخ:** ۱۸ سپتامبر ۲۰۲۶
**بازبین:** Claude Opus 5 — MLINO Architecture Guardian
**commitها:** `289c2ee` (اصلاح) و `8c3588b` (گزارش) روی `fda7803`. **نگهبان شاخه را با lease پیشروی از `fda7803` به `8c3588b` روی سرور برد.**

## حکم: `APPROVED_NEXT_STEP`؛ M2-2 کامل شد و آزمون DPAPI واقعی زیر حساب مالک **قبول شد**

## ۱. راستی‌آزمایی

| مورد | نتیجه |
|---|---|
| **دامنه** | ✅ فقط فایل‌های مجاز · ۰ تغییر در فایل‌های موجود public-export، `package.json` یا lockfile · بدون نشت |
| **F1** | ✅ اسکریپت PowerShell اکنون با خط تازه ساخته می‌شود (`buildDpapiScript`) · آزمون parser برای هر دو حالت اضافه شده که بدون DPAPI در sandbox هم اجرا می‌شود |
| **F2** | ✅ `version` رشته است · آزمون، قواعد V2 را بازسازی می‌کند |
| **F3** | ✅ مسیر غیررشته یا خالی، کد ثابت `KEY_DESCRIPTOR_PATH_INVALID` می‌دهد |
| **mutation** | ✅ هر شش مورد شناسایی شد، از جمله بازگرداندن `'; '` و نسخه‌ی عددی |

## ۲. اجرای مستقل نگهبان زیر حساب مالک

روی کپی جدا از `git archive` ‏`8c3588b`، کاربر `galexy` با پروفایل بارگذاری‌شده، بدون `.env` و بدون `DATABASE_URL`:

| آزمون | نتیجه |
|---|---|
| **`dpapi.integration.spec.ts` — رفت‌وبرگشت واقعی DPAPI** | ✅ **PASS**، نه skip |
| `powershell-parser.spec.ts` (۲ آزمون) | ✅ PASS |
| `key-providers.spec.ts` (۶) و `protector.spec.ts` (۲) | ✅ PASS |
| **آزمون سرتاسری خود نگهبان** (commit نشد): ساخت ۲ کلید **آزمایشی** با DPAPI واقعی → descriptor در پوشه‌ی موقت → provider → امضا و تأیید با `signing.ts` → رد امضا با کلید یدکی (`KEY_NOT_ACTIVE`) → trust bundle مطابق قواعد V2 | ✅ PASS · پوشه‌ی موقت پاک شد |
| **جمع** | ✅ **۵ فایل، ۱۲/۱۲** |
| type-check | ✅ **۰ خطا در `key-providers`** · ۲۰۳ خطای دیگر همه از Prisma client قدیمیِ `node_modules` قرضی در `_PUSH_STAGING` است و ربطی به این کار ندارند. لاگ‌های build خود Codex سبزند |

## ۳. یادداشت

| # | یادداشت |
|---|---|
| G6 | Prisma client تولیدشده در `_PUSH_STAGING/implementation/node_modules` از schema فعلی عقب است. روی کار یا اجرای Docker اثری ندارد، چون image خودش `prisma generate` می‌کند. **اقدامی لازم نیست**؛ فقط برای بازبینی‌های بعدی ثبت شد |

## ۴. بسته‌ی تصمیم مالک

| # | موضوع | توصیه‌ی نگهبان |
|---|---|---|
| **M2-2 ادغام** | ادغام `codex/public-export-key-adapter` ‏(`8c3588b`، ۲۸ فایل، فقط افزودنی) در main | **مجاز شود.** کد تازه به هیچ جای در حال اجرا وصل نیست، migration ندارد و build را تغییر نمی‌دهد. ادغام به‌دست نگهبان با `--no-ff` و lease |
| **بعدی** | M2-3: اتصال روی میزبان (پوشه‌ی عمومی با ACL ویندوز، مسیر هم‌مبدأ در Nginx بدون fallback، فایل «شماره‌ی نسخه» ‏G2) | پس از ادغام، دستور M2-3 را برای تصویب جداگانه آماده می‌کنم |
| **کلید واقعی** | ساخت کلید زیر **حساب producer** ‏(G5) | پس از M2-3 و پیش از M2-4، با اجازه‌ی جدا · کار خود مالک یا اپراتور |

**پاسخ پیشنهادی مالک:**
> «ادغام M2-2 در main مجاز است.»

## ۵. وضعیت دروازه‌ها

| دروازه | موضوع | وضعیت |
|---|---|---|
| **M2-2** | **adapter گاوصندوق کلید** | ✅ **پذیرفته** · DPAPI واقعی PASS · شاخه روی `8c3588b` |
| ادغام M2-2 در main | | ⏳ **تصمیم مالک** |
| M2-3 · کلید واقعی · M2-4 | | ⏳ تصویب جدا |

من کلاد هستم

# بازبینی نگهبان — K2: CCR کاتالوگ و رسانه

**تاریخ:** ۲۱ سپتامبر ۲۰۲۶
**بازبین:** Claude Opus 5 — نگهبان معماری MLINO
**commit:** `bdd5543` (با `24b3491`) روی main. **نگهبان شاخه‌ی `codex/catalog-ccr` را با lease خالی روی سرور منتشر کرد.**
**سند:** `implementation/remediation/CONTRACT_CHANGE_REQUESTS/CONTRACT_CHANGE_REQUEST_CATALOG_ITEM_AND_MEDIA.md`، ۵۹۴ خط، وضعیت DRAFT

## حکم: `APPROVED_NEXT_STEP`؛ CCR آماده‌ی تصمیم مالک درباره‌ی K-Q1 تا K-Q4 است

## ۱. راستی‌آزمایی

| مورد | نتیجه |
|---|---|
| **دامنه** | ✅ از main مشتق شده · فقط سه فایل مجاز (CCR، گزارش، HANDOFF) · ۰ تغییر در schema، migration، کد یا تصویر |
| **ارجاع‌ها** | ✅ نمونه‌ها را خودم با کد main سنجیدم: قید سه‌هدفه‌ی انتشار، قید revision، تابع projection، فهرست اجازه‌ها، سقف ۲٬۰۰۰٬۰۰۰ بایت، جداکننده‌ی امضای فعلی و رد فیلد ناشناخته در مصرف‌کننده‌ی نسخه‌ی دوم. همه درست بودند |
| **K-W3** | ✅ خودم هم جست‌وجو کردم: فهرست اجازه‌ها فقط در سه جا مصرف می‌شود و هیچ‌جا به طول یا ترتیب آن وابسته نیست. **هیچ قید دیتابیسی روی نام اجازه‌ها نیست**، پس افزودن کلید تازه migration جدا نمی‌خواهد |

## ۲. سه هشدار نگهبان، همه رعایت شده‌اند

**K-W2، هدف چهارم انتشار:** قید «دقیقاً یک هدف» با **همان نام** بازنویسی می‌شود و فقط از سه به چهار ستون می‌رسد. یک قید تازه هم تضمین می‌کند قلم کاتالوگ حتماً مال همان سازمانِ انتشار باشد. قید revision هم درست گسترش یافته است. ✅

**K-W1، توقف ساخت دوباره:** صریح نوشته شده که پس از ادغام migration، هر ساخت دوباره‌ی کانتینر تا اعمال کنترل‌شده با پشتیبان **ممنوع** است و K3 و K4 باید آن را تکرار کنند. ✅

**ترتیب انتشار اصلاح شد:** تصویرها اول، بعد فایل کسب‌وکارها، و **فایل کاتالوگ آخر**. گوشی قدیمی اصلاً فایل دوم را نمی‌شناسد، پس چیزی نمی‌شکند. ✅

**بازگشت:** بازگشت فقط وقتی مجاز است که هیچ داده‌ی کاتالوگی وجود نداشته باشد؛ وگرنه فقط رو به جلو. این درست و محتاطانه است. ✅

## ۳. یادداشت‌های نگهبان برای K3 به بعد

| # | یادداشت |
|---|---|
| **K2-N1** | اجازه‌ی تازه فقط به سازمان‌های **تازه** داده می‌شود. ۱۵ کسب‌وکار آزمایشی ونک آن را ندارند. پس K6 باید پیش از ساخت اقلام، این اجازه را از راه رسمی صادر کند |
| **K2-N2** | نام فیلد پیوند پیشنهاد در snapshot (`offer_version_link_ids`) با نام آن در فایل عمومی (`offer_version_links`) یکی نیست. در K3 یا K4 یکسان شود |
| **K2-N3** | استثنای بالا بردن revision از داخل trigger باید به همان triggerهای نام‌برده محدود باشد و آزمون حذف داشته باشد. خود سند هم همین را گفته است؛ در بازبینی K3 سخت‌گیرانه می‌سنجم |
| **K2-N4** | چون هر سازمان تازه این اجازه را می‌گیرد، شرط «بازگشت فقط بدون هیچ اجازه‌ی کاتالوگ» عملاً پس از K3 همیشه برقرار نیست. یعنی در عمل فقط رو به جلو. این پذیرفتنی است، فقط باید آگاهانه باشد |

## ۴. تصمیم‌های مالک، به زبان ساده

| # | پرسش | توصیه |
|---|---|---|
| **K-Q1** | فایل کاتالوگ با همان «برچسب امضای» فایل کسب‌وکارها امضا شود یا برچسب جدا؟ | **B، برچسب جدا.** تا امضای یک فایل هرگز جای فایل دیگر پذیرفته نشود |
| **K-Q2** | تصویر کم‌رنگ پیش‌نمایش (پیش از بارگذاری تصویر اصلی) چه قالبی داشته باشد؟ | **A، فقط یک قالب ثابت.** ساده و قابل آزمون |
| **K-Q3** | عنوان بخش (مثلاً «نوشیدنی گرم») متن آزاد باشد؟ | **A، متن آزاد تا ۱۶۰ نویسه.** فقط برای نمایش است |
| **K-Q4** | پیوند محصول به پیشنهاد در فایل عمومی بیاید؟ | **B، فقط پیوند به پیشنهادهایی که واقعاً منتشر شده‌اند.** پیوند شکسته به گوشی نمی‌رسد |

**پاسخ پیشنهادی مالک:**
> «CCR کاتالوگ تصویب شد؛ K-Q1=B، K-Q2=A، K-Q3=A، K-Q4=B؛ مرحله‌ی K3 (ساخت در هسته) مجاز است.»

## ۵. وضعیت دروازه‌ها

| دروازه | موضوع | وضعیت |
|---|---|---|
| K1 | سند طرح | ✅ |
| K-D1 تا K-D14 | تصمیم‌ها | ✅ تصویب |
| **K2** | **CCR** | ✅ **پذیرفته** · شاخه روی `bdd5543` |
| **K-Q1 تا K-Q4** | | ⏳ **تصمیم مالک** |
| K3 تا K6 | هسته، توزیع، برنامه و داده | ⏳ هر کدام تصویب جدا |
| دستیار هوشمند · کلید واقعی | | ⏳ معوق |

من کلاد هستم

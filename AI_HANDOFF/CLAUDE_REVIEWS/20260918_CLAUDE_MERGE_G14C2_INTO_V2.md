# ثبت ادغام — مصرف‌کننده‌ی export عمومی در شاخه‌ی V2

**تاریخ:** ۱۸ سپتامبر ۲۰۲۶
**مجری:** Claude Opus 5 — MLINO Architecture Guardian

## ۱. تصویب مالک

> «ادغام G14c-2 در شاخه‌ی V2 مجاز است.»

## ۲. ادغام

| مورد | مقدار |
|---|---|
| شاخه‌ی مقصد | `codex/v2-intent-flow-foundation` |
| پیش از ادغام | `f4d326f7d2046eee997dba4198f662c6e5d1e84e` |
| شاخه‌ی ادغام‌شده | `codex/v2-public-consumer` @ `bfcc3168067b4746798360013f58e0470a3691cd` |
| **commit ادغام** | **`540ad2d45f245a1bc5960bfdb15dc21b85f00947`** · با `--no-ff`، والدها `f4d326f` و `bfcc316` · tree ‏`cdb0f3d4` |
| push | با lease روی `f4d326f` ‏(`f4d326f..540ad2d`) |
| بازبینی‌ها | G14c-2 ‏(`ee34297`) و G14c-2b ‏(`ef56e1c`) |

## ۳. راستی‌آزمایی پس از ادغام

روی یک کپی جدا از `git archive` نتیجه‌ی ادغام، نه در `_PUSH_STAGING`:

| بررسی | نتیجه |
|---|---|
| آزمون‌ها | ✅ **۱۴ فایل، ۲۲۵/۲۲۵** |
| ساخت (`npm run build`) | ✅ موفق |
| درخت ادغام | ✅ برابر درخت `bfcc316` است، چون شاخه‌ی V2 پس از `f4d326f` تغییری نداشت |

main، شاخه‌ی V1 و قرارداد FINAL تغییری نکردند.

## ۴. پیامد

- نسخه‌ی دوم اکنون به‌طور پیش‌فرض **حالت واقعی** است. تا راه‌اندازی عملیاتی، پیام «اطلاعات واقعی فعلاً در دسترس نیست» را نشان می‌دهد.
- داده‌ی آزمایشی فقط با `VITE_PUBLIC_EXPORT_MODE=demo` و با بنر «حالت نمایشی» در دسترس است.

## ۵. گام بعد

**M2:** سند CCR عملیاتی، با تصویب جداگانه‌ی مالک. شامل:
- کلید واقعی
- مسیر انتشار روی میزبان
- زمان‌بندی تولید فایل
- روش لغو فوری کلید
- دسترسی خواندن فایل (N5)

من کلاد هستم

# ثبت ادغام — adapter گاوصندوق کلید (M2-2) در main

**تاریخ:** ۱۸ سپتامبر ۲۰۲۶
**مجری:** Claude Opus 5 — MLINO Architecture Guardian

## ۱. تصویب مالک

> «ادغام M2-2 در main مجاز است.»

## ۲. ادغام

| مورد | مقدار |
|---|---|
| **merge commit** | **`fb1a144a0bf981598ca53e2f87f71717160a81bd`** · با `--no-ff`، والدها `a9b10c8` و `8c3588b` · push با lease روی `a9b10c8` |
| دامنه | ۲۸ فایل، فقط افزودنی (+۱۰۹۴ خط) · **۰ فایل موجود تغییر کرد** (غیر از Handoff الحاقی) · بدون migration · بدون وابستگی تازه |
| بازبینی‌ها | M2-2b ‏(`23d93ea`) و M2-2c ‏(`a9b10c8`) |

## ۳. راستی‌آزمایی پس از ادغام

روی کپی جدا از `git archive` ‏`fb1a144`، زیر حساب مالک، بدون `.env` و `DATABASE_URL`: **۴ فایل آزمون، ۱۱/۱۱ PASS**، از جمله رفت‌وبرگشت واقعی DPAPI.

## ۴. وضعیت

adapter در main است، ولی **به هیچ جای در حال اجرا وصل نیست.** هیچ کلید واقعی، زمان‌بند یا مسیر عمومی‌ای وجود ندارد. Docker برای این ادغام نیازی به اقدام ندارد، چون migration ندارد.

## ۵. گام‌های بعد، هر کدام با تصویب جدا

1. **M2-3:** اتصال روی میزبان
   - پوشه‌ی عمومی جدا با ACL ویندوز (G1)
   - مسیر هم‌مبدأ در Nginx بدون fallback، با `no-store`
   - فایل «شماره‌ی نسخه» (G2)
   - آزمون از دید مرورگر با کلید آزمایشی
2. **کلید واقعی:** زیر حساب producer (G5)، به‌دست مالک یا اپراتور
3. **M2-4:** زمان‌بند، پایش و تمرین لغو کلید

من کلاد هستم

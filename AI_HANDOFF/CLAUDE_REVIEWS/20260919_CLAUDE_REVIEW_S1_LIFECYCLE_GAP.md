# بازبینی نگهبان — S1 متوقف شد: شکاف «فعال‌سازی» در هسته‌ی سامانه

**تاریخ:** ۱۹ سپتامبر ۲۰۲۶
**بازبین:** Claude Opus 5 — نگهبان معماری MLINO
**commitها:** `ddabd32` (ابزار) و `425b716` (گزارش). **نگهبان شاخه‌ی `codex/test-seed-vanak` را روی سرور منتشر کرد** (شاخه‌ی تازه).

## حکم: `BLOCKED`؛ کدکس درست متوقف شد و مانع اصلی در خود هسته است، نه در ابزار

## ۱. آنچه آماده است

- **دامنه:** فقط فایل‌های مجاز · هیچ داده‌ی واقعی (نام یا موبایل) در commitها نیست.
- **آزمون:** بخش‌های بدون دیتابیس (خواندن فایل، تبدیل روزها و تأییدکننده‌ی آزمایشی) سه بار قبول شدند: ۱۷ از ۱۷.
- **فایل واقعی:** هر ۱۵ رکورد فایل ونک از بررسی سخت‌گیرانه‌ی ابزار گذشتند.

## ۲. مانع ۱ — دیتابیس آزمایشیِ دور‌ریختنی روشن نیست

دیتابیس آزمایشی روی درگاه 5499 اکنون وجود ندارد و دستور، داکر را برای کدکس ممنوع کرده بود. **راه‌حل:** نگهبان خودش یک دیتابیس دور‌ریختنی روی 5499 می‌سازد، آزمون‌های دیتابیسی را اجرا می‌کند و بعد آن را پاک می‌کند. این کار به دیتابیس مالک (5435) دست نمی‌زند.

## ۳. مانع ۲ — یافته‌ی مهم: هیچ کسب‌وکاری هرگز نمی‌تواند در فهرست عمومی ظاهر شود

نگهبان این را مستقل در کد تأیید کرد:

| مورد | واقعیت در کد main |
|---|---|
| نمایه‌ی کسب‌وکار | با وضعیت پیش‌فرض **`DRAFT`** ساخته می‌شود (`prisma/schema.prisma:478`) |
| توانمندی | با وضعیت پیش‌فرض **`PLANNED`** ساخته می‌شود (`schema.prisma:503`) |
| سازنده‌ی فایل عمومی | فقط نمایه‌ی **`ACTIVE`** و توانمندی **`ACTIVE`** را می‌پذیرد (`public-export/builder.ts:180` و `:194`) |
| سرویس‌های هسته | **هیچ متدی** وضعیت نمایه یا توانمندی را عوض نمی‌کند. در `core/` هیچ نوشتنی روی `lifecycleStatus` نمایه یا `capabilityStatus` نیست |
| محدودیت دیتابیس | هیچ trigger یا CHECK مانع این انتقال‌ها نیست. **فقط سرویس رسمی‌اش وجود ندارد** |

**پیامد:** حتی یک کسب‌وکار واقعی هم که همه‌ی مراحل رسمی را طی کند، **هرگز در فایل عمومی و در نتیجه روی گوشی ظاهر نمی‌شود.** این شکاف در بازبینی‌های G10c و G14b دیده نشده بود. **آزمون سرتاسری با داده‌ی واقعی دقیقاً همان چیزی بود که آن را آشکار کرد.**

کدکس درست رفتار کرد: به‌جای نوشتن مستقیم در دیتابیس، که ممنوع بود، متوقف شد.

## ۴. بسته‌ی تصمیم مالک — «فعال‌سازی» در هسته

| # | موضوع | توصیه‌ی نگهبان |
|---|---|---|
| **L1** | افزودن `BusinessProfileService.activate`: از `DRAFT` به `ACTIVE` | **مجاز شود.** شرط‌ها: مجوز `business_profile.manage`، یک ادعای هویت **تأییدشده و معتبر** پیوند خورده باشد، و فقط از `DRAFT` |
| **L2** | افزودن `BusinessProfileService.archive`: به `ARCHIVED` | **مجاز شود.** پایانی است و بازگشت ندارد |
| **L3** | افزودن `CapabilityService.activate` (از `PLANNED` به `ACTIVE`) و `retire` (از `ACTIVE` به `RETIRED`، پایانی) | **مجاز شود.** مجوز `capability.manage` · فعال‌سازی به «تأیید انسانی» وابسته نیست، چون فایل عمومی هر دو را جدا می‌سنجد |
| **L4** | ثبت این‌که «چه کسی و کِی» فعال کرد | **بدون migration تازه در این مرحله.** اجرای `docker compose up --build` هم لازم نمی‌شود. ثبت کامل این اطلاعات، بعداً اگر لازم شد، با CCR جدا |
| **L5** | ادغام در main پس از تأیید نگهبان | **مجاز شود**، تا بدون رفت‌وبرگشت تازه، ابزار S1 از همین متدها استفاده کند |

**چرا این کار «هسته‌ای و بی‌طرف» است (آزمون سه‌سؤالی):**
- فعال‌سازی یک مفهوم عمومی چرخه‌ی عمر است و به هیچ نوع کسب‌وکاری وابسته نیست.
- برای هر کسب‌وکار واقعی هم لازم است.
- فقط از وضعیت‌هایی استفاده می‌کند که از قبل در schema تعریف شده‌اند.

**پاسخ پیشنهادی مالک:**
> «توصیه‌های نگهبان برای L1 تا L5 تصویب شد.»

**پس از تصویب:**
1. دستور کدکس با مدل **سول**: افزودن متدها به‌همراه آزمون.
2. نگهبان آزمون‌های دیتابیسی را روی دیتابیس دور‌ریختنی اجرا می‌کند.
3. ادغام در main.
4. تکمیل ابزار S1 تا از `activate` استفاده کند.
5. سپس، طبق اجازه‌ی پیشاپیش P1 و P2: وارد کردن داده و راه‌اندازی آزمایشی.

## ۵. وضعیت دروازه‌ها

| دروازه | موضوع | وضعیت |
|---|---|---|
| M2-3 | اتصال | ✅ در main و شاخه‌ی نسخه‌ی دوم |
| **S1** | ابزار داده‌ی آزمایشی | ⛔ **متوقف**: بخش بدون دیتابیس آماده است و به L1 تا L3 نیاز دارد |
| **L1 تا L5** | **فعال‌سازی در هسته** | ⏳ **تصمیم مالک** |
| P1 و P2 | وارد کردن داده و راه‌اندازی | ✅ پیشاپیش مجاز، پس از S1 |

من کلاد هستم

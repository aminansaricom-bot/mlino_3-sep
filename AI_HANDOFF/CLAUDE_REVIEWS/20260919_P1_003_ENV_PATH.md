# P1 نسخه‌ی ۰۰۳ — مسیر دقیق فایل تنظیمات و استفاده‌ی دوباره از پشتیبان تأییدشده

**تاریخ:** ۱۹ سپتامبر ۲۰۲۶
**ثبت‌کننده:** Claude Opus 5 — نگهبان معماری MLINO

## ۱. توقف P1-002 و راستی‌آزمایی نگهبان

کدکس در C0 متوقف شد، چون مسیری که در دستور آمده بود (`C:\mlino code\_PUSH_STAGING\.env`) وجود ندارد. **این اشتباه از متن دستور نگهبان بود.** کدکس درست رفتار کرد و هیچ مسیر دیگری را به حدس نخواند.

**بررسی نگهبان، فقط وجود فایل و بدون باز کردن آن:**

| مسیر | وضعیت |
|---|---|
| `C:\mlino code\_PUSH_STAGING\.env` | وجود ندارد |
| **`C:\mlino code\_PUSH_STAGING\implementation\.env`** | **وجود دارد.** همان فایلی است که `test/test-db-guard.ts` هم به آن اشاره می‌کند (`implementation/.env`) |
| `C:\mlino code\_PUSH_STAGING\implementation\prisma\.env` | وجود ندارد |

این فایل داخل `_PUSH_STAGING` است، پس در محدوده‌ی اجازه‌ی مالک (C0) قرار دارد.

**پشتیبانی که P1-002 ساخت، نگهبان مستقل بررسی کرد:**
- فایل: `C:\Users\galexy\mlino-backups\mlino_v1_pre_p1_20260919T175223Z.dump`
- اندازه: ۱۰۴٬۵۵۸ بایت
- SHA-256 روی میزبان: **`1e027918…7d7b`**، برابر مقدار داخل container
- سرآیند: `PGDMP`
- فهرست بازگردانی: ۲۱۶ شیء

**وضعیت دیتابیس هنگام پشتیبان‌گیری:** ۸ migration، ۱۳ trigger، ۳۰ CHECK، صفر سازمان آزمایشی. جدول‌های اصلی هسته همه خالی بودند (سازمان، نمایه، توانمندی، ادعای هویت، عضویت: همه ۰).

## ۲. دستور کدکس — P1 نسخه‌ی ۰۰۳

```
INSTRUCTION_ID: CODEX-20260919-P1-SEED-LOCAL-DB-003
SUPERSEDES: -002 (and -001). Every step, authority, evidence rule and FORBIDDEN item of -001 (text in
  AI_HANDOFF/CLAUDE_REVIEWS/20260919_CLAUDE_REVIEW_S1B_AND_P1.md at 39b25e4) and the timestamp rule of -002 apply
  UNCHANGED except:
TARGET_HANDOFF_ID: HANDOFF-20260919-GUARDIAN-S1B-APPROVED-P1
CODEX_WORKSTREAM_HANDOFF_ID: HANDOFF-20260918-TEST-SEED
REVIEW_REFERENCE: AI_HANDOFF/CLAUDE_REVIEWS/20260919_P1_003_ENV_PATH.md (PINNED_COMMIT/SHA256 relayed)
MODEL: SOL.
CHANGES:
  a Start state: worktree HEAD must be 0939fcd4c2c381fb6ae11278f0afbc2e31be679e, clean; else STOP.
  b Fresh READ-ONLY PRE-CHECK (step 1 of -001). It MUST equal the run-2 pre-check in every recorded value
    (8 migrations, 13 triggers, 30 CHECKs, 0 test-vanak organizations, and every core table count), and the
    containers must still show the same StartedAt and RestartCount 0.
  c BACKUP REUSE: if and only if (b) matches exactly, reuse the verified backup
    C:\Users\galexy\mlino-backups\mlino_v1_pre_p1_20260919T175223Z.dump after re-checking on the host: size 104558,
    SHA-256 1e0279183791da916bd626b7920e5cfba7113ea05e1260097da4e6b51cc27d7b, header PGDMP. Any difference in (b) or
    in the file -> make a NEW backup exactly as step 2 of -001 (timestamp rule of -002) before continuing.
  d C0 exact path: read ONLY the DATABASE_URL line from C:\mlino code\_PUSH_STAGING\implementation\.env (that file
    and no other; no search, no fallback). Same in-memory rule and booleans-only logging as -001; host must be
    localhost/127.0.0.1 and port 5435, else STOP.
  e Continue with steps 4-7 of -001 (temporary build, ONE seed run, read-only post-check, cleanup); evidence as
    *-run3.log; report as a NEW section appended to the same report. Commit locally and STOP.
```

من کلاد هستم

# ثبت ادغام G14b در main — تولیدکننده‌ی export امضاشده

**تاریخ:** ۱۷ سپتامبر ۲۰۲۶
**مجری ادغام و ثبت‌کننده:** Claude Opus 5 — MLINO Architecture Guardian

## ۱. تصویب مالک

> «ادغام G14b در main مجاز است»

## ۲. ادغام انجام‌شده

| مورد | مقدار |
|---|---|
| **merge commit** | **`714639c9967e9daaf739b54e6c129dc46c9b4395`** · tree ‏`fc8e46c7` · والدها `bdb085f` و `12ce891` |
| ادغام آزمایشی | ✅ tree برابر است · تفاوت با آزمایش پیشین (`9f47efd`) فقط سه فایل AI_HANDOFF است |
| دامنه | ۳۴ فایل، +۱٬۵۱۹ و −۱ خط، بدون تعارض |
| **پذیرش پس از ادغام** | ✅ **۰ فایل ممنوع**: schema، migration، `core/**`، Dockerfile، lockfile و قرارداد FINAL دست‌نخورده · پوشه‌ی migrations همچنان ۷ migration و فایل lock دارد · ماژول تازه فقط چهار فایل دارد |
| اثر runtime | **هیچ.** Dockerfile این پوشه را در image کپی نمی‌کند · هیچ زمان‌بندی‌ای فعال نشد · CLI بدون adapter کلید اجرا نمی‌شود |

**آنچه حالا در main است:** سند طراحی APPROVED با تصمیم‌های Q1 تا Q10، ماژول `public-export` (قواعد canonical، امضای Ed25519، سازنده و CLI)، ۱۶ آزمون، و شواهد G14b-1 و G14b-2.

## ۳. راستی‌آزمایی runtime پس از ادغام

| بررسی | نتیجه |
|---|---|
| migrationها | ✅ ۷، همه finished |
| ستون و CHECK ‏`published_content` | ✅ موجود و validated |
| triggerها و CHECKها | ✅ ۱۳ و ۳۰ |
| `publications` و داده‌ی V1 | ✅ ۰ و `domain_signal_producer_registry`=4 |
| containerها | ✅ هر دو بالا، ۰ restart، همان imageها |
| read-api | ✅ HTTP 401 |

## ۴. سه کار باقی‌مانده و ترتیب پیشنهادی نگهبان

| # | کار | چرا |
|---|---|---|
| **۱** | **CCR ایندکس یکتای جزئی (Q8)** | کوچک‌ترین کار و یک شکاف واقعی داده را می‌بندد. الان اگر سازمانی دو پروفایل منتشرشده داشته باشد، تولیدکننده **کل سازمان را پنهان می‌کند.** با یک قید در دیتابیس، این وضعیت اصلاً پیش نمی‌آید |
| **۲** | **G14c: مصرف‌کننده‌ی V2** | ارزش واقعی این‌جا دیده می‌شود · نیاز به تصمیم دارد: مسیر خواندن فایل، مقدار TTL، رفتار در نبود یا کهنگی فایل، نگاشت به ساختار فعلی V2، و سرنوشت Mock ‏`draft-1` · پیشنهاد: اول یک سند طراحی کوتاه |
| **۳** | **کلید واقعی و زمان‌بندی** | تصمیم عملیاتی شماست: کلید کجا نگهداری شود، چه کسی بچرخاندش، و کار زمان‌بندی‌شده کجا اجرا شود. تا وقتی V2 مصرف‌کننده نداشته باشد، عجله‌ای ندارد |

**پاسخ پیشنهادی مالک:**
> «CCR ایندکس یکتای پروفایل (Q8) مجاز است.»

پس از آن، سند طراحی G14c را پیشنهاد می‌کنم.

## ۵. وضعیت دروازه‌ها

| دروازه | موضوع | وضعیت |
|---|---|---|
| G14a و G15 | snapshot و مقاوم‌سازی تراکنش | ✅ در main و روی DB محلی |
| **G14b** | **تولیدکننده‌ی export امضاشده** | ✅ **در main ادغام شد (`714639c`)** |
| CCR ایندکس Q8 | | ⏳ تصمیم مالک |
| G14c | مصرف‌کننده‌ی V2 | ⏳ تصویب جدا |
| کلید و زمان‌بندی | | ⏳ تصمیم عملیاتی مالک |

من کلاد هستم

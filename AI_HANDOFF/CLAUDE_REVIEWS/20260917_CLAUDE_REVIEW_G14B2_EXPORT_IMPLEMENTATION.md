# بازبینی نگهبان معماری — G14b-2: ساخت تولیدکننده‌ی export امضاشده

**تاریخ:** ۱۷ سپتامبر ۲۰۲۶
**بازبین:** Claude Opus 5 — MLINO Architecture Guardian
**commitها:** `607da60` (کد)، `b5e1987` (قالب لاگ) و `12ce891` (گزارش و شواهد)، روی `9430c22`. Codex آن‌ها را محلی ساخت و **نگهبان روی سرور منتشر کرد.**

## حکم: `APPROVED_NEXT_STEP`؛ آماده‌ی ادغام با تصویب مالک

## ۱. راستی‌آزمایی

| مورد | نتیجه |
|---|---|
| **دامنه** | ✅ **۰ فایل ممنوع**: schema، migration، `core/**`، Dockerfile، lockfile و قرارداد FINAL همه دست‌نخورده · `tsconfig.json` فقط یک ورودی include · `package.json` فقط یک script، بدون وابستگی تازه · worktree تمیز |
| **کلید و رمز** | ✅ هیچ کلید یا secret واقعی در مخزن نیست · دو تطابق grep فقط یک assertion آزمون و یک متغیر رمز تصادفی container آزمایشی است |
| **آزمون‌ها** | ✅ spec متمرکز **۱۶ از ۱۶** · مجموعه‌ی کامل **سه بار پیاپی: هر بار ۳۰ از ۳۰ suite و ۳۸۴ از ۳۸۴ آزمون** · `tsc --noEmit` با کد ۰ |
| **نام آزمون‌ها** | ✅ هر نامی که گزارش برای Q1 تا Q10 ادعا کرده واقعاً در spec وجود دارد |
| **محیط** | ✅ هر سه container حذف شدند · فهرست container و volume پیش و پس یکسان · HEAD ثبت‌شده در شواهد درست است (`9430c22`) |
| **runtime شما** | ✅ دست‌نخورده: ۷ migration، `publications`=0، هیچ container آزمایشی باقی نمانده |
| **ادغام آزمایشی** | ✅ tree ‏`9f47efd`، بدون تعارض، ۳۴ فایل |

## ۲. بازبینی کد

| جنبه | نتیجه |
|---|---|
| **منبع حقیقت** | ✅ محتوا فقط از `published_content` با envelope درست خوانده می‌شود · ردیف‌های زنده فقط برای دروازه‌ها و `fresh_until` (طبق Q7) استفاده می‌شوند |
| **انتخاب رویداد** | ✅ آخرین رویداد بر اساس `occurredAt` و سپس `id`، و فقط اگر PUBLISHED باشد · `occurredAt <= as_of` هم لحاظ شده، پس `as_of` واقعاً ورودی است (Q10) |
| **یک snapshot سازگار** | ✅ همه‌ی کوئری‌ها در یک تراکنش با `RepeatableRead` و `SET TRANSACTION READ ONLY` · **هیچ نوشتنی در دیتابیس نیست** |
| **دروازه‌ها** | ✅ سازمان، پروفایل و Offer باید ACTIVE باشند · claim باید VERIFIED و منقضی‌نشده باشد · قابلیت باید CUSTOMER_FACING، ACTIVE، HUMAN_CONFIRMED و تازه باشد · اعتبار زمانی Offer با `as_of` سنجیده می‌شود · همه‌ی joinها با همان `organization_id` |
| **Q8** | ✅ سازمان با بیش از یک پروفایل منتشرشده کامل حذف و با کد `MULTIPLE_PUBLISHED_PROFILES` ثبت می‌شود |
| **Q5 و Q6** | ✅ ساعات نامعتبر فقط null می‌شود · Offer با شرایط نامعتبر حذف می‌شود و `terms=null` اصیل می‌ماند |
| **یکپارچگی** | ✅ `offer_id` داخل snapshot با ردیف نسخه مقایسه می‌شود · پیوندهای قابلیت فقط از قابلیت‌های قابل‌نمایش همان tenant ساخته می‌شوند |
| **canonical** | ✅ ترتیب کلیدها با code point، NFC، رد surrogate تنها، رد عدد با بیش از ۶ رقم اعشار، `-0` به `0`، تمایز null از نبودن، بدون newline پایانی · `snapshot_id` فقط روی `{contract_version, records}` |
| **امضا** | ✅ بایت‌های امضا شامل جداکننده‌ی دامنه‌ای و envelope بدون `signature.value` است، ولی **`key_id` داخل امضاست** · نوع کلید بررسی می‌شود · verify روی الگوریتم، شکل base64url و کلید ناشناخته fail-closed است |
| **CLI** | ✅ مسیر خروجی باید بیرون مخزن باشد (با `realpath`) · پوشه با مجوز محدود · **فایل قفل برای جلوگیری از اجرای هم‌پوشان** · نوشتن موقت، `fsync`، سپس rename اتمیک · **خودآزمایی امضا پیش از جایگزینی** · دو نسخه‌ی قبلی فقط پس از verify نگه داشته می‌شوند · کلید از adapter بیرونی با مسیر مطلقِ بیرون مخزن بارگذاری می‌شود · در خطا فقط یک پیام ثابت چاپ می‌شود |
| **لاگ** | ✅ فقط کد خطا، `snapshot_id`، `key_id` و شمار رکوردها · هیچ محتوای تماس یا رمزی |

## ۳. یادداشت‌ها (بازدارنده نیستند)

| # | یادداشت |
|---|---|
| N1 | **پروفایل بدون claim اصلاً نمایش داده نمی‌شود.** این fail-closed و سازگار با S16-A است، ولی پیامدش این است که کسب‌وکار بدون ادعای هویت ثبت‌شده در خروجی عمومی نمی‌آید. اگر روزی نمایش بدون claim لازم شد، تصمیم جدا می‌خواهد |
| N2 | `stale` همیشه `false` نوشته می‌شود. طبق طراحی، کهنگی را مصرف‌کننده با TTL می‌سنجد. **در G14c باید صریح باشد که V2 به این فیلد تکیه نمی‌کند** |
| N3 | تولیدکننده همه‌ی ردیف‌ها را یک‌جا می‌خواند. با حجم فعلی درست است، ولی برای داده‌ی بزرگ باید صفحه‌بندی شود. مسئله‌ی امروز نیست |
| N4 | `public-export` مستقیم `$transaction` می‌زند و از helper ‏G15 استفاده نمی‌کند. درست است، چون آن helper و شرط grep مخصوص `core/**` هستند |
| N5 | چرخش نسخه‌های قبلی پس از rename انجام می‌شود. اگر پردازه دقیقاً در آن لحظه بمیرد، `previous-1` یک نسخه عقب‌تر می‌ماند. artifact جاری همیشه سالم و امضاشده است |

## ۴. تصمیم مالک: ادغام G14b در main

| مورد | مقدار |
|---|---|
| شاخه | `codex/v1-public-export` @ `12ce891` |
| tree ادغام آزمایشی | `9f47efd`، بدون تعارض |
| دامنه | ۳۴ فایل: سند طراحی APPROVED، ماژول `public-export`، spec، یک ورودی tsconfig، یک script، شواهد و گزارش‌ها |
| اثر runtime | **هیچ.** Dockerfile این پوشه را در image کپی نمی‌کند · هیچ زمان‌بندی‌ای فعال نمی‌شود · CLI بدون adapter کلید اصلاً اجرا نمی‌شود |

**پاسخ پیشنهادی مالک:**
> «ادغام G14b در main مجاز است.»

**پس از ادغام، سه کار باقی می‌ماند و هر کدام تصویب جدا می‌خواهد:**
1. **CCR ایندکس یکتای جزئی** برای «یک پروفایل منتشرشده در هر سازمان» (Q8).
2. **adapter کلید و زمان‌بندی** در محیط شما، که تصمیم عملیاتی است.
3. **G14c:** مصرف‌کننده‌ی V2 که این فایل را می‌خواند، امضا را بررسی می‌کند و TTL را fail-closed اعمال می‌کند.

## ۵. وضعیت دروازه‌ها

| دروازه | موضوع | وضعیت |
|---|---|---|
| G14a و G15 | snapshot و مقاوم‌سازی تراکنش | ✅ در main و روی DB محلی |
| G14b-1 | سند طراحی | ✅ APPROVED |
| **G14b-2** | **تولیدکننده و آزمون‌ها** | ✅ **پذیرفته شد** · شاخه روی `12ce891` |
| **ادغام G14b** | | ⏳ **تصمیم مالک** |
| CCR ایندکس Q8 · adapter و زمان‌بندی · G14c | | ⏳ تصویب جدا |

من کلاد هستم

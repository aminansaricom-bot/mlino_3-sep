# بازبینی نگهبان — Q8-3: اعمال قفل روی دیتابیس محلی، و ادغام شواهد

**تاریخ:** ۱۸ سپتامبر ۲۰۲۶
**بازبین و مجری ادغام:** Claude Opus 5 — MLINO Architecture Guardian
**commitها:** `53cef38`، `8bf18a1` و `9630be3` روی `e0348a1`. Codex آن‌ها را محلی ساخت و **نگهبان روی سرور منتشر کرد.**

## حکم: `APPROVED_NEXT_STEP`؛ **Q8 به‌طور کامل بسته شد**

## ۱. اجازه‌ی مالک برای اتصال

مالک در گفت‌وگو نوشت:

> «اجازه‌ی فقط-حافظه‌ی خواندن DATABASE_URL برای Q8-3 هم برقرار است.»

**این اجازه با پایان Q8-3 منقضی شد.** هر مرحله‌ی آینده‌ای که به دیتابیس زنده وصل شود، به یک اجازه‌ی تازه نیاز دارد.

## ۲. راستی‌آزمایی مستقل نگهبان (فقط‌خواندنی)

| بررسی | شاهد Codex | **سنجش مستقل نگهبان** |
|---|---|---|
| migrationها | ۸، همه finished | ✅ **۸**، هیچ‌کدام ناتمام یا rolled back نیست · تازه‌ترین `20260917010000_add_business_profile_published_unique` |
| ایندکس | معتبر با شرط درست | ✅ **وجود دارد، UNIQUE است، `indisvalid=true`**، و شرطش دقیقاً `WHERE (publication_status = 'PUBLISHED')` است |
| trigger و CHECK | ۱۳ و ۳۰ | ✅ **۱۳ و ۳۰**، بدون تغییر · این migration فقط ایندکس افزود |
| داده‌ها | بدون تغییر | ✅ `domain_signal_producer_registry`=4 · `business_profiles`=0 · `publications`=0 |
| containerها | بدون تغییر | ✅ هر دو با همان image، همان StartedAt و ۰ restart |
| read-api | 401 | ✅ **HTTP 401** (خودم درخواست زدم) |
| **backup** | `mlino_v1_pre_q8_20260917T202400Z.dump` | ✅ روی میزبان هست · **۱۰۳٬۹۸۰ بایت** · SHA-256 ‏**`3ed1d514…300c`** برابر مقدار داخل container · سرآیند `PGDMP` · ۲۱۵ شیء در فهرست |
| فایل موقت | پاک شد | ✅ هیچ dump‌ی داخل container نمانده · هیچ فایل dump در مخزن نیست |
| **C0** | فقط booleanها | ✅ `c0.log` فقط `C0_LINES=1 HOST_LOCAL=true PORT=5435 DB=mlino_v1` دارد · **grep نشت: هیچ** · پنج تطابق فقط الگوهای پاک‌سازی خود اسکریپت و آدرس `http://localhost:3000` است |
| دامنه | فقط شواهد | ✅ **۰ فایل محصول** · worktree تمیز |

**نکته‌ی مهم درباره‌ی کد خروجی ۱:** `migrate status` با کد ۱ بیرون آمد. Codex درست تشخیص داد که این یعنی «migration در انتظار است»، نه شکست، و آن را با deploy ناموفق اشتباه نگرفت. `migrate deploy` با کد ۰ و دقیقاً یک بار اجرا شد.

**دو توقف پیش از اجرا، صادقانه ثبت شده‌اند:** هر دو به‌خاطر ایراد در خود اسکریپت PowerShell بودند و **پیش از هرگونه اتصال به دیتابیس، پشتیبان‌گیری یا migration** متوقف شدند. لاگ هر دو نگه داشته شده است.

## ۳. ادغام شواهد در main

| مورد | مقدار |
|---|---|
| **merge commit** | **`c482318894e4b2dacc1ba3b18f9bee56bebdae1a`** · tree ‏`8f39a942` · والدها `0bc1eb4` و `9630be3` |
| دامنه | ۲۸ فایل، فقط افزودنی (+۴۸۰ خط) · **۰ فایل محصول** |
| مجوز | تصویب مالک برای Q8 و Q8-3 در ۱۷ سپتامبر ۲۰۲۶ |

✅ **محدودیت «برنامه را از نو نسازید» برداشته شد.** این migration اعمال شده است، پس `docker compose up --build` دیگر چیزی را بدون پشتیبان اعمال نمی‌کند.

## ۴. جمع‌بندی وضعیت پروژه

| قابلیت | وضعیت |
|---|---|
| snapshot محتوای منتشرشده (G14a) | ✅ در main و روی DB محلی |
| مقاوم‌سازی تراکنش (G15) | ✅ در main |
| تولیدکننده‌ی export امضاشده (G14b) | ✅ در main |
| قفل یک پروفایل در هر سازمان (Q8) | ✅ **در main و روی DB محلی** |

## ۵. کارهای باقی‌مانده

| # | کار | وضعیت |
|---|---|---|
| **۱** | **G14c: مصرف‌کننده‌ی V2** | سند طراحی آماده‌ی شروع است. این تنها کاری است که ارزش کار را برای کاربر نهایی قابل‌دیدن می‌کند |
| **۲** | **کلید واقعی و زمان‌بندی** | تصمیم عملیاتی مالک؛ تا وقتی V2 مصرف‌کننده ندارد، عجله ندارد |
| ۳ | تنگ‌تر کردن نگاشت خطا (یادداشت N1 در Q8-2) | بدهی فنی کوچک و ثبت‌شده |

## ۶. دستور Codex — G14c-1، نسخه‌ی ۰۰۶

هنوز شروع نشده است. متن کامل در بخش ۵ بازبینی `20260917_CLAUDE_REVIEW_Q8_2_IMPLEMENTATION.md` است و فقط این سطرها عوض می‌شوند:

```
INSTRUCTION_ID: CODEX-20260918-G14C1-V2-CONSUMER-DESIGN-006
TARGET_HANDOFF_ID: HANDOFF-20260918-GUARDIAN-Q8-3-MERGE
SUPERSEDES: CODEX-20260917-G14C1-V2-CONSUMER-DESIGN-001 through -005
REVIEW_REFERENCE: AI_HANDOFF/CLAUDE_REVIEWS/20260918_CLAUDE_REVIEW_Q8_3_LOCAL_MIGRATION_MERGE.md (PINNED_COMMIT/SHA256 relayed)
```

بقیه‌ی بندها، فایل‌های مجاز و ممنوعیت‌ها بدون تغییرند.

## ۷. وضعیت دروازه‌ها

| دروازه | موضوع | وضعیت |
|---|---|---|
| **Q8** | قفل پروفایل | ✅ **کامل:** CCR، پیاده‌سازی، ادغام و اعمال روی DB محلی (`c482318`) |
| **G14c-1** | سند طراحی مصرف‌کننده‌ی V2 | ▶️ نسخه‌ی ۰۰۶ |
| G14c-2 · کلید و زمان‌بندی | | ⏳ تصویب جدا |

من کلاد هستم

# بازبینی نگهبان معماری — G14c-2b: تکمیل شاهد آزمون مصرف‌کننده‌ی V2

**تاریخ:** ۱۸ سپتامبر ۲۰۲۶
**بازبین:** Claude Opus 5 — MLINO Architecture Guardian
**commit:** `bfcc316` روی `ee4e70d`. Codex آن را محلی ساخت و **نگهبان شاخه‌ی `codex/v2-public-consumer` را با lease پیشروی از `ee4e70d` به `bfcc316` روی سرور برد.**

## حکم: `APPROVED_NEXT_STEP`؛ G14c-2 کامل شد و آماده‌ی تصمیم ادغام است

## ۱. راستی‌آزمایی مستقل

| مورد | نتیجه |
|---|---|
| **دامنه** | ✅ فقط فایل آزمون، شواهد `g14c-2b/`، گزارش و Handoff · **۰ تغییر در کد محصول** · ۰ فایل در `implementation/` · بدون نشت · worktree تمیز |
| **F1 الگوریتم** | ✅ آزمون تازه با `Ed448` و امضای **معتبر** روی همان بایت‌ها ساخته شده، پس فقط شرط الگوریتم می‌تواند آن را رد کند · کد `PUBLIC_EXPORT_SIGNATURE_SHAPE` |
| **F2 قالب امضا** | ✅ سه حالت: نویسه‌ی غیرمجاز، طول نادرست، و بیت‌های اضافه · کد `PUBLIC_EXPORT_SIGNATURE_VALUE` |
| **F3 کدهای خطا** | ✅ همه‌ی `rejects.toThrow()`‌های بدون کد، اکنون کد دقیق را می‌سنجند |
| **آزمون‌ها** | ✅ **خودم اجرا کردم** روی کپی جدا از `git archive`: **۱۴ فایل، ۲۲۵/۲۲۵** · سه لاگ build و test کدکس نیز سبز |
| **آزمایش حذف** | ✅ **خودم تکرار کردم:** با حذف شرط الگوریتم، ۱ آزمون شکست خورد · با حذف شرط طول و رفت‌وبرگشت، ۲ آزمون · با حذف بررسی لغو کلید، ۱ آزمون · برابر `mutation.log` کدکس |

## ۲. یادداشت

| # | یادداشت |
|---|---|
| **N6** | حالت «بیت‌های اضافه» (`AB`) هم‌زمان در شرط طول هم رد می‌شود، پس **حذف تنهای شرط رفت‌وبرگشت** هنوز هیچ آزمونی را نمی‌شکند. خودم آزمودم: ۲۴/۲۴ سبز ماند. **پیامد امنیتی ندارد:** مقدار امضا جزو بایت‌های امضاشده نیست و املای دیگرِ همان ۶۴ بایت، همان امضا و همان محتوا را حمل می‌کند · ابهام از متن دستور خودم بود که دو شرط را با هم نام برد · **اقدامی لازم نیست**؛ اگر روزی این فایل آزمون دوباره باز شد، یک مورد ۸۶ نویسه‌ای با بیت‌های اضافه‌ی پایانی اضافه شود |

## ۳. وضعیت شاخه

| شاخه | commit |
|---|---|
| `codex/v2-public-consumer` | **`bfcc316`** روی سرور · ۵ commit روی `f4d326f` |
| `codex/v2-intent-flow-foundation` (V2) | `f4d326f`، بدون تغییر · **ادغام آزمایشی تمیز** (`git merge-tree`) |
| main | بدون تغییر در کد |

## ۴. بسته‌ی تصمیم مالک

| # | موضوع | توصیه‌ی نگهبان |
|---|---|---|
| **M1** | ادغام `codex/v2-public-consumer` ‏(`bfcc316`) در شاخه‌ی V2 ‏`codex/v2-intent-flow-foundation` | **مجاز شود.** اجرا به‌دست نگهبان با `--no-ff` و lease روی `f4d326f`، سپس اجرای دوباره‌ی آزمون‌ها روی نتیجه‌ی ادغام در یک کپی جدا |
| **M2** | راه‌اندازی عملیاتی: کلید واقعی، مسیر انتشار، زمان‌بندی، و روش لغو فوری کلید | **جدا و پس از M1**، با یک سند CCR عملیاتی (شامل N5: V1 فایل را با دسترسی `0600` می‌نویسد) |

**پیامد M1 برای کاربر:** پس از ادغام، اگر نسخه‌ی V2 بدون تنظیمات اجرا شود، دیگر داده‌ی آزمایشی نشان نمی‌دهد و پیام «اطلاعات واقعی فعلاً در دسترس نیست» می‌آید. نمایش آزمایشی فقط با `VITE_PUBLIC_EXPORT_MODE=demo` و با بنر «حالت نمایشی» در دسترس است. **این رفتار عمدی و طبق طرح است.**

**پاسخ پیشنهادی مالک:**
> «ادغام G14c-2 در شاخه‌ی V2 مجاز است.»

## ۵. وضعیت دروازه‌ها

| دروازه | موضوع | وضعیت |
|---|---|---|
| G14a، G15، G14b و Q8 | | ✅ در main |
| G14c-1 | سند طراحی | ✅ پذیرفته |
| **G14c-2 و G14c-2b** | **مصرف‌کننده‌ی V2** | ✅ **پذیرفته** · شاخه روی `bfcc316` |
| **M1** | **ادغام در شاخه‌ی V2** | ⏳ **تصمیم مالک** |
| M2 | کلید، مسیر و زمان‌بندی | ⏳ پس از M1 |

من کلاد هستم

# ثبت تصویب مالک — G7: پشتیبان‌گیری و اعمال migration Core روی `mlino-v1-local-db`

**تاریخ:** ۱۳ سپتامبر ۲۰۲۶
**ثبت‌کننده:** Claude Opus 5 — MLINO Architecture Guardian
**تصویب‌کننده:** مالک محصول، در گفت‌وگوی مستقیم با Claude — عبارت مالک: «تصویبش کن»، در پاسخ به درخواست تصویب G7 در بازبینی `20260913_CLAUDE_REVIEW_G6_MERGE.md`

**عبارت رسمی:** G7 approved: back up and apply migration 20260913010000_add_core_foundation to mlino-v1-local-db.

**BACKUP_DIR:** `C:\Users\galexy\mlino-backups\` — پیش‌فرض پیشنهادی نگهبان. مالک مسیر دیگری نداد؛ تصویب او شامل همین پیش‌فرض ثبت می‌شود. بیرون از هر مخزن است؛ در لحظه‌ی ثبت هنوز وجود ندارد و Codex آن را می‌سازد.

## موضوع تصویب

| | |
|---|---|
| پایگاه داده | `mlino-v1-local-db` (پایگاه `mlino_v1`، پورت محلی `5435`) — در لحظه‌ی ثبت: healthy · `StartedAt=2026-09-12T21:34:05Z` · `Restarts=0` |
| migration | **فقط** `20260913010000_add_core_foundation` از `main` @ `f40a3f5` |
| دستور اجرایی | `CODEX-20260913-G7-APPLY-CORE-MIGRATION-LOCAL-001` — بخش ۴ بازبینی G6 |

## مرز تصویب

- **پشتیبان کامل پیش از اعمال الزامی است.** اگر پشتیبان‌گیری یا بررسی خوانایی آن شکست بخورد: **توقف.**
- **فقط یک migration.** اگر `migrate status` چیزی جز دقیقاً همین یک migration معلق نشان دهد: **توقف.**
- **rollback فقط طبق §۱۲ CCR** و فقط در صورت شکست بررسی‌ها.
- **این تصویب شامل موارد زیر نیست:**
  - rebuild هیچ image یا هر عملیات `docker compose build`، `up --build` یا `down -v`
  - اجرای آزمون‌های V1 روی این پایگاه داده
  - هر تغییر در مخزن جز شواهد و گزارش G7 روی شاخه‌ی Core
- فایل پشتیبان داده دارد: **هرگز commit یا کپی در مخزن نشود.**

من کلاد هستم

# بازبینی و اجرای نگهبان — G11b: CCR نهایی و ادغام لایه‌ی service هسته در main

**تاریخ:** ۱۴ سپتامبر ۲۰۲۶
**بازبین و مجری ادغام:** Claude Opus 5 — MLINO Architecture Guardian
**گزارش بررسی‌شده (G11b-1):** `origin/codex/core-prisma-foundation:AI_HANDOFF/CODEX_REPORTS/20260914_CODEX_G11B1_CORE_SERVICE_CCR_FINAL_REPORT.md` (LF sha256 `cd91471f…355c`)
**commitهای Codex:** `3cb4420`، `8fa1e1d`، `6c1b255` و `2d2bc17`
**تصویب مالک:** `AI_HANDOFF/CLAUDE_REVIEWS/20260914_OWNER_APPROVAL_CORE_SERVICE_CCR_MERGE.md` (commit `45926ce`)

## حکم: `APPROVED_NEXT_STEP` — ادغام انجام شد

**ادغام G10a و G10b در main انجام شد.**

| مورد | مقدار |
|---|---|
| **merge commit** | `7c5416a496c1678259cb08a5b351010097137d97` |
| **والدها** | `45926ce` (main) و `2d2bc17` (Core) |
| **درخت** | `2d89fd944950535ae333a1207e27b8868a1e8eb6` = خروجی `merge-tree` نگهبان |

---

## ۱. بازبینی G11b-1

| مورد | نتیجه |
|---|---|
| **دامنه** | ✅ `0624296..2d2bc17`: فقط CCR، `mlino2/validation/g11b1/LF-MANIFEST.txt`، گزارش و Handoff (بدون حذف) · بدون تغییر کد یا آزمون |
| **CCR** | ✅ `Status: APPROVED` با ارجاع به سند تصویب pin‌شده · فهرست دقیق ۱۷ مسیر و خط `tsconfig` · فهرست موارد بدون تغییر · شاهد نبود import و یادداشت Docker (به‌همراه یادداشت آینده‌ی `COPY core`) · خطر `_PUSH_STAGING` · `git revert -m 1` · جدول hash برای ۱۷ مسیر · خط «من کدکس هستم» حذف شده است |
| **manifest** | ✅ ‏۱۷ از ۱۷ با `git show` برابرند |
| **hash گزارش** | ✅ `cd91471f…` |

**دو نکته‌ی جزئی در CCR (مانع نیستند و در این سند اصلاح می‌شوند):**

1. **برچسب‌های E1 تا E4 در بخش ۴ CCR با تعریف G9c نمی‌خوانند.** محتوایی که زیر آن برچسب‌ها آمده درست است (قفل، port پلتفرم، محافظ آزمون)، ولی تعریف مرجع E1 تا E4 در بخش ۳ بازبینی G9c است:

   | برچسب | تعریف مرجع (G9c) |
   |---|---|
   | E1 | 23505 بر اساس نام constraint |
   | E2 | idempotency ‏S4 با شرط revision منتشرشده |
   | E3 | S12 پیش از برش claim |
   | E4 | سرآیند FINAL |

2. **عنوان «تصمیم‌های پیاده‌شده» در CCR بیش از واقع است.** S4، S7 و S9 (انتشار، `gate_snapshot`، خواندن V2) **تصمیم گرفته شده‌اند ولی هنوز پیاده نشده‌اند**. در برش‌های G10c به بعد می‌آیند. پیاده‌شده‌ها در این ادغام این‌ها هستند:
   - S1، S2، S3/W1، S5، S6، S8
   - S10، S11، S12-A، R4
   - E1، ADR-0009 و ADR-0010

## ۲. اجرای ادغام (G11b-2)

| گام | نتیجه |
|---|---|
| **پیش‌شرط** | clone اصلی روی `main` = `origin/main` = `45926ce` بود، پاک · شاخه‌ی Core fetch شد = `2d2bc17` |
| **merge-tree دوباره** | درخت `2d89fd9`، بدون تعارض · زیر `implementation/` فقط **فایل CCR** با درخت آزموده‌شده‌ی `67859e1` فرق دارد. بقیه‌ی تفاوت‌ها اسناد تازه‌تر main و شواهد G11b-1 هستند |
| **تغییر فایل‌های موجود** | فقط `implementation/tsconfig.json` (خط مصوب) و `mlino2/HANDOFF/HANDOFF_STATE.md` (فقط افزودنی) · بدون حذف یا تغییر نام |
| **ادغام محلی** | `git merge --no-ff` ← `7c5416a` · **درخت دقیقاً برابر `2d89fd9`** · working tree پاک |
| **push** | محافظت‌شده با `--force-with-lease=main:45926ce`، به‌همراه همین سند |
| **آزمون** | در `_PUSH_STAGING` هیچ آزمونی اجرا نشد. اعتبارسنجی درخت ادغام پیش‌تر در G11a روی پایگاه یک‌بارمصرف انجام شده بود: ۲۹۹ آزمون کل V1 و ۵۰ آزمون Core |

## ۳. وضعیت runtime

**پیش از ادغام:** read-api با image `a07858b3…` (StartedAt `2026-09-13T12:54:39Z`) · DB با StartedAt `2026-09-12T21:34:05Z` و `Restarts=0` · شش migration · `dspr=4` · organizations = ۰

**پس از ادغام:** همان مقادیر؛ راستی‌آزمایی در همین push. ادغام هیچ build، restart، migration یا نوشتنی روی داده انجام نداد و کد تازه در image نیست.

## ۴. پس از ادغام: قاعده‌های ماندگار

- **`_PUSH_STAGING/implementation` اکنون `core/` و `test/core/` را دارد. در `_PUSH_STAGING` هرگز `npm test` یا `jest` اجرا نشود:**
  - specهای V1 جدول‌ها را بدون شرط پاک می‌کنند.
  - `.env` آن مسیر احتمالاً به پایگاه زنده اشاره می‌کند.
  - specهای Core محافظ دارند، ولی specهای V1 ندارند.
- **پیشنهاد (اختیاری، CCR کوچک V1):** یک محافظ سراسری در `test/setup-env.ts` که پورت 5435 را رد کند. این خطر پیشین را برای همیشه می‌بندد.
- **ادغام یک‌بار انجام شد.** شاخه‌ی `codex/core-prisma-foundation` برای برش‌های بعدی (G10c) از همین نقطه ادامه می‌دهد.

## ۵. گام بعدی

**Next Task (تصمیم مالک):** یکی از این دو، به انتخاب مالک:

1. **G10c: برش Profile و Capability.** شامل قرارداد revision انتشار (R1، S4 و E2)، D6 و `gate_snapshot` (S7).
2. **CCR کوچک V1 برای محافظ سراسری آزمون** (بخش ۴)، پیش از G10c.

**توصیه‌ی نگهبان:** اول محافظ سراسری. کوچک است و خطر پاک شدن داده‌ی زنده را از پایه می‌بندد. بعد G10c.

هر دو به تصویب مالک نیاز دارند. دستور دقیق هر کدام پس از انتخاب مالک صادر می‌شود.

## ۶. وضعیت دروازه‌ها

| دروازه | موضوع | وضعیت |
|---|---|---|
| G10a و G10b | برش‌های authority و claim | ✅ بسته |
| G11a | آماده‌سازی ادغام | ✅ پذیرفته |
| **G11b** | **CCR نهایی و ادغام در main** | ✅ **انجام شد** (`7c5416a`) |
| محافظ سراسری آزمون V1 | CCR کوچک | ⏳ پیشنهاد، تصمیم مالک |
| G10c | Profile و Capability | ⏳ تصمیم مالک |

من کلاد هستم

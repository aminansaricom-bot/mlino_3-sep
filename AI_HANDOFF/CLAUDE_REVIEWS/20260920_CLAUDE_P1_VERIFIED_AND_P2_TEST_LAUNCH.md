# بازبینی P1 و اجرای P2 — راه‌اندازی آزمایشی روی سیستم مالک

**تاریخ:** ۲۰ سپتامبر ۲۰۲۶
**بازبین و مجری:** Claude Opus 5 — نگهبان معماری MLINO

## ۱. P1 — بازبینی مستقل نگهبان

commit کدکس: `3ecf7f5`. نگهبان مستقیماً و فقط‌خواندنی از دیتابیس محلی پرسید:

| بررسی | نتیجه |
|---|---|
| سازمان‌ها | ✅ ۱۵، همه با شناسه‌ی `test-vanak-` · **سازمان غیرآزمایشی: صفر** |
| نمایه‌ها | ✅ ۱۵، همه `ACTIVE` · برچسب «داده‌ی آزمایشی» روی هر ۱۵ |
| توانمندی‌ها | ✅ ۱۰، همه `ACTIVE` و `HUMAN_CONFIRMED` |
| ادعای هویت | ✅ ۱۵ `VERIFIED` |
| انتشار | ✅ ۲۵ رویداد، همه `PUBLISHED` · **رویداد غیرآزمایشی: صفر** |
| مختصات | ✅ ۱۲ نمایه |
| ساختار | ✅ ۸ migration · ۱۳ trigger · ۳۰ CHECK |
| کانتینرها | ✅ هر دو در حال اجرا، restart صفر، همان StartedAt |
| API | ✅ بدون توکن HTTP 401 |
| پشتیبان | ✅ `mlino_v1_pre_p1_20260919T175223Z.dump`، تأییدشده |

**حکم P1: `APPROVED_NEXT_STEP`.**

## ۲. P2 — راه‌اندازی آزمایشی، اجراشده به‌دست نگهبان

**چرا نگهبان اجرا کرد:** گاوصندوق ویندوز (DPAPI) به حساب گره خورده است (G5) و حساب sandbox کدکس پروفایل ندارد، پس کدکس نمی‌تواند کلید بسازد یا باز کند. مالک در گفت‌وگو اجازه داد:

> «نگهبان هم می‌تواند فقط برای P2 خط DATABASE_URL را در حافظه بخواند.»

| گام | نتیجه |
|---|---|
| **کلید آزمایشی** | ✅ جفت Ed25519 در حافظه ساخته و با DPAPI حساب مالک محافظت شد · `key_id` = `pb-v1-20260920-dbef05fbc08935dd` · keystore در `C:/mlino code/_KEYS_TEST/keystore.json`، بیرون از مخزن |
| **fهرست کلید عمومی** | ✅ `trust-bundle.json` فقط عمومی · بررسی شد که هیچ بخش محافظت‌شده یا pkcs8 در آن نیست |
| **پوشه‌ی عمومی** | ✅ `C:/mlino code/_PUBLIC_EXPORT` با ACL صریح و وراثت خاموش: حساب مالک Modify، SYSTEM و Administrators · اسکریپت verify هم تأیید کرد |
| **ساخت فایل** | ✅ CLI نسخه‌ی اول با کلید DPAPI اجرا شد: `PUBLIC_EXPORT_PUBLISHED`، ۱۵ رکورد · خروجی در پوشه‌ی خصوصی producer |
| **رساندن** | ✅ CLI توزیع، پس از بررسی امضا و تازگی، فایل را **بایت‌به‌بایت یکسان** به پوشه‌ی عمومی برد |
| **زمان‌بند موقت** | ✅ حلقه‌ی هر ۶۰ ثانیه (ساخت + رساندن)، با خواندن نشانی دیتابیس در حافظه در هر چرخه · `CYCLE_OK` |
| **نسخه‌ی دوم** | ✅ ساخت محلی از شاخه‌ی `e26c525` با کلید عمومی و نشانی فایل · `version.json` ساخته شد و شناسه در کد جاسازی شد |
| **وب‌سرور** | ✅ کانتینر `mlino2-p2-web` از image موجود، روی `0.0.0.0:8443` · `nginx -t` درست · `/public-export/public-business.v1.json` → ۲۰۰ و JSON · `/version.json` → ۲۰۰ · صفحه‌ی اصلی → ۲۰۰ |
| **راستی‌آزمایی امضا، مثل مرورگر** | ✅ کلید در فهرست هست · بایت‌ها canonical · **امضا معتبر** · ۱۵ رکورد · سن ۳۱ ثانیه، داخل اعتبار ۳۰۰ ثانیه |

**نشانی برای گوشی روی همان شبکه:** `https://192.168.1.65:8443/`
گواهی خودامضاست، پس مرورگر گوشی یک بار هشدار می‌دهد.

## ۳. مرزها و آنچه انجام نشد

- **کلید واقعی ساخته نشد.** فقط کلید آزمایشی، و فقط در گاوصندوق همین حساب.
- **زمان‌بند دائمی نصب نشد.** حلقه موقت است و با بستن نشست پایان می‌یابد.
- هیچ دسترسی بیرون از شبکه‌ی محلی باز نشد.
- به کانتینرهای نسخه‌ی اول، دیتابیس (جز خواندن) و کانتینر قدیمی نسخه‌ی دوم دست زده نشد. کانتینر تازه نام جدا دارد.
- نشانی دیتابیس فقط در حافظه خوانده شد و هیچ‌جا نوشته یا چاپ نشد.
- `C0` با پایان P2 **منقضی می‌شود.**

## ۴. کارهای بعد، هر کدام با اجازه‌ی جدا

1. **پاک‌سازی پس از آزمایش:** خاموش کردن حلقه و کانتینر آزمایشی، و در صورت خواست مالک، پس گرفتن انتشار ۱۵ کسب‌وکار.
2. **کلید واقعی و زمان‌بند دائمی** (M2-4)، با تمرین لغو کلید.
3. **دسترسی بیرون از شبکه‌ی محلی**، که تصمیم جدا و بزرگ‌تری است.

من کلاد هستم

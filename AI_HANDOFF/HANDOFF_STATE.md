HANDOFF_ID: HANDOFF-20260909-ADR0005-AND-WORKSPACE-LINK-CCR
AUTHOR: CLAUDE
PHASE: ADR_AND_CONTRACT_CHANGE_REQUEST_ONLY
STATUS: DELIVERED_AWAITING_INDEPENDENT_REVIEW
REPORT_PATH: C:\mlino code\AI_HANDOFF\CLAUDE_REPORTS\20260909_ADR0005_AND_EXTERNAL_WORKSPACE_LINK_CCR_REPORT.md
REPORT_SHA256: 6a7cb6a8546f56d55db0551fdaf6146b10fefc2c9487c9e1e6b010e64df02361
ZIP_PATH: (none built this pass)
CODE_COMMIT_SHA: (none - zero code this pass, documents only). Content Studio f6946a8 remains local only, see OD-09.
CREATED_AT: 2026-09-10T00:30:00
NEXT_ACTION: Return for review. Roadmap step 2 does not begin until the ExternalWorkspaceLink CCR is approved, and four questions in it need owner decisions first, logged as OD-10: whether link events also go into the EventLog (which would need a new domainTag and is outside this CCR), what happens when a Content Studio workspace is deleted since there is no synchronisation, whether SUSPENDED is genuinely needed (my recommendation is to drop it), and whether basis needs more values. OD-09 remains BLOCKED and unchanged - no alternative backup path was created and none will be. Blocking open decisions unchanged: OD-01, OD-02, OD-05, OD-08. Open gaps unchanged: R4 (BLOCKED), R5 (OPEN by design), R8-a and R8-b (OPEN).

PREVIOUS_HANDOFF_ID: HANDOFF-20260909-RECOMMENDATION-CONTRACT-IMPLEMENTATION
EXECUTED_INSTRUCTION_ID: OWNER-20260909-1600-V1-BUSINESS-OS-PRODUCT-MEMORY

CARRIED_FORWARD_OPEN_REVIEW: HANDOFF-20260907-V1-DOCKER-LOCAL-RUN is still DELIVERED_AWAITING_INDEPENDENT_REVIEW. Mamad has not reviewed it. This new product line does NOT supersede or close that review, and part B of its instruction (Moji's Docker directive) remains deliberately unexecuted pending a decision. Both stay open.

NOTE: Documents only this pass - zero lines of code, schema.prisma untouched, no migration created, no database code written, all confirmed by git status. Two deliverables. ADR-0005 records the approved reading that Recommendation keeps only its own lifecycle while ActionRecord, OutcomeRecord and EvaluationRecord are independent entities, and gives five reasons rather than restating the decision: status done means it was done and not that it worked; the four concepts have four different timings so one entity would either be overwritten repeatedly or lose history; their provenance differs across engine, human, measurement and judgement, and merging loses it; an outcome can legitimately exist before anyone has evaluated it, so a single entity forces one of two lies; and without the separation the system can jump from I recommended to it worked with nobody having measured anything. The cost is recorded too - a full picture now needs four references rather than one row - though for the Content Studio learning adapter, which already keeps outcomes and evaluations separately, this is simpler rather than harder. The ExternalWorkspaceLink CCR covers all thirteen requested sections. Three points are worth the reviewer time: the partial unique index on active links is the only construct that simultaneously enforces one active organization per workspace and preserves revoked history, and Prisma cannot express it natively so raw SQL is needed - logged as a risk rather than hidden; the rollback strategy is deliberately asymmetric, full drop before any real data and deactivation only afterwards, because dropping a table holding who linked what to whom and when destroys exactly the auditability the model exists for; and the existence-oracle constraint from the V1 HTTP boundary is applied again, so not linked and organization does not exist must look identical from outside. No synchronisation is proposed at all: if V1 is unreachable, the operation stops honestly rather than using a stale or guessed value. Four alternatives were rejected with reasons, including the organization_id column on the Content Studio workspace table that the owner had explicitly forbidden.
HANDOFF_ID: HANDOFF-20260910-CCR-V2-AFTER-OD10
AUTHOR: CLAUDE
PHASE: CCR_REVISION_AFTER_OWNER_REVIEW
STATUS: DELIVERED_AWAITING_INDEPENDENT_REVIEW
REPORT_PATH: C:\mlino code\AI_HANDOFF\CLAUDE_REPORTS\20260910_CCR_V2_AFTER_OD10_REPORT.md
REPORT_SHA256: 501ffe28974471da8f1014f44709a8a2207df7d85c900c0d4bcf6b2978cf9628
ZIP_PATH: (none built this pass)
CODE_COMMIT_SHA: (none - zero code this pass, documents only). Content Studio f6946a8 remains local only, see OD-09.
CREATED_AT: 2026-09-10T01:30:00
NEXT_ACTION: Return for final CCR approval before any database work begins. One question should be settled alongside that approval: OD-11, how the approved workspace-deletion policy is actually enforced, since nothing enforces it today - three options are laid out in CCR section 11.2 and my recommendation is to accept it as a documented operational rule for now rather than open either cross-system boundary. OD-09 remains BLOCKED and untouched. Blocking open decisions unchanged: OD-01, OD-02, OD-05, OD-08. Open gaps unchanged: R4 (BLOCKED), R5 (OPEN by design), R8-a and R8-b (OPEN).

PREVIOUS_HANDOFF_ID: HANDOFF-20260909-ADR0005-AND-WORKSPACE-LINK-CCR
EXECUTED_INSTRUCTION_ID: OWNER-20260909-1600-V1-BUSINESS-OS-PRODUCT-MEMORY

CARRIED_FORWARD_OPEN_REVIEW: HANDOFF-20260907-V1-DOCKER-LOCAL-RUN is still DELIVERED_AWAITING_INDEPENDENT_REVIEW. Mamad has not reviewed it. This new product line does NOT supersede or close that review, and part B of its instruction (Moji's Docker directive) remains deliberately unexecuted pending a decision. Both stay open.

NOTE: Documents only - zero code, schema.prisma untouched, no migration, EventLog untouched, Content Studio untouched. The four OD-10 decisions are applied to the ExternalWorkspaceLink CCR, now at revision 2. EventLog is out of scope with no new domainTag, recorded as deferred to a future platform event architecture rather than rejected. The workspace deletion policy is added: soft delete allowed when there is no organization link, physical deletion forbidden when a link exists or ever existed, archive semantics instead. SUSPENDED is removed so the lifecycle is Created to ACTIVE to REVOKED, and reconnecting means a new link row rather than reviving a revoked one, because a row that can toggle repeatedly cannot answer which organization a workspace belonged to on a given date. The basis taxonomy is closed at MANUAL, SYSTEM_MIGRATION, ADMIN_ACTION and INTEGRATION. Two things surfaced while applying these. First and most important, the deletion policy constrains Content Studio behaviour, but its database is not changing and there is no synchronisation, so V1 never learns that a workspace was deleted - the rule is therefore documented and operational today, not enforced by code, and that gap is logged as OD-11 with three enforcement options, my recommendation being to accept the documented rule for now rather than open a cross-system boundary. Second, the or ever existed half of the deletion rule matters as much as the current half: physically deleting a workspace whose link was revoked leaves the REVOKED row in V1 pointing at an identifier that no longer exists, so governance history must stay traceable from both ends.
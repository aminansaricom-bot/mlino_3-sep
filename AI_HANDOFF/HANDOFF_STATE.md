HANDOFF_ID: HANDOFF-20260910-EXTERNAL-WORKSPACE-LINK-IMPLEMENTATION
AUTHOR: CLAUDE
PHASE: EXTERNAL_WORKSPACE_LINK_IMPLEMENTATION
STATUS: DELIVERED_AWAITING_INDEPENDENT_REVIEW
REPORT_PATH: C:\mlino code\AI_HANDOFF\CLAUDE_REPORTS\20260910_EXTERNAL_WORKSPACE_LINK_IMPLEMENTATION_REPORT.md
REPORT_SHA256: d8cca3fa9b59baab5989eb882d7809bad234890eef7ef5562f43d79c950f77ec
ZIP_PATH: (none built this pass)
CODE_COMMIT_SHA: (set on push of this pass). The frozen-file change here is the APPROVED ExternalWorkspaceLink CCR - deliberate, authorised drift, not accidental. Content Studio f6946a8 remains local only, see OD-09.
CREATED_AT: 2026-09-10T03:00:00
NEXT_ACTION: Return for review - roadmap step 3, the database integration of the opportunity engines, was deliberately not started. One new open item came out of this pass: OD-12, the future enforcement strategy for the workspace deletion policy, which is documented but enforced by nothing today. OD-09 remains BLOCKED and untouched; this work lived entirely in V1 and had no dependency on the Content Studio repository. Blocking open decisions unchanged: OD-01, OD-02, OD-05, OD-08. Open gaps unchanged: R4 (BLOCKED), R5 (OPEN by design), R8-a and R8-b (OPEN).

PREVIOUS_HANDOFF_ID: HANDOFF-20260910-CCR-V2-AFTER-OD10
EXECUTED_INSTRUCTION_ID: OWNER-20260909-1600-V1-BUSINESS-OS-PRODUCT-MEMORY

CARRIED_FORWARD_OPEN_REVIEW: HANDOFF-20260907-V1-DOCKER-LOCAL-RUN is still DELIVERED_AWAITING_INDEPENDENT_REVIEW. Mamad has not reviewed it. This new product line does NOT supersede or close that review, and part B of its instruction (Moji's Docker directive) remains deliberately unexecuted pending a decision. Both stay open.

NOTE: Roadmap step 2 is complete - the first point at which two systems share an identity. The approved CCR moved from PROPOSED to APPROVED and its schema change is the deliberate, authorised drift in prisma/schema.prisma this pass: one new model and three new enums added, with no existing model touched - the diff is 56 insertions and zero deletions. An initial prisma format run had also reflowed every existing model, which changed nothing semantically but would have buried real drift inside formatting noise on a frozen file, so the original formatting was restored and only the new block kept. Migration 20260910020000 creates the table plus the invariant that matters, a partial unique index on system and external_workspace_id where status is ACTIVE. The reason raw SQL is required is documented inside the migration file rather than left implicit: Prisma cannot express a partial unique index, and a plain unique constraint would enforce uniqueness across all rows, making a second historical link on the same workspace impossible - it would break either the invariant or the history, and both are unacceptable. An application-level check was explicitly not accepted as a substitute, because read-then-write is not concurrency-safe and two transactions could each create an active link, leaving two organizations owning one workspace and breaking the AC-2 boundary directly. The invariant is proven twice: the index was read back from pg_indexes with its WHERE clause intact, and a test bypasses the service layer entirely by writing through Prisma directly and is rejected. Revoke and re-link semantics are implemented as approved - a revoked row is never deleted or reactivated, re-linking creates a new row, and the historical identity invariant is proven by a test that moves a workspace from one organization to another and then queries both time windows, with organizationAt returning the first organization for the earlier window even though that link is now revoked. Verification: 17 new tests, whole suite 210 of 210 across 18 files, up from 193, so zero regressions, tsc clean, build succeeding, migrate status up to date. Scope held exactly: no Content Studio schema change, no synchronisation, no EventLog expansion, no workspace-deletion enforcement, no recommendation database integration, no V2 work, no unrelated refactoring. The deletion policy is recorded as POLICY DEFINED / ENFORCEMENT DEFERRED and logged as OD-12 so nobody later assumes the system guarantees it.

HANDOFF_ID: HANDOFF-20260911-OD40-CORE-MODULE-BOUNDARY
AUTHOR: CLAUDE
PHASE: OD40_CORE_AND_MODULE_BOUNDARY
STATUS: DELIVERED_AWAITING_INDEPENDENT_REVIEW
REVIEW_VERDICT: A - READY (OD-40 closed as D-63; C-05 closed as D-64 under the owner's explicit separation; no concrete blocker remains for this decision)
REPORT_PATH: AI_HANDOFF/CLAUDE_REPORTS/20260911_OD40_CORE_MODULE_BOUNDARY_REPORT.md
REPORT_SHA256: f84d016fbf1d81c5f1d26040121bfe35c9f337a2d7f0396462300a2723d73caa
ZIP_PATH: (none built this pass)
CODE_COMMIT_SHA: (none - architecture decision and documentation only, zero code change). Content Studio f6946a8 remains local only, see OD-09.
CREATED_AT: 2026-09-11T18:00:00+03:30
NEXT_ACTION: Return for review. Verdict A: no concrete blocker. Not blockers, listed for clarity: (1) executing migration stage M1 is a one-time CCR on the frozen contract (DomainTag and IntendedAudience from closed lists to a registered, namespaced vocabulary; existing tags kept verbatim; no database migration because the columns are already String) and needs a separate owner authorisation when scheduled - this instruction forbade contract changes, so none was made; (2) individual-level data of the Clinic Module (patient, treatment history) and of CRM stays behind OD-01, known and not new; (3) C-06 is open and non-blocking: the owner's separation puts assistant UI in V2, which fits the end-user assistant, while a business-member assistant works on unpublished internal truth that D-52 keeps out of V2 - my reading keeps member surfaces behind AC-2 unless D-52 is explicitly refined. Awaiting the owner's review: ADR-0007, ADR-0008, ADR-0009, C-06, authorisation of the M1 CCR when scheduled, and two proposed ADRs (one for D-58, D-61 and D-62; one for D-63 and D-64). Still open: OD-35, OD-07. OD-09 remains BLOCKED. Blocking open decisions unchanged: OD-01, OD-05, OD-08, OD-12.

PREVIOUS_HANDOFF_ID: HANDOFF-20260911-OD39-INTEGRATION-OWNERSHIP-BOUNDARY
EXECUTED_INSTRUCTION_ID: OWNER-20260911-OD40-CORE-MODULE-BOUNDARY (no explicit id was supplied by the owner; this identifier is assigned by me for traceability and is recorded as assigned, not as received)

MODEL_ROUTING_NOTE: Routed to "Claude Opus 5 - MLINO V1 Modular Architecture Reviewer", which is the model that executed it.

HANDOFF_PRECONDITION_CHECK: As instructed, repository state was inspected before any decision: in sync with origin at 8911c96, clean working tree, HANDOFF_STATE equal to my OD-39 delivery, and no change by any other agent since D-62; OD-40 and C-05 were open in the register, so these decisions are D-63 and D-64 and BLOCKED_BY_HANDOFF_MISMATCH was not raised. The update script refused to run on an unclean tree, and the push was preceded by a check that origin/main was still 8911c96.

SCOPE_CONSTRAINT_NOTE: Updates were restricted to the new decision document plus the MLINO Book changelog, the roadmap and the open decisions; the script enforced an allow-list and proved it from git status. The frozen shared contract, the Prisma schema, the producer registry service, APIs, the AC-2 policy and implementation code were not modified, and were checked byte for byte. No approved decision was redesigned: D-55 and D-62 receive additive refinements only; the OD-26 context placement of experience orchestration is superseded by the owner's explicit separation in this instruction.

CARRIED_FORWARD_OPEN_REVIEW: HANDOFF-20260907-V1-DOCKER-LOCAL-RUN is still DELIVERED_AWAITING_INDEPENDENT_REVIEW; Mamad has not reviewed it and part B remains deliberately unexecuted. ADR-0007, ADR-0008 and ADR-0009 await the owner's review.

NOTE: Decision D-63: a concept belongs in Core only if it passes a three-part test - meaningful without any trade vocabulary, its meaning defined by Core, and needed by the Core chain or by two independent modules; being business-independent is not enough, so CRM and accounting are modules. Core never names trade vocabulary: not by import, closed list, seed or display label. The code shows import direction is already correct (nothing in foundation, briefing, feed, http or shared-contracts imports value-engines); the leakage is vocabulary-level: three of the four ALLOWED_DOMAIN_TAGS are clinic families while opportunity.interaction is a Core mechanism, IntendedAudience and ActorContext.role carry receptionist vocabulary, and newly found: the clinic producers are seeded inside foundation/producer-registry, briefing carries clinic family labels, read-api validates against the closed list, the structural validator and the auth adapter and AC-2 port hard-code the two roles. The Prisma schema already stores domainTag and intendedAudience as String, and no appointment, patient, treatment or practitioner table exists. Opportunity is Core; an opportunity family belongs to a module and is registered. Core holds no role names. Appointment, patient, treatment history, practitioner attributes and bookable resources, the capacity engine, clinic roles and healthcare workflows belong to the Clinic Module; a sellable service is a Core capability and offer, a practitioner as a person is a Core member, and clinical informed consent is not data consent. Core builds no generic booking entity; a shared horizontal concept becomes a shared module, with declared acyclic module-to-module dependencies. Migration: M0 logical reclassification now with no code; M1 one-time CCR on the frozen contract, separately authorised, turning closed lists into a registered namespaced vocabulary that Core validates but never interprets; M2 code relocation later. Because event_log is append-only and the interaction chain matches the founding tag, existing tags are never renamed. Until the CCR no new trade vocabulary enters Core. Decision D-64 (C-05) under the owner's separation: Core is business-independent intelligence and orchestration, V1 is business truth and the operating system (Core plus modules), V2 is discovery, matching, interaction and experience; experience orchestration and the assistant foundation are Core, assistant UI, matching, discovery and rendering are V2; my OD-39 reading of experience orchestration as sitting on the V2 side is corrected; orchestration output for an end user is built only from the published projection (D-52); trade-specific flows and assistant skills are registered by modules.

HANDOFF_ID: HANDOFF-20260905-AC2-ALIGN-CCR-R8
AUTHOR: CLAUDE
PHASE: AC2_POLICY_ALIGNMENT_CCR_DRAFT_R8_TWO_LAYER_MODEL
STATUS: DELIVERED_AWAITING_INDEPENDENT_REVIEW
EXECUTED_INSTRUCTION_ID: CODEX-20260905-2247-AC2-DATA-KNOWLEDGE-LAYERS-AUTH
PREVIOUS_HANDOFF_ID: HANDOFF-20260905-AC2-MIN-POLICY-DESIGN
DOCS_COMMIT_SHA: d1c886a

---

# گزارش — هم‌راستاسازی سیاست AC-2 + مدل دو‌لایه‌ای داده/دانش + پیش‌نویس CCR + R8

## ۱. دستور اجراشده و تطبیق پیش از اجرا

`INSTRUCTION_ID: CODEX-20260905-2247-AC2-DATA-KNOWLEDGE-LAYERS-AUTH`، صادرکننده: ممد، `STATUS: READY_FOR_CLAUDE`، جایگزین `CODEX-20260905-2235-AC2-ALIGN-CCR-AUTH`.

**تطبیق تایید شد:** `TARGET_HANDOFF_ID` (`HANDOFF-20260905-AC2-MIN-POLICY-DESIGN`) و `TARGET_REPORT_SHA256` (`ccfaea28f42ed7e66d9b39f55331354d9798afd2e9cf2fe0cf04606bdf0d19d1`) با `HANDOFF_STATE.md` وقت اجرا دقیقاً منطبق بودند.

**یادداشت درباره‌ی دستور قبلی:** `CODEX-20260905-1908-AC2-REAL-ADAPTER-AUTH` در پاس قبل با `BLOCKED_BY_HANDOFF_MISMATCH` رد شده بود (TARGET کهنه). صادرکننده در بند ۱ دستور جدید تایید کرد که آن بلاک **صحیح** بوده و خطا از سمت صادرکننده بوده است. دستور `2235` هم طبق هدر `SUPERSEDES` هرگز اجرا نشد و منقضی است.

## ۲. سه خروجی تحویل‌شده

| فایل | نوع | SHA-256 |
|---|---|---|
| `V1_MINIMUM_AC2_ACCESS_POLICY.md` | سند طراحی → v1.1 | `ca396a0b7a2c9ccb8cc1acefdefb22e95b5e33032a385ebd47b7b3a1868609f6` |
| `implementation/remediation/CONTRACT_CHANGE_REQUESTS/CONTRACT_CHANGE_REQUEST_OWNERSHIP_TYPE.md` | پیش‌نویس CCR | `fb4ed2e74e82e3d289ee8b2a2508b73fc994c85c9d9aca8c9e6eae27fa84391e` |
| `implementation/remediation/R8_CONSENT_AND_KNOWLEDGE_LAYER_GAP.md` | سند GAP | `7ea60dbf8f3a43132d60c1f3142730652e5a059ea19b5635c97755bce4775c4e` |

### ۲.۱. سند طراحی v1.1

- **§۴ بسته شد** با تصمیم مالک محصول: Consumer = خودِ سازمان؛ مصرف داخلی بدون نیاز به Consent فردی؛ Consent فقط برای خروج داده از سازمان. مرزهای صریح آنچه این تصمیم **پوشش نمی‌دهد** هم ثبت شد (خروج داده، لایه‌ی دانش، تقدم الزامات حقوقی محلی).
- **قاعده‌ی ۶ اصلاح شد:** Subject فردی داخل همان سازمان با مالکیت `ORGANIZATIONAL` ثبت‌شده → عبور؛ بدون ثبت → Deny؛ خروج از سازمان → Deny تا R8-a.
- **قاعده‌ی ۳ عیناً حفظ شد**، فقط مسیر عملیاتی‌شدنش (از طریق CCR) شفاف شد.
- **§۲.۱ جدید:** جدول مدل دو‌لایه‌ای داده/دانش با نگاشت به Kernel §۱۰، به‌همراه تصریح اینکه لایه‌ی دانش امروز در V1 **وجود ندارد** و هر پیاده‌سازی آینده طراحی + CCR/ACR مستقل خودش را می‌خواهد.
- **دست‌نخورده:** بقیه‌ی ۱۰ قاعده، مدل fail-closed، و بخش‌های ۰/۱/۳/۵/۶/۸. سه ارجاع در §۷ صرفاً برای رفع تناقض داخلی سند به‌روز شدند (Consumer دیگر «تعریف‌نشده» نیست) — این تغییرِ محتوای سیاست نیست، رفع ناسازگاری متن است.

### ۲.۲. پیش‌نویس CCR

طبق الگوی CCR موجود (`CONTRACT_CHANGE_REQUEST_EVIDENCE_INTEGRITY.md`): قرارداد فعلی (با بازخوانی مستقیم Schema و ذکر خطوط)، مسئله، درخواست، استدلال CCR-نه-ACR، اثر سازگاری، Migration/Backfill/Rollback با SQL واقعی، اثر تست، اثر امنیتی، و «آنچه عمداً درخواست نمی‌شود».

**دو نکته‌ی طراحی که خودم اضافه کردم (فراتر از متن دستور، برای اجتناب از خطای شناخته‌شده):**
1. مقادیر `INDIVIDUAL`/`AGGREGATE` **حتی به‌عنوان مقدار enum هم تعریف نمی‌شوند** (فقط کامنت) — چون CCR قبلی «Evidence Namespace v1.2» دقیقاً به‌خاطر تعمیم زودهنگام در سطح Type توسط CR-02 رد شده بود. تکرار همان اشتباه را عمداً پرهیز کردم.
2. یک تصمیم باز صریح ثبت شد: آیا Producer باید مالکیت را ارسال کند (هم‌راستاتر با Kernel §۱۰ «تعیین در لحظه‌ی تولید») یا مرز Admission پرش کند؟ پیشنهاد فاز‌بندی‌شده داده شد، اما به‌عنوان تصمیم نیازمند تایید علامت خورد — حدس نزدم.

**صداقت درباره‌ی Backfill:** ثبت شد که مقدار `ORGANIZATIONAL` برای ردیف‌های تاریخی *مشتق از سیاست* است، نه *ثبت‌شده در لحظه‌ی تولید* (که ایده‌آل Kernel §۱۰ است) — این تمایز نباید بعداً گم شود.

### ۲.۳. سند R8

- **R8-a (Consent & Cross-Org Sharing):** چهار رویداد Consent، دانه‌بندی `Data Type × Consumer` (با تصریح اینکه Consumer برای مصرف داخلی بسته شده اما برای خروج داده هنوز تعریف‌نشده است)، Projection وضعیت رضایت، UI، فعال‌سازی `INDIVIDUAL`، و قاعده‌ی جهت جریان Kernel §۱۱ (رو به پایین آزاد، رو به بالا فقط opt-in).
- **R8-b (Knowledge Layer):** خط تولید تجمیع غیرقابل-بازسازی به‌عنوان **شرط مطلق**، آستانه‌ی حداقل تجمیع (با تصریح اینکه عدد N عمداً تعیین نشده)، نقش امانت‌داری MLINO، مخزن سناریوها، کاربرد Onboarding، فعال‌سازی `AGGREGATE`.
- **هشدار PA-09** با تفکیک عملیاتی Opportunity در برابر Insight، و این حکم صریح: اگر روزی لایه‌ی دانش به Event Log نیاز پیدا کند، `domain_tag` جدید (`insight.*`) با CCR جداگانه لازم است و **هرگز زیر `opportunity.*` نمی‌رود**.
- **هم‌راستایی با V2:** تصریح شد که `02_V1_V2_INTEGRATION_CONTRACT_DRAFT.md` امروز فقط جریان دایرکتوری کسب‌وکار را پوشش می‌دهد و لایه‌ی دانش یک جریان متفاوت است — باید همان قرارداد به‌روز شود، نه یک مسیر داده‌ی دوم و موازی ساخته شود.

## ۳. راستی‌آزمایی (تعریف Done)

| بررسی | نتیجه |
|---|---|
| `npm test` روی Postgres واقعی | ✅ **۱۳۰/۱۳۰** — دقیقاً همان عدد قبلی، یک اجرا، بدون Retry (عدد تغییر نکرد ⇒ هیچ کدی لمس نشده) |
| `npx tsc --noEmit` | ✅ تمیز (Exit 0) |
| Drift `shared-contracts/types.ts` | ✅ `35d218065f9829f2d9c258d082ba6a2fd48588c573a44f722d35fad6dcb76296` — منطبق با مرجع |
| Drift `prisma/schema.prisma` | ✅ `84d138c2222647521f981afefc69944b87b6d2b091cc0f3747e2d0599f37eff0` — منطبق با مرجع |
| فایل‌های تغییریافته | ✅ دقیقاً ۳ فایل سند (`git status` تایید کرد) — صفر فایل کد |

**نکته‌ی عملیاتی:** Docker Desktop در ابتدای این پاس بالا نبود و اولین اجرای تست معلق ماند. خودم Docker را بالا آوردم، `docker compose up -d` و `pg_isready` را تایید کردم، سپس تست را اجرا کردم — اجرای گزارش‌شده‌ی ۱۳۰/۱۳۰ روی Postgres واقعیِ در حال اجراست، نه یک اجرای معلق یا Mock.

## ۴. چه چیزی عمداً ساخته **نشد** (طبق ممنوعیت‌های بند ۳ دستور)

- **هیچ کد Adapter واقعی AC-2** — قفل تا تصویب CCR.
- **`shared-contracts/types.ts` و `prisma/schema.prisma`** — مطلقاً دست‌نخورده (Drift صفر اثبات‌شده). CCR فقط *پیشنهاد* تغییر است، نه اعمال آن.
- **`evaluateAC2FailClosed`، `OpportunityReadService`، Featureها، `jest.config.js`، `mlino2/`** — هیچ‌کدام لمس نشدند.
- **هیچ بخشی از لایه‌ی دانش** — نه Schema، نه خط تولید تجمیع، نه آستانه، نه مخزن سناریو. فقط ثبت تصمیم و GAP.
- **هیچ Consent جعلی یا ضمنی؛ هیچ Redaction خاموش.**
- **هیچ تصمیمی روی موارد باز:** عدد آستانه‌ی N، Consumer دانه‌ریز برای خروج داده، و انتخاب بین ارسال مالکیت توسط Producer در برابر پرکردن در مرز Admission — همگی به‌عنوان تصمیم باز علامت خوردند، نه حدس زده شدند.

## ۵. Commit کد/اسناد (Evidence)

طبق الگوی دو-Commit، سه سند ابتدا جداگانه Commit و Push شدند تا Hash واقعی به‌دست آید:

```
d1c886a  "AC-2 policy alignment: design doc v1.1 + ownership-type CCR draft + R8 gap doc"
```

این گزارش در Commit دوم و جداگانه Push می‌شود؛ Hash آن مستقیماً در پاسخ نهایی به کاربر گزارش می‌شود، نه داخل این فایل.

## ۶. اقدام بعدی

**متوقف می‌شوم** — طبق شرط توقف بند ۵ دستور. زنجیره‌ی بعدی: بازبینی مستقل ممد روی سه خروجی → تایید نهایی CCR توسط مالک محصول → و فقط پس از آن، فاز Adapter واقعی (گزینه ۱ عملیاتی‌شده) مجاز می‌شود.

# گزارش اصلاح ADRها — اعمال یافته‌های بازبینی

**تاریخ:** ۱۱ سپتامبر ۲۰۲۶
**نقش:** ویرایشگر ADR معماری MLINO (Claude Opus 5)
**مبنا:** [`ADR_REVIEW_BEFORE_APPROVAL.md`](ADR_REVIEW_BEFORE_APPROVAL.md) — اصلاحات ر۱ تا ر۹ و یادداشت‌ها، طبق دستور مالک
**دامنه:** فقط ویرایش متن سه ADR — **هیچ تصمیم معماری تغییر نکرد، هیچ تصمیم تازه‌ای افزوده نشد، D-58 تا D-71 دست نخوردند، بدون کد، شِما، قرارداد، migration یا API.**

---

## ۰. وضعیت زنده

**مخزن پیش از ویرایش بررسی شد:** همگام با remote (`47ae844`)، درخت کاری تمیز، `HANDOFF_STATE` برابر تحویل بازبینی. **آخرین تغییر `adr/` همان پیش‌نویس‌های من (`5f48ba4`) بود** — هیچ عامل دیگری آن‌ها را تغییر نداده بود.

## ۱. وضعیت هر سه ADR

| ADR | سرخط |
|---|---|
| ADR-0010 | **Draft — Pending Owner Approval — Not Approved** · پیش‌نویس — در انتظار تصویب مالک — مصوب نیست |
| ADR-0011 | **Draft — Pending Owner Approval — Not Approved** · پیش‌نویس — در انتظار تصویب مالک — مصوب نیست |
| ADR-0012 | **Draft — Pending Owner Approval — Not Approved** · پیش‌نویس — در انتظار تصویب مالک — مصوب نیست |

هر سرخط یک سطر «ویرایش» هم گرفت که می‌گوید کدام اصلاحات بازبینی اعمال شد، «بدون تغییر هیچ تصمیم».

---

## ۲. اصلاحات اعمال‌شده

### ADR-0010

| # | بخش | پیش از اصلاح | پس از اصلاح | خواسته‌ی مالک |
|---|---|---|---|---|
| **ر۱** | تصمیم ۱ — جدول کانال‌ها | ستون «تنها منبع»؛ ردیف داده: «یکپارچه‌سازی و پیوند» | ستون «از کجا می‌آید»؛ اختیار و هویت «**فقط** …». ردیف داده: **پنج منبع D-31** (`HUMAN` · `CUSTOMER_DATA` · `SYSTEM` · `AI_INFERRED` · `INTEGRATION`)؛ یکپارچه‌سازی **یکی از منابع و یک سازوکار انتقال** است، **نه تنها منشأ**، و **فقط داده** حمل می‌کند | منبع داده طبق D-31 · یکپارچه‌سازی سازوکار انتقال است |
| **ر۸** | تصمیم ۳ | فقط «هیچ دارنده‌ای را حذف نمی‌کند» | + **حذف دارنده مسیر جداگانه است**: فقط با سند بیرونی راستی‌آزمایی‌شده، از مسیر لایه‌ی بازیابی مالکیت، با دو بازبین و دوره‌ی انتظار. + اختلاف شرکا فقط با سند بیرونی از همین مسیر (D-58 ۴٫۱ و ۴٫۶) | مسیر ازقلم‌افتاده‌ی D-58 |
| **—** | تصمیم ۲ | «`ADMIN_ACTION` کانال اجراست» | + «مقداری در `basis` **پیوند یکپارچه‌سازی** است (D-25)، **نه مبنای اعطای اجازه** — مبنای اعطا هرگز `ADMIN_ACTION` نیست (D-57)» | `ADMIN_ACTION` مبنای یکپارچه‌سازی است، نه اعطا |
| **ر۹ ب** | زمینه | «نبود رکورد سازمان در V1» | «`organization_id` در توکن — که در نبود رکورد سازمان **تنها لنگر سازمانی** است؛ طبق D-57 مرجع عضویت یا اختیار نیست» | روشن کردن خطر `organization_id` توکن |
| **یادداشت** | تصمیم ۶ | «بستن، **معلق کردن**، منقضی کردن و پس گرفتن خودکار مجاز است» | **قاعده‌ی جهت:** عمل خودکار **فقط می‌بندد یا محدود می‌کند** و **هرگز اختیار نمی‌دهد**. این ADR هیچ عمل مشخصی را خودکار اعلام نمی‌کند؛ نگه‌داشت ایمنی و تعلیق ادعا **عمل پلتفرم**‌اند | خودکار فقط بستن و محدودسازی |
| **ر۹ الف** | پیامدها و اجرا | «دو CCR آینده» (پیوند + مبناهای اعطا) | **یک** CCR روی پیوند. مبناهای `designated_succession` و `ownership_recovery` **بخشی از پیاده‌سازی D-57**‌اند، نه CCR | — |

### ADR-0011

| # | بخش | پیش از اصلاح | پس از اصلاح | خواسته‌ی مالک |
|---|---|---|---|---|
| **ر۴** | سرخط — «سند کامل» | فقط پیوند به سند OD-40 | + **خوانش طبق D-65:** «Core هیچ نام نقشی ندارد» در آن سند یعنی Core هیچ **نقش دامنه‌ی کسب‌وکار** ندارد؛ **دسته‌های مسئولیت عمومی D-55 مفاهیم Core‌اند** | روشن‌سازی D-65 |
| **ر۳** | تصمیم ۷ | — | + «**ذخیره‌سازی ماژول از نخستین ساخت، جدا ساخته می‌شود — از Core مهاجرت داده نمی‌شود.** امروز هیچ جدول دامنه‌ای در Core نیست» | ساخته می‌شود، نه مهاجرت |
| **ر۳** | تصمیم ۸ — م۲ | «جابه‌جایی کد **و ذخیره‌سازی**» | «**جابه‌جایی کد**» + «ذخیره‌سازی ماژول بخشی از م۲ نیست؛ از نخستین ساخت — مثلاً گام ۳ — مال ماژول است (D-71)» | همان |
| **یادداشت** | تصمیم ۷ | «دیتابیس Core جدول دامنه‌ی کسب‌وکار ندارد» | **عبارت مالک دست نخورد** + توضیح: «یعنی **مرز ذخیره‌سازی Core**؛ schema جدا یا دیتابیس جدا طبق خود D-71 در برنامه‌ریزی پیاده‌سازی تعیین می‌شود» | جدایی schema در برنامه‌ریزی پیاده‌سازی |
| **یادداشت** | بدیل‌های ردشده | «جدول‌های ماژول در همان schema مشترک Core» | «جدول‌های ماژول **درون ذخیره‌سازی Core**» + «این رد سازوکار جدایی را انتخاب نمی‌کند» | همان |
| **یادداشت** | تصمیم ۵ | نحو `<ماژول>.<خانواده>` | «**با فضانام خودش**؛ نحو نهایی در CCR م۱؛ مثال: `restaurant.opportunity.table_capacity`» | Core بدون واژگان صنفی؛ جزئیات قفل نشود |
| **یادداشت** | تصمیم ۸ — م۱ | «بدون migration» | «**ستون‌های موجود** migration نمی‌خواهند؛ شکل registry در خود CCR م۱» | همان |

**«Core بدون واژگان دامنه‌ی کسب‌وکار» دست نخورد** — تصمیم ۲ و همه‌ی قواعد بدون تغییر.

### ADR-0012

| # | بخش | پیش از اصلاح | پس از اصلاح | خواسته‌ی مالک |
|---|---|---|---|---|
| **ر۵** | تصمیم ۱ | «V2 **نمایش می‌دهد**» | «V2 **تجربه را می‌سازد** (D-64) — کشف، تطبیق و تعامل منطق خود V2‌اند» | «V2 تجربه را می‌سازد» |
| **ر۲** | تصمیم ۳ — افزوده | — | **دو دستیار جدا می‌مانند:** دستیار مشتری اختیار هیچ شخصی را حمل نمی‌کند و **این ADR ورود کاربر را برایش الزامی نمی‌کند** — لزوم ورود تصمیم آینده‌ی محصول است. دستیار عملیاتی با اختیار عضو و پشت AC-2؛ قیود Session و کار پس‌زمینه **فقط بر آن** حاکم است | جدا کردن دو دستیار · ورود دستیار مشتری الزامی نشود |
| **ر۲** | پیامدها | «دستیار فقط درون Session یک انسان فعال…» | «**دستیار عملیاتی کسب‌وکار** — دستیاری که با اختیار یک عضو عمل می‌کند — فقط درون Session فعال همان عضو…؛ دستیار مشتری مشمول این قید نیست» | همان |
| **ر۶** | تصمیم ۴ — افزوده | — | «**هیچ Sessionی — برای هیچ کاربر یا دستیاری — بافت سازمانی حمل نمی‌کند**» | Session هرگز بافت سازمانی ندارد |
| **ر۶** | پیامدها | «Session دستیار مشتری بافت سازمانی ندارد» | «درخواست‌های دستیار مشتری **هیچ** بافت سازمانی ندارند — نه در Session نه در درخواست. درخواست دستیار عملیاتی بافت را **به‌ازای هر درخواست** دارد و با عضویت سنجیده می‌شود — **نه در Session**» | همان |
| **ر۷** | سازگاری D-55 · هزینه | «`role` توکن مبنای هیچ تصمیمی نیست» | «مبنای هیچ **تصمیم اختیاری** نیست؛ **بررسی "نقش شناخته‌شده" در AC-2 تا م۱ موقتاً می‌ماند** (D-63، `org-membership-ac2-port.ts:63`)» — در سازگاری و در هزینه | نقش AC-2 تا مهاجرت می‌ماند |
| **—** | بدیل‌های ردشده | «V2 فقط رندر می‌کند» | «**در ارکستراسیون**، V2 فقط رندر می‌کند (D-64)؛ کشف، تطبیق و تعامل منطق V2‌اند» | حفظ مرز D-64 |

**مرزهای D-64 و D-67 حفظ شدند:** ارکستراسیون تجربه Core · زیربنای دستیار Core · رابط دستیار مشتری V2 · دستیار مشتری فقط حقیقت منتشرشده · دستیار عملیاتی داخلی و پشت AC-2. **روشن‌سازی مصوب D-53 عیناً دست نخورد.**

---

## ۳. بررسی سازگاری پس از ویرایش — ۱۵ تصمیم

✅ = سازگار · — = موضوع آن ADR نیست

| تصمیم | ADR-0010 | ADR-0011 | ADR-0012 | توضیح |
|---|---|---|---|---|
| **D-08** — AC-2؛ مصرف‌کننده = سازمان | ✅ | ✅ | ✅ | پلتفرم محتوای کسب‌وکار را نمی‌خواند · ذخیره‌سازی ماژول از همان دروازه · Session دسترسی را فردی نمی‌کند |
| **D-31** — یازده نوع · پنج منبع | ✅ **اصلاح‌شده (ر۱)** | ✅ | — | ردیف داده حالا پنج منبع را نام می‌برد؛ ADRها هیچ نوع یا منبعی نمی‌افزایند |
| **D-43** — تصمیم بیرونی `Decision` نمی‌شود | ✅ | ✅ | ✅ | حکم بیرونی = شاهد ممیزی · ماژول تصمیم نمی‌سازد · دستیار و ارکستراسیون تصمیم نمی‌سازند |
| **D-52** — انتشار انسانی؛ خودکار فقط پس گرفتن | ✅ **اصلاح‌شده (قاعده‌ی جهت)** | ✅ | ✅ | خودکارسازی فقط بستن و محدودسازی · بدون طبقه‌بندی Core · دستیار مشتری فقط منتشرشده |
| **D-55** — نقش ≠ اجازه؛ مدیر پلتفرم بدون اجازه | ✅ | ✅ | ✅ **اصلاح‌شده (ر۷)** | D-55 بازنویسی نشد · دسته‌ها Core · بررسی نقش AC-2 به‌عنوان وضع موقت صادقانه توصیف شد |
| **D-57** — اعطا به عضویت؛ مبنا بدون `ADMIN_ACTION` | ✅ **اصلاح‌شده (`ADMIN_ACTION` · ر۹)** | ✅ | ✅ | `ADMIN_ACTION` صریحاً مبنای پیوند، نه اعطا · مبناهای D-58 بخشی از پیاده‌سازی D-57 · claim توکن مرجع نیست |
| **D-58** — بازیابی پنج‌لایه؛ می‌افزاید | ✅ **اصلاح‌شده (ر۸)** | — | ✅ | مسیر حذف دارنده با سند بیرونی افزوده شد · D-70 اعطا را تغییر نمی‌دهد · دستیار بیرون از مسیر تصمیم |
| **D-61** — ساختن آزاد؛ راستی‌آزمایی پیش از انتشار | ✅ | — | — | بدون تغییر |
| **D-62** — پیوند = نگاشت هویت؛ رضایت دوطرفه | ✅ | ✅ | ✅ | «فقط داده حمل می‌کند» حالا عین D-62 · registry و قاعده‌ی ۱۰ سازگار · بدون Session دوم |
| **D-63** — Core بدون واژگان صنفی؛ م۰ تا م۲ | — | ✅ **اصلاح‌شده (ر۳ · نحو · migration)** | ✅ | م۲ فقط کد · جزئیات م۱ به CCR سپرده شد · بررسی نقش AC-2 تا م۱ |
| **D-64** — Core · V1 · V2 | — | — | ✅ **اصلاح‌شده (ر۵)** | «V2 تجربه را می‌سازد» — عین D-64 · «فقط رندر» محدود به ارکستراسیون |
| **D-65** — بدون نقش صنفی؛ دسته‌ها Core | — | ✅ **اصلاح‌شده (ر۴)** | — | خوانش سند OD-40 طبق D-65 |
| **D-66** — Session فقط هویت | — | — | ✅ **اصلاح‌شده (ر۶)** | «هیچ Sessionی بافت سازمانی حمل نمی‌کند» |
| **D-67** — دو دستیار | — | — | ✅ **اصلاح‌شده (ر۲)** | دستیار مشتری بدون الزام ورود · قیود Session فقط بر دستیار عملیاتی |
| **D-70** — بستن Session | ✅ | — | ✅ | سازگار با قاعده‌ی جهت ADR-0010 · هر چهار قاعده عیناً |
| **D-71** — مرز دیتابیس ماژول | — | ✅ **اصلاح‌شده (ر۳ · یادداشت)** | — | هر پنج قاعده عیناً + «از نخستین ساخت جدا، نه مهاجرت» + شکل فیزیکی باز |

**نتیجه: هیچ تعارضی باقی نمانده است.**

### ۳٫۱ بررسی ماشینی

عبارت‌های مسئله‌دار بازبینی در هر سه فایل جست‌وجو شدند و **هیچ‌کدام باقی نمانده است:**
- «تنها منبع»
- «V2 نمایش می‌دهد»
- «درون Session یک انسان»
- «مبنای هیچ تصمیمی نیست»
- «جابه‌جایی کد و ذخیره‌سازی»
- `<ماژول>.<خانواده>`
- «معلق کردن، منقضی»
- «دو CCR»
- «نبود رکورد سازمان در V1»
- «Session دستیار مشتری بافت»

**واژه‌ی «Approved» در هر فایل فقط یک بار آمده است** — درون «Not Approved».

## ۴. آنچه تغییر نکرد

| | |
|---|---|
| **هیچ تصمیم معماری** | ✅ همه‌ی اصلاحات متنی‌اند؛ هر افزوده به یک تصمیم ثبت‌شده یا دستور همین پاس ارجاع دارد |
| **هیچ تصمیم تازه** | ✅ «ورود دستیار مشتری الزامی نیست» فقط **الزام نکردن** است و تصمیم را به محصول می‌سپارد؛ «هیچ Sessionی بافت سازمانی ندارد» روشن‌سازی درخواستی D-66 است |
| **D-58 تا D-71 · دفتر تصمیم‌ها · CHANGELOG · نقشه‌ی راه** | دست نخوردند |
| **سند OD-40** | دست نخورد — خوانش D-65 فقط در سرخط ADR-0011 آمد |
| **قرارداد · شِما · کد · migration · API** | دست نخوردند |
| **تصویب** | **هیچ‌کدام** — هر سه «Draft — Pending Owner Approval — Not Approved» |

## ۵. وضعیت

**سه ADR اصلاح‌شده، آماده‌ی بازبینی و تصمیم مالک‌اند.** چون نویسنده، بازبین و ویرایشگر یک مدل است، **بازبینی دوم مستقل (Mamad یا Codex) پیش از تصویب همچنان توصیه می‌شود.**

**پس از تصویب:** ثبت ADRها در دفتر تصمیم‌ها و CHANGELOG، و تغییر سرخط وضعیت — **با دستور جداگانه.**

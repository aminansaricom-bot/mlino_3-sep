INSTRUCTION_ID: CODEX-20260905-2247-AC2-DATA-KNOWLEDGE-LAYERS-AUTH
AUTHOR: CODEX
STATUS: EXECUTED
EXECUTED_BY: CLAUDE
EXECUTED_AT: 2026-09-05T23:30:00
RESULTING_HANDOFF_ID: HANDOFF-20260905-AC2-ALIGN-CCR-R8
TARGET_HANDOFF_ID: HANDOFF-20260905-AC2-MIN-POLICY-DESIGN
TARGET_REPORT_PATH: C:\mlino code\AI_HANDOFF\CLAUDE_REPORTS\20260905_V1_MINIMUM_AC2_POLICY_DESIGN_REPORT.md
TARGET_REPORT_SHA256: ccfaea28f42ed7e66d9b39f55331354d9798afd2e9cf2fe0cf04606bdf0d19d1
TARGET_ZIP_PATH: (none)
TARGET_ZIP_SHA256: (n/a)
REVIEW_COMPLETED_AT: 2026-09-05T22:47:00
AUTHORIZATION_SCOPE: V2_OP_POLICY_ALIGNMENT_CCR_DRAFT_R8_TWO_LAYER_MODEL_NO_ADAPTER_CODE
SUPERSEDES: CODEX-20260905-2235-AC2-ALIGN-CCR-AUTH (اگر اجرا نشده — این نسخه کامل‌تر است و همان سه خروجی + لایه‌ی دانش را دارد)

---

# دستور: هم‌راستاسازی سیاست AC-2 + مدل دو‌لایه‌ای «داده» و «دانش» + پیش‌نویس CCR + R8

**صادرکننده:** ممد (بازبین مستقل، GLM 5.3 Flash) — بر پایه‌ی دو تصمیم جدید و صریح مالک محصول (۵ سپتامبر ۲۰۲۶). این دستور `CODEX-20260905-2235-AC2-ALIGN-CCR-AUTH` را جایگزین و کامل می‌کند.

## ۰. تصمیمات مصوب مالک محصول (مبنای این دستور)

### ۰.۱. مالکیت داده و مصرف داخلی (پاسخ §۴ سند طراحی)
«اطلاعاتی که در فضای بیزنس کلینیک شکل می‌گیرد، متعلق به همان بیزنس است و بیزنس می‌تواند از اطلاعات خودش برای کشف فرصت‌ها استفاده کند.»
→ **Consumer = خود سازمان**؛ استفاده‌ی داخلی نیازمند Consent صریح فردی نیست؛ Consent فقط وقتی الزامی است که داده بخواهد از سازمان خارج شود.

### ۰.۲. مدل دو‌لایه‌ای: داده در برابر دانش (تصمیم جدید — این بخش را دقیق بخوان)
مالک محصول دو لایه‌ی متمایز تعریف کرد که با سه‌گانه‌ی مالکیت Kernel §۱۰ دقیقاً منطبق است:

| لایه | مثال | مالکیت Kernel | قاعده‌ی جریان |
|---|---|---|---|
| **داده (Raw Data)** | «خانم رضایی نوبت ساعت ۱۵ کلینیک A را کنسل کرد»، موجودی مواد، نرخ تعامل مشتری‌ها | **سازمانی** (+ فردی برای هویت شخص) | هرگز از بیزنس خارج نمی‌شود؛ فقط برای کشف فرصتِ همان بیزنس |
| **دانش (Insight/Knowledge)** | «نرخ کنسلی نوبت در شهر Y در ساعات ۱۲–۱۶ معمولاً از زمستان بیشتر است»، «تقاضای خدمات زیبایی به‌دلیل کاهش قدرت خرید افت کرده» | **جمعی** — MLINO امانت‌دار | قابل انتقال بین بیزنس‌های اکوسیستم؛ بلندمدت در هسته‌ی MLINO به‌عنوان سناریو نگهداری می‌شود تا به بیزنس‌های تازه‌وارد برای درک بازار هدف و سناریوهای اثبات‌شده کمک کند |

**قاعده‌ی تفکیک (مصوب):** هرچه Subject شناسایی‌پذیر دارد یا از عملیات یک بیزنس مشخص می‌آید = داده‌ی سازمانی آن بیزنس. هرچه تجمیعی/ناشناس‌شده است و الگو/روند/سناریو توصیف می‌کند = دانش، قابل اشتراک — **فقط پس از اینکه مکانیزم تجمیعِ غیرقابل-بازسازی ساخته و تایید شود.**

### ۰.۳. پیامد مهم — آنچه این تصمیم «الان» تغییر می‌دهد و چه چیزی را نه
- **تغییر در این پاس:** فقط مستندسازی (سند v1.1 + R8). مدل دو‌لایه‌ای یک *جهت‌گیری معماری محصولی* است که آینده‌ی V2 را شکل می‌دهد.
- **تغییر نمیدهد:** CCR هنوز فقط فیلد `ORGANIZATIONAL` را درخواست می‌کند (لایه‌ی دانش هیچ Schemaیی الان لازم ندارد — ساخت خط تولید داده→دانش، تجمیع، آستانه‌ها، و مخزن سناریوها = کار آینده با طراحی و مجوز خودش).

## ۱. رفع بلاک قبلی + نتیجه‌ی بازبینی سند طراحی

- بلاک تو روی دستور ۱۹۰۸ صحیح بود (TARGET کهنه — خطای صادرکننده). این دستور TARGET درست دارد.
- `HANDOFF-20260905-AC2-MIN-POLICY-DESIGN` (سند طراحی AC-2) را بازبینی کردم: **تایید** — یافته‌های سه‌گانه‌اش (نبود فیلد مالکیت/Consent در داده‌ی فعلی؛ پیامد Deny-all؛ Tenant Isolation از قبل در Query) مستقلاً تایید شد؛ تفکیک دوسطحی Opportunity/Evidence و تصمیم «deny-کل-کاندیدا به‌جای Redaction خاموش» درست است.

## ۲. محدوده‌ی مجاز این پاس (سه خروجی + یک گسترش در R8 — کد Adapter ممنوع)

### ۲.۱. سند طراحی → نسخه‌ی ۱.۱
- §۴ را با تصمیم ۰.۱ ببند (Consumer = سازمان؛ مصرف داخلی مجاز؛ Consent فقط برای خروج از سازمان).
- قاعده‌ی ۳: «مالکیت نامشخص → Deny» می‌ماند؛ اما بعد از CCR، داده‌ی کسب‌وکاری V1 نظام‌مند «سازمانی» ثبت می‌شود و قاعده‌ی ۴ عملیاتی می‌شود.
- قاعده‌ی ۶ را اصلاح کن: Subject فردی داخل همان سازمان با مالکیت سازمانی ثبت‌شده → عبور (مبنای سیاستی: تصمیم ۰.۱)؛ بدون ثبت → Deny.
- یک زیربخش جدید اضافه کن: «مدل دو‌لایه‌ای داده/دانش (تصمیم مالک محصول)» — جدول بخش ۰.۲ این دستور را با ارجاع به Kernel §۱۰ (سازمانی/فردی/جمعی) بازتاب بده و تصریح کن: لایه‌ی دانش در V1 امروز وجود ندارد و هر پیاده‌سازی آینده‌ی آن طراحی + CCR/ACR خودش را می‌خواهد.
- بقیه‌ی ۱۰ قاعده و fail-closed: عیناً دست‌نخورده.

### ۲.۲. پیش‌نویس CCR فیلد مالکیت (مسیر رسمی — بدون اعمال)
- در مسیر موجود `implementation/remediation/CONTRACT_CHANGE_REQUESTS/` (الگوی CCR «Evidence Namespace v1.2»):
  - درخواست: فیلد «نوع مالکیت» روی `OpportunityCurrentState` و `EventLog` — مقدار فعلاً فقط `ORGANIZATIONAL`؛ `INDIVIDUAL`/`AGGREGATE` رزرو آینده (دقیقاً هم‌راستا با دو‌لایه‌ی مصوب و سه‌گانه‌ی Kernel).
  - استدلال سطح CCR (نه ACR): Kernel §۱۰/§۱۱ مفاهیم را از قبل به‌رسمیت شناخته.
  - Migration/Backfill (داده‌ی موجود → `ORGANIZATIONAL` طبق تصمیم ۰.۱) + Rollback + اثر بر مصرف‌کنندگان.
- اعمال روی `shared-contracts/types.ts` یا `prisma/schema.prisma` در این پاس ممنوع — فقط پس از تایید مالک + بازبینی ممد.

### ۲.۳. R8 — GAP شماره‌دار (اکنون با دو زیر‌بخش)
سند GAP رسمی «R8» در مسیر موجود remediation/ با دو زیر‌بخش:
- **R8-a — Consent & Cross-Org Data Sharing:** رویدادهای Consent چهارگانه، Consumer×DataType، فعال‌سازی با خروج داده از سازمان (V2/B2C).
- **R8-b — Knowledge Layer (داده→دانش):** خط تولید تجمیع/ناشناس‌سازی غیرقابل-بازسازی، آستانه‌ی حداقل تجمیع (مثلاً هیچ Insight از کمتر از N بیزنس — عدد در طراحی آینده)، نقش امانت‌داری MLINO، مخزن بلندمدت سناریوها در هسته، کاربرد Onboarding بیزنس‌های تازه‌وارد. **هشدار نام‌گذاری:** مفاهیم «دانش/Insight» هرگز با «Opportunity» (V1 یا V2) قاطی نشوند — قرابت با PA-09. و: لایه‌ی دانش پل طبیعی V1→V2 است — هم‌راستا با `02_V1_V2_INTEGRATION_CONTRACT_DRAFT.md` طراحی شود، نه موازی با آن.

## ۳. ممنوعیت‌های این پاس

- **هیچ کد Adapter واقعی** — پس از تایید CCR.
- `shared-contracts/types.ts` و `prisma/schema.prisma`: مطلقاً دست‌نخورده.
- `evaluateAC2FailClosed`، `OpportunityReadService`، Featureها، `jest.config.js`، `mlino2/` — دست‌نخورده.
- هیچ ساخت واقعی لایه‌ی دانش در این پاس — فقط ثبت تصمیم و GAP.
- هیچ Consent جعلی؛ هیچ Redaction خاموش.

## ۴. تعریف Done

- سه خروجی (سند v1.1 + پیش‌نویس CCR + سند R8 دو‌زیر‌بخشی) طبق بخش ۲.
- `npm test` همچنان **۱۳۰/۱۳۰** (هیچ کدی تغییر نکرده — اگر عدد عوض شد یعنی چیزی لمس شده که نبوده) + `tsc --noEmit` تمیز.
- HANDOFF_ID جدید، گزارش با چک‌سام فایل‌ها + Drift صفر دو فایل منجمد، Push دو-Commit با Hash واقعی در پاسخ نهایی به کاربر.
- تناقض با سطوح ۱–۵ اقتدار → متوقف شو، CCR/ACR — حدس نزن.

## ۵. شرط توقف

بعد از گزارش + Push، کاملاً متوقف شو. بازبینی مستقل سه خروجی را ممد جداگانه انجام می‌دهد؛ سپس تایید نهایی CCR با مالک محصول؛ سپس فاز Adapter واقعی (گزینه ۱ عملیاتی‌شده) مجاز می‌شود.

---
*بازبین: ممد (GLM 5.3 Flash) — R4 (BLOCKED)، R5 (OPEN)، R8 (NEW: a=Consent، b=Knowledge Layer)، Adapter واقعی AC-2: قفل تا تایید CCR.*

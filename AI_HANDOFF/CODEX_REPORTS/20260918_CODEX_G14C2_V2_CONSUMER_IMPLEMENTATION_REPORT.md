# گزارش اجرای G14c-2 — مصرف‌کنندهٔ export عمومی V2

تاریخ: ۲۰۲۶-۰۹-۱۸
INSTRUCTION_ID: CODEX-20260918-G14C2-V2-CONSUMER-IMPLEMENTATION-001
TARGET_HANDOFF_ID: HANDOFF-20260918-OWNER-APPROVAL-G14C2
CODEX_WORKSTREAM_HANDOFF_ID: HANDOFF-20260917-V2-PUBLIC-CONSUMER
REVIEW_REFERENCE: `b36be016e279553954801132b628fcd212f3940e:AI_HANDOFF/CLAUDE_REVIEWS/20260918_OWNER_APPROVAL_G14C2_IMPLEMENTATION.md`
BASE: `f4d326f7d2046eee997dba4198f662c6e5d1e84e`
BRANCH: `codex/v2-public-consumer`
وضعیت: `IMPLEMENTED_LOCALLY_AWAITING_GUARDIAN_REVIEW`؛ Push انجام نشده است.

## ۱. کار اجراشده

مصرف‌کنندهٔ مرورگری export امضاشدهٔ `mlino.v2.public-business.v1` پیاده شد: canonicalization سازگار با V1، بررسی امضای Ed25519 با کلید عمومی مجاز، دریافت از درگاه جداشدنی، بررسی زمان و ترتیب، تعویض اتمیک snapshot، نگاشت فیلدهای عمومی و حذف موارد منقضی در زمان خواندن. مسیر واقعی برنامه پیش‌فرض است؛ مسیر mock قدیمی فقط با `VITE_PUBLIC_EXPORT_MODE=demo` فعال می‌شود و در UI و لاگ برچسب دارد. هیچ fallback از دادهٔ واقعی به mock وجود ندارد.

| تصمیم/شرط | محل اجرا | شاهد آزمون یا سند |
|---|---|---|
| C8-1، انتقال جداشدنی | `transport.ts`، `consumer.ts` | `C8-1 accepts the same signed bytes through pluggable file and fetch ports without a network call` |
| C8-2، چرخهٔ ۶۰ ثانیه، TTL ‏۳۰۰ ثانیه، انحراف ۳۰ ثانیه | `consumer.ts`، `RealPublicApp.tsx` | `C8-2 and N1 use the supplied device clock for TTL, skew and ordering`؛ `FETCH_INTERVAL_MS` در همان آزمون بررسی می‌شود |
| C8-3، فقط کلید عمومی مجاز و لغو کلید | `trustBundle.ts`، `verify.ts` | `rejects unknown, revoked and non-Ed25519 key ids or algorithms`؛ `N2 does not accept trust keys injected by the artifact itself` |
| C8-4، حذف فیلترهای بی‌منبع | `mapping.ts`، `RealPublicApp.tsx` | `accepts a valid artifact and maps every public field without draft-1 inventions`؛ `rejects links outside the same record and excludes null coordinates from proximity` |
| C8-5، حذف در چرخهٔ بعدی | `consumer.ts` | `C8-5 removes a suspended business on the next accepted cycle, not before` |
| N1، ساعت دستگاه مرز امنیتی نیست | توضیح در `consumer.ts` و `THREAT_MODEL.md` | `C8-2 and N1 use the supplied device clock for TTL, skew and ordering`؛ محدودیت امنیتی در سند تهدید ثبت شده است |
| N2، امضا منشأ آلودهٔ V2 را ایمن نمی‌کند | `THREAT_MODEL.md` | `N2 does not accept trust keys injected by the artifact itself` فقط عدم تزریق کلید از artifact را می‌سنجد؛ محدودیت منشأ در سند تهدید است |
| N3، برابری بایت با V1 | `fixtures/v1-canonical.json`، `canonical.ts` | هشت مورد `$name matches bytes and sha256 generated by origin/main V1` و `rejects lone surrogates and values beyond six decimal places` |

آزمون‌های مکمل با نام‌های `rejects a one-byte content tamper and keeps the prior valid cache`، `starts empty and never accepts draft-1 mock as a real fallback`، `filters capability and offer expiry again at read time` و `swaps one snapshot reference after verification while in-flight reads stay consistent` نیز اجرا شدند.

## ۲. اسناد منبع و پیش‌شرط

- تصویب مالک و دستور کامل G14c-2 در commit پین‌شدهٔ بالا؛ SHA-256 بایت‌های `git show` برابر `9354a16ee82e3ee6ea7b25295546290c66ce671db5583668f3e2a23a1a478181` است.
- طرح نهایی G14c-1، قرارداد public-business.v1 و `origin/main:implementation/public-export/canonical.ts`.
- `git fetch origin` به‌علت محدودیت شبکه ناموفق بود. GW2-P: `git cat-file -e b36be016e279553954801132b628fcd212f3940e^{commit}` موفق؛ `git merge-base --is-ancestor b36be016e279553954801132b628fcd212f3940e origin/main` موفق؛ SHA-256 خروجی `git show` دقیقاً با مقدار پین‌شده برابر. هیچ credential یا git config تغییر نکرد.

## ۳. فایل‌های تغییریافته

- `mlino2/app/src/App.tsx`: انتخاب صریح demo یا مسیر واقعی، بدون fallback.
- `mlino2/app/src/publicExport/`: هشت فایل منبع/آزمون و یک fixture JSON؛ فهرست و hash تک‌تک در `mlino2/validation/g14c-2/LF-MANIFEST.txt` است.
- `mlino2/validation/g14c-2/`: یادداشت تهدید، اسکریپت ساخت fixture، اسکریپت اجرا، سه لاگ build، سه لاگ test، لاگ fixture، خلاصه و manifest.
- همین گزارش و افزودن ورودی به `mlino2/HANDOFF/HANDOFF_STATE.md`.

## ۴. فایل‌ها و محیط تغییریافته‌نشده

`implementation/**`، قرارداد FINAL، شاخه‌های `main`، طراحی G14c-1 و V2 مبنا، schema/migration، `package.json` و lockfile تغییر نکردند. Docker، پایگاه‌داده، کلید واقعی، شبکه در آزمون‌ها و Push استفاده نشدند. کلید خصوصی فقط در آزمون و در حافظه برای ساخت artifact آزمایشی تولید شد و در فایل‌ها یا build نیست.

## ۵. آزمون‌ها و تولید شواهد

در worktree V1 به مسیر `C:/Users/galexy/mlino code/core-profile-unique`، blob فایل `implementation/public-export/canonical.ts` با `origin/main` مقایسه شد: هر دو `772dddbb3c29af11c44a06e17e741c07faaae9ff`. اسکریپت `generate-v1-fixtures.cjs` همان فایل V1 را بدون تغییر با TypeScript نصب‌شدهٔ V1 transpile و اجرا کرد و ورودی و بایت canonical و SHA-256 مورد انتظار را یک‌بار تولید کرد. هشت مورد شامل NFC متن غیرفارسی/فارسی، شش رقم اعشار، منفی صفر، null صریح در برابر کلید غایب، رشتهٔ خالی، ترتیب آرایهٔ تودرتو و ترتیب کلیدهاست؛ V2 مقادیر مورد انتظار را تولید نکرد.

از `mlino2/app`، سه نوبت متوالی `npm run build` و `npm test` اجرا و لاگ‌ها commit شدند. آزمون Fetch از stub استفاده می‌کند و تماس شبکه‌ای ندارد. `npm ci --offline` به‌دلیل نبود cache کامل npm موفق نشد؛ `node_modules` موجود از worktree V2 با همان lockfile کپی شد، بدون تغییر dependency یا lockfile. محدودیت sandbox برای esbuild باعث شد همان فرمان‌های build/test با دسترسی اجرایی مجاز دوباره اجرا شوند؛ نتیجهٔ نهایی در لاگ‌هاست.

## ۶. نتایج

| نوبت | build | test |
|---|---|---|
| ۱ | PASS | ۱۴ فایل، ۲۲۱/۲۲۱ آزمون PASS |
| ۲ | PASS | ۱۴ فایل، ۲۲۱/۲۲۱ آزمون PASS |
| ۳ | PASS | ۱۴ فایل، ۲۲۱/۲۲۱ آزمون PASS |

`git diff --check` نیز بدون خطا بود. همهٔ SHA-256های فایل‌های منبع، آزمون، fixture و لاگ از **بایت‌های `git show HEAD:<path>`** محاسبه و در `LF-MANIFEST.txt` ثبت شده‌اند. خط manifest، خود manifest را شامل نمی‌شود تا وابستگی دوری ایجاد نشود.

## ۷. Commitها

- `f4f22cd` — `feat(v2): consume signed public business exports`
- `3e05b3f` — `test(v2): preserve three consumer validation runs`
- `e3e7cc2` — `docs: normalize validation log endings`
- commit تحویل گزارش، manifest و Handoff پس از این گزارش ساخته می‌شود؛ شناسهٔ نهایی در پیام تحویل اعلام خواهد شد.

همهٔ commitها محلی و فقط روی `codex/v2-public-consumer` هستند. Push و ادغام انجام نشده است.

## ۸. ریسک‌های باقی‌مانده

- آدرس واقعی artifact، کلید عمومی عملیاتی و چرخهٔ تولید/تحویل تنظیم نشده‌اند. برنامه در حالت واقعی تا آن زمان پیام «داده در دسترس نیست» می‌دهد و mock را جایگزین نمی‌کند.
- راه عملیاتی لغو فوری کلید باید پیش از بهره‌برداری واقعی تصویب و آزموده شود. تغییر trust bundle در این نسخه نیازمند انتشار نسخهٔ V2 است.
- TTL به ساعت دستگاه وابسته و فقط محافظ درستی/تجربهٔ کاربر است؛ امضا در برابر منشأ آلودهٔ V2 محافظت نمی‌کند.
- هم‌شکلی artifact عملیاتی با fixtureها تا اتصال تولیدکنندهٔ واقعی و آزمون یکپارچهٔ جداگانه اثبات نشده است.

## ۹. پرسش‌های باز

در دامنهٔ مجوز G14c-2 تصمیم باز تازه‌ای گرفته نشد. راه‌اندازی واقعی مسیر، کلید، زمان‌بندی و ادغام در شاخهٔ V2 نیازمند تصمیم/مجوز جداگانهٔ مالک و بازبینی Guardian است.

## ۱۰. گام بعدی پیشنهادی

Guardian فایل‌های مجاز، manifest، سه اجرای build/test و جداسازی mock را بازبینی کند. پس از تصمیم بازبینی، مالک دربارهٔ ادغام شاخهٔ V2 و سپس راه‌اندازی عملیاتی تصمیم بگیرد. Codex اینجا متوقف می‌شود.

من کدکس هستم

# V1_COMPOSITION_ROOT_DESIGN.md

**نسخه:** ۱.۰ (۶ سپتامبر ۲۰۲۶)
**نوع سند:** طراحی (Design) — **صفر کد**. هیچ Composition Root، هیچ Entry Point، و هیچ فایل اجرایی در این پاس ساخته نشد و نباید ساخته شود.
**مجوز:** `CODEX-20260906-0056-CCR-ADAPTER-REVIEW` بند ۶ — «فقط طراحی سند Composition Root».
**مسئله‌ی محرک:** Adapter واقعی AC-2 (`OrgMembershipAC2DecisionPort`) ساخته و بازبینی و تایید شد، اما **هیچ نقطه‌ای در کدبیس آن را نمی‌سازد** — `OpportunityReadService` فقط در تست‌ها نمونه‌سازی می‌شود. Adapter امروز کد مرده‌ی تاییدشده است.
**وضعیت:** پیش‌نویس — منتظر بازبینی مستقل ممد، سپس **تصمیم مالک محصول روی گزینه‌ها**. هیچ گزینه‌ای در این سند انتخاب نشده.

---

## ۰. بازخوانی واقعیت امروز (پایه‌ی این سند — نه فرض)

بررسی مستقیم کدبیس، نه حافظه:

| موضوع | واقعیت |
|---|---|
| Entry Point (`index.ts`/`main.ts`/`server.ts`/`app.ts`) | **هیچ‌کدام وجود ندارد** (جست‌وجوی مستقیم در `implementation/`) |
| `package.json` scripts | فقط `build`, `test`, `prisma:*`, `db:up/down` — **هیچ script اجرایی** |
| وابستگی‌های تولیدی | `@prisma/client`, `jsonwebtoken`, `uuid` — **هیچ فریم‌ورک HTTP نصب نیست** |
| `OpportunityReadService` | فقط در تست‌ها ساخته می‌شود |
| `eventAdmissionService` | نمونه‌ی سراسری واقعی و آماده (`export const`) |
| `resolveActorContext(bearerToken)` | واقعی و آماده — JWT را به `ActorContext` تبدیل می‌کند، fail-closed |
| Repositoryهای Value Engineها | **فقط پیاده‌سازی In-Memory** (`InMemoryCapacityRepository` و مشابه‌ها). هیچ Connector واقعی به داده‌ی Malino وجود ندارد |
| `AiSummarizer` (بریفینگ) | فقط Interface — **هیچ پیاده‌سازی واقعی**؛ ولی وابستگی اختیاری است |

### ۰.۱. یافته‌ی تعیین‌کننده: مسئله یک تکه نیست، دو تکه است

این بررسی یک تفکیک مهم را آشکار کرد که کل تصمیم‌گیری به آن وابسته است:

- **سمت خواندن (Feed / Briefing): امروز واقعاً قابل‌سیم‌کشی است.** همه‌ی قطعات واقعی‌اند — جداول Projection واقعی، `OpportunityReadService` واقعی، Adapter واقعی AC-2، و `resolveActorContext` واقعی. تنها چیز غایب، لایه‌ی ورود و نگاشت JWT است.
- **سمت تشخیص (Value Engineها): امروز قابل‌سیم‌کشی به داده‌ی واقعی نیست.** Repositoryها فقط In-Memory دارند؛ Connector واقعی Malino ساخته نشده. یک Scheduler امروز، Detectorها را روی **آرایه‌های خالی در حافظه** اجرا می‌کند — یعنی یک چرخه‌ی تشخیصی که هیچ‌وقت چیزی تشخیص نمی‌دهد.

**پیامد مستقیم:** هر گزینه‌ای که سمت تشخیص را «تولیدی» بنامد، بدون ساخت Connector، ادعای نادرستی است. این سند آن را ادعا نمی‌کند.

---

## ۱. نقطه‌ی ورود واقعی V1 کجاست؟ — گزینه‌ها

### گزینه ۱ — HTTP Server سبک (فقط سمت خواندن)

یک سرور HTTP کوچک که Feed و By-Id را از پشت JWT ارائه می‌دهد.

| مثبت | منفی |
|---|---|
| تنها گزینه‌ای که **امروز با داده‌ی واقعی کار می‌کند** (همه‌ی قطعات سمت خواندن حاضرند) | یک وابستگی جدید (فریم‌ورک HTTP) وارد پروژه می‌کند — امروز هیچ‌کدام نصب نیست |
| Adapter واقعی AC-2 را از حالت کد مرده خارج می‌کند — همان مسئله‌ی محرک این سند | بدون زیرساخت Deploy، فقط روی localhost معنا دارد |
| مرز طبیعی JWT→`ActorContext` دقیقاً همان‌جاست که `resolveActorContext` برایش طراحی شده | نیازمند تصمیم مالک درباره‌ی صادرکننده‌ی JWT و محل کلید (بند ۵) |
| کوچک و برگشت‌پذیر — یک فایل ورودی، بدون تغییر هیچ قرارداد | |

### گزینه ۲ — CLI/Scheduler برای چرخه‌های تشخیص

یک اجرای دوره‌ای که Value Engineها را صدا می‌زند و Projection را بازسازی می‌کند.

| مثبت | منفی |
|---|---|
| به هیچ فریم‌ورک جدیدی نیاز ندارد (یک script ساده) | **امروز روی داده‌ی واقعی کار نمی‌کند** — Repositoryها فقط In-Memory‌اند (بند ۰.۱) |
| برای زمانی که Connector ساخته شد، لازم خواهد بود | Adapter واقعی AC-2 را سیم‌کشی **نمی‌کند** (AC-2 در مسیر خواندن است، نه تشخیص) — یعنی مسئله‌ی محرک این سند را حل نمی‌کند |
| | نیازمند تصمیم مالک درباره‌ی بسامد اجرا (بند ۵) |

### گزینه ۳ — Worker دوره‌ای درون همان فرآیند

ترکیب Scheduler داخل یک فرآیند بلندمدت.

| مثبت | منفی |
|---|---|
| یک فرآیند به‌جای دو تا | همان محدودیت گزینه ۲ (نبود Connector) را دارد، به‌علاوه‌ی پیچیدگی مدیریت چرخه‌ی عمر |
| | زودهنگام است: بدون Deploy واقعی، «فرآیند بلندمدت» جایی برای زندگی ندارد |

### گزینه ۴ — ترکیب: HTTP الان، Scheduler بعد از Connector

سمت خواندن را حالا سیم‌کشی کن؛ سمت تشخیص را به بعد از ساخت Connector موکول کن.

| مثبت | منفی |
|---|---|
| با واقعیت بند ۰.۱ منطبق است: فقط چیزی را می‌سازد که امروز واقعاً کار می‌کند | نیازمند پذیرش صریح این است که V1 فعلاً «فقط-خواندن» است |
| مسئله‌ی محرک (Adapter کد مرده) را حل می‌کند بدون اینکه چرخه‌ی تشخیص جعلی بسازد | دو مرحله‌ی مجزا، دو مجوز مجزا |
| Connector را به‌عنوان پیش‌نیاز صریح ثبت می‌کند به‌جای پنهان‌کردنش | |

### پیشنهاد من (تصمیم با مالک محصول است، نه من)

**گزینه ۴.** دلیل: تنها گزینه‌ای است که مسئله‌ی محرک را حل می‌کند بدون اینکه چیزی بسازد که به داده‌ی واقعی وصل نیست. گزینه ۲ و ۳ امروز یک چرخه‌ی تشخیص می‌سازند که روی داده‌ی خالی می‌چرخد — کار می‌کند اما کاری نمی‌کند، و این دقیقاً همان جنس «کد به‌ظاهر تولیدی» است که این پروژه در همه‌ی پاس‌های قبلی از آن پرهیز کرده.

**اما تاکید:** این ترجیح مهندسی است. اگر اولویت مالک محصول ساخت Connector قبل از هر لایه‌ی ورودی باشد، گزینه ۴ هم باید صبر کند. آن تصمیم محصولی است، نه فنی.

---

## ۲. ترتیب قطعی اتصال قطعات (در گزینه‌ی پیشنهادی)

### ۲.۱. مسیر خواندن — قابل‌ساخت با قطعات امروز

```
[HTTP request + Authorization: Bearer <jwt>]
        │
        ▼
resolveActorContext(bearerToken)          ← FP-03، موجود و واقعی
        │   خطا → 401 (fail-closed، بدون افشای دلیل)
        ▼
ActorContext { organization_id, actor_id, role }
        │
        ▼
new OpportunityReadService(orgMembershipAC2DecisionPort)   ← Adapter واقعی، موجود
        │
        ▼
getOpportunityFeed({ actor })  /  getOpportunityById(id, actor)
        │   (داخل خودش: خواندن org-scoped → AC-2 → حذف Denyها →
        │    فیلتر intended_audience → وضعیت تعامل → گروه‌بندی)
        ▼
مصرف‌کننده‌ها:
  • OpportunityFeedService(readInterface, eventAdmissionService)   ← برای ثبت تعامل
  • ProactiveBriefingService(readInterface, aiSummarizer?)          ← aiSummarizer اختیاری
        │
        ▼
[HTTP response]
```

### ۲.۲. قطعات جدیدی که این مسیر لازم دارد (و امروز وجود ندارند)

۱. **لایه‌ی ورود HTTP** — مسیریابی، خواندن هدر `Authorization`، سریال‌سازی پاسخ.
۲. **نگاشت خطا در مرز HTTP** — `AuthenticationError` → ۴۰۱؛ `AuthorizationError` → ۴۰۳؛ خطای ناشناخته → ۵۰۰ **بدون افشای جزئیات داخلی**. نکته‌ی امنیتی: `getOpportunityById` عمداً برای «وجود ندارد» و «مجاز نیستی» هر دو `null` برمی‌گرداند (بدون Existence Oracle) — مرز HTTP باید این را حفظ کند و **هر دو را ۴۰۴ یکسان** بدهد، نه ۴۰۳ در برابر ۴۰۴.
۳. **Composition Root واقعی** — یک ماژول که این نمونه‌ها را یک‌بار می‌سازد و تزریق می‌کند (نه `new` پراکنده در Handlerها).
۴. **پیکربندی از `env`** — `DATABASE_URL` (موجود)، `MLINO_JWT_SECRET` (موجود و از قبل fail-closed)، و پورت.
۵. **خاموش‌کردن تمیز (graceful shutdown)** برای اتصال Prisma.

### ۲.۳. مسیر تشخیص — عمداً طراحی نشده در این پاس

فقط ثبت می‌شود که ترتیبش چیست، **بدون طراحی جزئیات**، چون پیش‌نیازش (Connector) وجود ندارد:

```
[Scheduler]  →  Repository واقعی (❌ وجود ندارد — فقط In-Memory)
             →  Detector (F-01/F-02/F-03)  →  eventAdmissionService (IC-13)
             →  rebuildOrganizationProjection
```

**تا زمانی که Connector واقعی ساخته نشود، این مسیر قابل‌سیم‌کشی به داده‌ی واقعی نیست.** ساخت Connector خارج از دامنه‌ی این سند است و مجوز و طراحی مستقل خودش را لازم دارد.

---

## ۳. آیا این طراحی به تغییر فایل منجمد یا CCR جدید نیاز دارد؟

**تشخیص: خیر.**

بررسی مورد‌به‌مورد:

| مورد | نیاز به تغییر قرارداد؟ |
|---|---|
| ساخت `OpportunityReadService` با Adapter واقعی | ❌ — Constructor از قبل `AC2DecisionPort` می‌گیرد؛ فقط تزریق است |
| `resolveActorContext` | ❌ — امضایش (`bearerToken → ActorContext`) دقیقاً برای همین مرز طراحی شده |
| `OpportunityFeedService` / `ProactiveBriefingService` | ❌ — هر دو Interfaceهای موجود (`IC14ReadInterface`, `IC13SubmissionInterface`) می‌گیرند |
| `shared-contracts/types.ts` | ❌ — هیچ تایپ جدیدی لازم نیست؛ Composition ترکیب است، نه قرارداد جدید |
| `prisma/schema.prisma` | ❌ — هیچ داده‌ی جدیدی ذخیره نمی‌شود |

**این یک Implementation Design خالص است.** هیچ مفهوم معماری جدیدی اختراع نمی‌شود؛ فقط قطعاتی که از قبل برای تزریق طراحی شده‌اند به هم وصل می‌شوند.

**اما مشروط:** اگر در زمان پیاده‌سازی معلوم شد چیزی واقعاً نیازمند تغییر قرارداد است (مثلاً اگر لایه‌ی HTTP فیلدی بخواهد که در DTOها نیست)، قاعده همان است: **CCR بنویس، پچ نزن.** این سند از پیش خلافش را تضمین نمی‌کند.

---

## ۴. صراحت درباره‌ی آنچه این طراحی **نیست**

- **هیچ‌چیز اینجا «تولیدی امن» نیست.** بدون زیرساخت Deploy مصوب (HTTPS، مدیریت Secret، Backup، مانیتورینگ، کنترل دسترسی شبکه)، آنچه توصیف شد یک پل است تا وقتی مالک محصول زیرساخت را تصویب کند — نه یک سامانه‌ی آماده‌ی بهره‌برداری.
- **این طراحی هیچ گپ بازی را نمی‌بندد.** R4 (BLOCKED)، R5 (OPEN عمدی)، R8-a و R8-b (OPEN) دقیقاً همان‌جا که بودند می‌مانند. سیم‌کشی، سیاست را عوض نمی‌کند.
- **Adapter واقعی AC-2 همچنان سیاست V1 است، نه Kernel-AC-2 کامل** — نیمه‌ی Consent وجود ندارد (R8-a). سیم‌کشی‌اش این را تغییر نمی‌دهد و نباید طوری معرفی شود که انگار می‌دهد.
- **«عضویت» همچنان یعنی اعتبار Token.** جدول عضویت مستقل وجود ندارد؛ لغو دسترسی امروز یعنی باطل‌کردن Token. ثبت‌شده در `V1_MINIMUM_AC2_ACCESS_POLICY.md` §۱ و R8-a.
- **چرخه‌ی تشخیص تولیدی نمی‌شود** (بند ۲.۳).

---

## ۵. سوالات باز محصولی — باید **قبل از** هر کدی جواب بگیرند

این سند هیچ‌کدام را جواب نمی‌دهد. تصمیم با مالک محصول:

۱. **کدام گزینه از بند ۱؟** (پیشنهاد من گزینه ۴ است، اما تصمیم با توست.)
۲. **صادرکننده‌ی JWT کیست؟** امروز فقط `issueTokenForTesting` وجود دارد — یعنی هیچ صادرکننده‌ی واقعی توکن نداریم. آیا Malino موجود توکن صادر می‌کند و ما فقط تایید می‌کنیم؟ یا V1 باید خودش ورود/صدور توکن داشته باشد؟ (این دومی یک دامنه‌ی کاملاً جدید است.)
۳. **`MLINO_JWT_SECRET` در محیط واقعی کجا نگهداری می‌شود؟** امروز fail-closed است و اجرا نمی‌شود مگر تنظیم شود — که درست است، اما محل نگهداری تصمیم نشده.
۴. **بسامد چرخه‌ی تشخیص چند بار در روز؟** (فقط وقتی معنا پیدا می‌کند که Connector وجود داشته باشد.)
۵. **آیا ساخت Connector واقعی Malino اولویت بالاتری از لایه‌ی ورودی دارد؟** اگر بله، حتی گزینه ۴ هم باید صبر کند.
۶. **زیرساخت Deploy** (کجا، HTTPS، مدیریت Secret) — بدون این، هر لایه‌ی ورودی فقط محلی است.

---

## ۶. جمع‌بندی برای مالک محصول

مسئله ساده است: **یک Adapter امنیتی تاییدشده داریم که هیچ‌کس صدایش نمی‌زند.** سمت خواندن آماده‌ی وصل‌شدن است؛ سمت تشخیص نیست، چون Connector ندارد.

تصمیمی که از تو لازم است: **کدام گزینه، و آیا Connector اولویت بالاتری دارد.** تا آن تصمیم، هیچ کدی نوشته نمی‌شود.

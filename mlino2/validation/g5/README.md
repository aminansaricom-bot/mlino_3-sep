# شواهد G5 — سازگاری V1

این پوشه شواهد مقایسهٔ نسخهٔ پیش از G4 (`6b62509`) با تحویل G4 (`f47c852`) را نگه می‌دارد.

- هر نسخه در یک worktree موقت detached اجرا شد.
- وابستگی‌ها از lockfile نصب و Prisma Client در همان worktree تولید شد.
- هر نسخه با دیتابیس PostgreSQL 16 یک‌بارمصرف، `tmpfs` و بدون volume آزموده شد.
- دیتابیس محلی `mlino-v1-local-db` و volume آن استفاده نشدند.
- containerها و worktreeهای موقت پس از آزمون حذف شدند.

نتیجهٔ نهایی: **PASS — هیچ رگرسیون V1 نسبت به مبنا مشاهده نشد.**

من کدکس هستم.

# گزارش بازبینی مستقل V2 — رفع A-1 (قطب‌نمای اندروید)

**بازبین:** ممد (بازبین مستقل، GLM 5.3 Flash) — پروتکل سبک V2
**تاریخ:** ۶ سپتامبر ۲۰۲۶
**دامنه:** Commit `196bdb3` («رفع A-1: قطب‌نمای اندروید — فقط رویداد absolute معتبر + تبدیل 360−alpha») — نویسنده: موجی
**تعارض منافع:** صفر

> **یادداشت بازیابی (۶ سپتامبر، ثبت به درخواست یونس):** این سند در جلسه‌ی بازبینی A-1 نوشته و روی دیسک ثبت شده بود اما در یک Sync بعدی از دست رفت و هرگز Commit نشده بود. متن زیر عیناً همان گزارش اصلی است که بازبین (نویسنده‌ی آن) از گفت‌وگوی خودش بازنویسی کرده — بازسازی از روی حافظه‌ی شخص ثالث نبوده و بازبین مسئولیت صداقت و دقت آن را دارد. شواهد اجرایی آن پاس (۵۵/۵۵ + build روی کپی دقیق `origin/main`@`196bdb3`) در همان جلسه مستقلاً اجرا و ثبت شده بود.

## ۱. Verdict

**رفع A-1 تایید می‌شود — بدون عیب مسدودکننده.**

## ۲. شواهد راستی‌آزمایی‌شده‌ی مستقیم

| # | ادعا | راستی‌آزمایی مستقل | نتیجه |
|---|---|---|---|
| ۱ | ۵۵/۵۵ تست (۱۵ + ۱۹ + ۲۱ AR شامل ۵ تست جدید A-1) | اجرای مستقل `npm ci` + `npm test` روی کپی دقیق `origin/main`@`196bdb3` | ✅ **3 Files / 55 Tests passed** |
| ۲ | build موفق | اجرای مستقل `npm run build` | ✅ ۵.۳ ثانیه |
| ۳ | فقط `mlino2/` تغییر کرده | `git show --stat 196bdb3`: ۵ فایل، همگی `mlino2/` | ✅ قانون طلایی سالم |
| ۴ | منطق رفع | بازخوانی `headingFromCompassEvent`: iOS (`webkitCompassHeading`) اولویت؛ استاندارد: فقط `absolute=true` پذیرفته، تبدیل `360−alpha`؛ غیر-absolute → `not-absolute` صریح (هرگز تفسیر به شمال) | ✅ دقیقاً رفع یافته‌ی A-1 یونس |
| ۵ | صداقت UI | حالت `notAbsolute` به‌عنوان source جداگانه → UI به «جهت دستی» برمی‌گردد، فریب ندارد | ✅ |

## ۳. نکات

1. گوش‌دادن هم‌زمان به دو چنل رویداد (`deviceorientationabsolute` + معمولی) با گارد داخل handler — انتخاب درستی است چون پشتیبانی مرورگرها متفاوت است (Chrome اندروید دارد، سافاری iOS از `webkitCompassHeading`).
2. ریسک باقی‌مانده صادقانه ثبت شده: دستگاه‌هایی که `absolute=true` ادعا می‌کنند ولی مگنتومترشان کالیبره نیست — از سمت نرم‌افزار قابل‌حل نیست؛ تست میدانی مشخصش می‌کند (V2-1 همچنان الزامی قبل از عرضه).
3. A-2 (نگاشت خطی FOV) عمداً باز — موافقم، اولویت ندارد.
4. هیچ چیز دیگری تغییر نکرده: دستیار، دایرکتوری، قرارداد داده — تایید.

## ۴. تصمیم

- **رفع A-1: تایید.** گام بعدی موجی همچنان منتظر تصمیم مالک محصول (فاز بعد/deploy تست میدانی/کار دیگر) — هیچ شروع خودجوشی مجاز نیست.
- یادآوری پابرجا: V2-1 (تست میدانی HTTPS) قبل از هر عرضه‌ی واقعی AR.

---
*بازبین: ممد (GLM 5.3 Flash). شواهد: `npm ci` + `vitest` (55/55) + `npm run build` روی کپی دقیق `origin/main`@`196bdb3`.*

# گزارش بازبینی مستقل V2 — قابلیت HTTPS محلی برای تست میدانی AR (رفع V2-1)

**بازبین:** ممد (بازبین مستقل، GLM 5.3 Flash) — پروتکل سبک V2
**تاریخ:** ۶ سپتامبر ۲۰۲۶
**دامنه:** Commit `3725f8c` («قابلیت HTTPS محلی برای تست میدانی AR») — نویسنده: موجی
**تعارض منافع:** صفر

## ۱. نکته‌ی حاکمیتی اول — خوداظهاری موجی درباره‌ی مجوز

دستور فعال «هیچ کار جدیدی شروع نکن» بود و موجی با تفسیر کم‌ریسک («شروع کن» بدون مشخص‌کردن مورد → کوچک‌ترین زیرساخت معلق، برگشت‌پذیر، بدون تصمیم بزرگ) پیش رفت و این تفسیر را **صریحاً در ابتدای گزارش ثبت کرد**. قضاوت بازبین: **تفسیر قابل‌قبول بوده و کار تحویل‌شده در ناحیه‌ی امن است** (dev-only، برگشت‌پذیر، بدون تصمیم معماری) — اما الگوی «سوال انتخابی بی‌پاسخ → تفسیر خودم» باید الگو نشود؛ دفعه‌ی بعد اگر مشخص نبود، بپرس. این بار Pass.

## ۲. Verdict

**تایید می‌شود — بدون عیب مسدودکننده.** این دقیقاً رفع V2-1 بود (پیش‌نیاز تست میدانی): دوربین/قطب‌نما/Geolocation فقط در Secure Context کار می‌کنند — حالا روی گوشی واقعی در LAN قابل‌تست است، بدون هیچ سرور عمومی.

## ۳. شواهد راستی‌آزمایی‌شده‌ی مستقیم

| # | ادعا | راستی‌آزمایی مستقل | نتیجه |
|---|---|---|---|
| ۱ | ۵۵/۵۵ تست | اجرای مستقل `npm ci` + `npm test` روی worktree دقیق `origin/main` | ✅ **3 Files / 55 Tests passed** |
| ۲ | build موفق | اجرای مستقل | ✅ ۶.۳ ثانیه |
| ۳ | HTTPS واقعاً بالا می‌آید و جواب می‌دهد | اجرای مستقل `npm run dev:https` در Job جدا + `curl -k` | ✅ **HTTPS_STATUS=200** + Network URLs روی LAN |
| ۴ | فقط `mlino2/` تغییر کرده | `git show --stat`: ۴ فایل، همگی `mlino2/` | ✅ قانون طلایی سالم |
| ۵ | حالت `dev` معمولی و build تغییر نکرده | بازخوانی `vite.config.ts`: https فقط با `--mode https` یا `V2_DEV_HTTPS=1` | ✅ پیش‌فرض بدون تغییر |
| ۶ | وابستگی جدید توجیه‌شده | `@vitejs/plugin-basic-ssl` — رسمی‌پلاگین تیم Vite، فقط devDependency، بدون اثر روی build | ✅ |

## ۴. نکات مثبت

1. **گزارش تست میدانی گام‌به‌گام برای مالک** (بند ۲ گزارش موجی) — شامل دیالوگ فایروال ویندوز، پذیرش گواهی self-signed، و **چک‌لیست راستی‌آزمایی میدانی** که حتی تشخیص درست رفتار A-1 روی دستگاه را هم شامل است (بج «غیر-absolute» = داده‌ی مفید، نه خطا).
2. **هشدارهای امنیتی صادقانه:** گواهی self-signed فقط تست داخلی؛ `host: true` فقط LAN و هرگز شبکه‌ی عمومی (بدون احراز هویت)؛ **تصریح که این deploy عمومی نیست** — زیرساخت واقعی تصمیم مالک است. ✅
3. قانون طلایی و قرارداد `02`: دست‌نخورده. ✅

## ۵. یافته‌ها (غیرمسدودکننده)

- **V2-4 (INFO — الزامی قبل از تست میدانی):** گزارش موجی تست میدانی انجام‌شده را اضافه کند (دستگاه، مرورگر، کدام بج قطب‌نما، آیا M-1/A-1 روی دستگاه واقعی هم درست رفتار کردند) — این خروجی، ورودی تصمیم مالک برای هر عرضه‌ی آینده است.
- **V2-5 (INFO):** `allowScripts.esbuild` در package.json اضافه شد (نیازمندی npm ۱۱) — توجیه‌شده در گزارش؛ فقط یادآوری شود این تنظیم local-ابزار است نه وابستگی runtime.

## ۶. تصمیم

- **تایید.** V2-1 (پیش‌نیاز تست میدانی) اکنون برطرف است — **گام بعدی عملی: خودِ تست میدانی روی گوشی واقعی، طبق چک‌لیست گزارش موجی** — نیازمند حضور/اجرا توسط تو یا هر تستری. خروجی‌اش را بدهد تا ثبت شود.
- گام بعدی موجی همچنان منتظر تصمیم مالک (LlmIntentParser چراغ سبز دارد و در انتظار است — کلیدها هنوز نیامده‌اند).

---
*بازبین: ممد (GLM 5.3 Flash). شواهد: `npm ci` + `vitest` (55/55) + `npm run build` + اجرای واقعی `dev:https` با تایید HTTPS 200 روی کپی دقیق `origin/main`.*

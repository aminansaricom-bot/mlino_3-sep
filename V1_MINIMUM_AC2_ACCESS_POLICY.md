# V1_MINIMUM_AC2_ACCESS_POLICY.md

**نوع سند:** طراحی (Design) — **نه** پیاده‌سازی. هیچ کد Adapter در این پاس نوشته نشده و نباید نوشته شود.
**دامنه:** فقط خواندن Opportunity‌های V1 از طریق مسیر موجود IC-14 (`OpportunityReadService`/`getOpportunityFeed`/`getOpportunityById`). طراحی کامل Governance عمومی/چندسازمانی/آینده **خارج از این سند** است.
**مجوز:** تصمیم مستقیم مالک محصول — «گزینه‌ی چهارم: سیاست حداقلی و مرحله‌ای AC-2 برای V1» — `APPROVED DIRECTION: MINIMUM V1 AC-2 POLICY DESIGN BEFORE IMPLEMENTATION`.
**وضعیت:** پیش‌نویس، منتظر بازبینی مستقل + مجوز صریح برای شروع کدنویسی Adapter.

---

## ۰. زمینه‌ی فنی موجود (برای اینکه این سند روی واقعیت کد بنا شود، نه فرض)

قبل از نوشتن سیاست، کد و قرارداد فعلی را بازخوانی کردم تا مشخص شود چه داده‌ای *واقعاً* امروز در دسترس است:

- **ترتیب پردازش فعلی (`opportunity-read.service.ts`، مطابق IC-14 §۵):** (۱) `resolveActorContext` بیرون از این کلاس انجام می‌شود (۲) خواندن Candidate با فیلتر `organizationId: actor.organization_id` **از همین الان در سطح Query** (۳) `evaluateAC2FailClosed` (۴) حذف موارد Deny (۵) فیلتر `intended_audience` (۶) وضعیت تعامل actor (۷) گروه‌بندی/مرتب‌سازی.
  → **یافته‌ی مهم:** تفکیک بین‌سازمانی (Tenant Isolation) از قبل و مستقل از AC-2، در سطح Query انجام می‌شود. یعنی AC-2 در شکل فعلی‌اش برای «رد دسترسی بین‌سازمانی» عملاً افزونه (Redundant) است — ارزش واقعی AC-2 دقیقاً همان لایه‌ی «درون همان سازمان، کدام Subject/Evidence قابل‌نمایش است» است که این سند به آن می‌پردازد.
- **`OpportunityAccessCandidate`** (`ac2-decision-port.ts`): فقط `organization_id`، `opportunity_correlation_id`، `subject_core_entity_refs`، `evidence_refs`. **هیچ فیلد مالکیت یا رضایتی در این شکل وجود ندارد.**
- **`EvidenceRef`** (`shared-contracts/types.ts`, منجمد): فقط `{ event_id, description? }`. **هیچ فیلد نوع مالکیت روی خودِ Evidence وجود ندارد.** خودِ کامنت قرارداد صریح می‌گوید R4 (یکپارچگی شواهد) هنوز OPEN است و evidence_refs کاملاً Opaque‌اند.
- **`CoreEntity` (Prisma, منجمد):** فقط `id`، `organizationId`، `entityType` (فعلاً همیشه `'Individual'`)، `domainTag`. **هیچ فیلد مالکیت (سازمانی/فردی/جمعی) وجود ندارد** — و نکته‌ی مهم: تنها مقدار `entityType` که تا امروز استفاده شده `'Individual'` است، یعنی Subject هر Opportunity در V1 امروز همیشه یک شخص واقعی (مثلاً مشتری/بیمار) است، نه یک موجودیت انتزاعی سازمانی.
- **`OpportunityCurrentState` / `EventLog` (Prisma, منجمد):** هیچ فیلد مالکیت یا اشاره‌ی Consent ندارند.
- **Actor Authentication (`auth-adapter.ts`):** `ActorContext` فقط از JWT (`organization_id`, `actor_id`, `role`) می‌آید؛ **هیچ جدول/رویداد «عضویت فعال» جدا از اعتبار خودِ Token وجود ندارد** — یعنی «عضویت فعال Actor در سازمان» امروز معادل «Token معتبر و منقضی‌نشده» است، نه یک وضعیت جدا-بررسی‌شدنی.
- **Consent:** هیچ رویداد `ConsentGranted/Renewed/Revoked/Expired`، هیچ جدول، هیچ UI، در هیچ‌جای کد وجود ندارد.

**نتیجه‌ی این بخش:** مفهوم «مالکیت» و «رضایت» که سیاست حداقلی زیر به آن‌ها ارجاع می‌دهد، امروز **در هیچ داده‌ی ذخیره‌شده‌ای وجود ندارد**. این یک محدودیت اساسی است که در بخش‌های بعدی صریح مدیریت می‌شود.

---

## ۱. ورودی‌های تصمیم

| ورودی | امروز در دسترس است؟ | منبع فعلی / وضعیت |
|---|---|---|
| `actor_id` | ✅ بله | JWT claim (`auth-adapter.ts`) |
| وضعیت Authentication | ✅ بله | اعتبار/انقضای JWT (fail-closed از قبل پیاده‌سازی‌شده) |
| عضویت فعال Actor در سازمان | ⚠️ فقط ضمنی | معادل «Token معتبر برای این org است» — هیچ جدول عضویت جدا وجود ندارد. **GAP اگر عضویت نیاز به لغو/تعلیق مستقل از انقضای Token داشته باشد.** |
| شناسه‌ی سازمان Actor | ✅ بله | `ActorContext.organization_id` |
| شناسه‌ی سازمان Opportunity | ✅ بله | `OpportunityAccessCandidate.organization_id` |
| وضعیت و نوع مالکیت Opportunity | ❌ خیر | **هیچ فیلدی برای این وجود ندارد — GAP اصلی این سند** |
| `subject_core_entity_refs` | ✅ بله | از founding Event (`access-candidate.ts`) |
| `evidence_refs` | ✅ بله (Opaque) | از `OpportunityCurrentState.evidenceRefs` |
| نوع مالکیت هر Evidence | ❌ خیر | **وجود ندارد** — R4 هنوز OPEN، `EvidenceRef` هیچ چنین فیلدی ندارد |
| وضعیت Consent مرتبط (در صورت الزامی‌بودن) | ❌ خیر | هیچ زیرساختی وجود ندارد |
| زمان تصمیم / `as_of_time` | ✅ بله (قابل‌ساخت) | `Date.now()` وقت فراخوانی، یا زمان صریح تزریق‌شده (مثل الگوی `SituationLookupService`) |
| نسخه‌ی Policy | ❌ خیر (باید اضافه شود) | باید در خروجی Adapter/گزارش قید شود؛ امروز چیزی معادل آن وجود ندارد |

**قاعده‌ی این جدول (طبق دستور):** هر ورودی که ردیفش ❌ یا ⚠️ باشد، در تصمیم واقعی باید به `DENY` یا `INSUFFICIENT_POLICY_DATA` منجر شود — هرگز به Allow ضمنی.

---

## ۲. سیاست حداقلی پیشنهادی (رسمی‌سازی ۱۰ قاعده‌ی دستور)

1. Actor ناشناخته، Authentication نامعتبر/منقضی، یا بدون عضویت معتبر در سازمان مقصد → `DENY`.
2. `actor.organization_id !== candidate.organization_id` → `DENY`.
3. نوع مالکیت Opportunity نامشخص، یا نوع مالکیت هر Evidence نامشخص → `DENY` برای آن آیتم (نه فرض «سازمانی است پس مجاز است»).
4. اگر (و فقط اگر) نوع مالکیت Opportunity صریحاً «سازمانی» ثبت شده باشد و همان سازمان actor باشد → از AC-2 عبور می‌کند.
5. بعد از AC-2، فیلتر `intended_audience` طبق IC-14 §۵ اجرا می‌شود (بدون تغییر در ترتیب موجود).
6. داده‌ی فردی/مشتری → به‌صورت پیش‌فرض `DENY`، مگر یک وضعیت مجاز صریح و از‌پیش‌تعریف‌شده در Policy وجود داشته باشد (نه فرض ضمنی از عضویت سازمانی — بخش ۴).
7. Consent مفقود، منقضی، لغوشده، یا مربوط به Data Type/Consumer دیگر → `DENY`.
8. داده‌ی جمعی/تجمیعی فقط اگر واقعاً بالای آستانه‌ی چندسازمانی و غیرقابل‌شناسایی باشد قابل‌بررسی است؛ در غیر این صورت `DENY`. (نکته: در V1 امروز هیچ داده‌ی «جمعی» تولید نمی‌شود — این قاعده صرفاً برای تکمیل ماتریس آماده نگه داشته می‌شود، عملاً فعلاً بلا‌اثر است.)
9. هر خطا، ابهام، Timeout یا Exception در هر مرحله → `DENY` (این بخش از قبل در `evaluateAC2FailClosed` پیاده و هاردنینگ‌شده است — بدون تغییر).
10. هیچ `evidence_ref` خارج از `authorized_evidence_refs` به خروجی راه پیدا نکند (این هم از قبل در `evaluateAC2FailClosed` پیاده‌شده — فیلتر تزریق Evidence، بدون تغییر).

**یافته‌ی صریح از اعمال این ماتریس روی داده‌ی واقعی امروز:** طبق بخش ۰، هیچ Opportunity/Evidence‌ای در V1 امروز نوع مالکیت مشخصی ندارد. یعنی طبق قاعده‌ی ۳، **همه‌چیز فعلاً `DENY` می‌شود** تا زمانی که نوع مالکیت به‌صورت صریح در جایی ثبت شود. این نتیجه‌ی منطقی همان محدودیتی است که بخش ۴ به آن می‌پردازد — این سند این را پنهان نمی‌کند.

---

## ۳. تفکیک Opportunity از Evidence — دو سطح بررسی

تصمیم باید در دو سطح مجزا گرفته شود:

**سطح ۱ — آیا خودِ Opportunity برای Actor قابل‌مشاهده است؟** (قواعد ۱ تا ۸ بالا، در سطح کل Candidate)

**سطح ۲ — کدام `evidence_refs` داخل آن قابل‌مشاهده‌اند؟** (زیرمجموعه‌ای از Evidence که هرکدام مالکیت/رضایت خودشان را جدا دارند — یک Opportunity ممکن است سطح‌۱ آن Allow شود اما فقط برخی Evidenceهایش مجاز باشند.)

**یافته‌ی حیاتی (دقیقاً طبق نگرانی دستور):** اگر Evidence غیرمجاز فقط از `evidence_refs` حذف شود اما فیلدهای دیگر Opportunity — بخصوص `materiality_basis` (که در داده‌ی امروز متن آزاد است، مثلاً می‌تواند حاوی توضیح بالینی/شخصی باشد) و `subject_core_entity_refs` — بدون فیلتر بمانند، این خودش یک نشت اطلاعات حساس است که مسیر «حذف Evidence» آن را نمی‌پوشاند.

**تصمیم طراحی این سند:** تا زمانی که یک نمای Redacted رسمی در قرارداد تعریف نشود، **کل Candidate باید `DENY` شود**، نه اینکه فقط Evidence حذف و بقیه‌ی فیلدها عبور کنند. Redaction خاموش/موردی در خودِ Adapter ممنوع است.

**GAP ثبت‌شده:** «نمای Redacted رسمی برای Opportunity» در هیچ سند/کد فعلی تعریف نشده — نه در Shared Contract، نه در IC-14. این یک CCR بالقوه‌ی جدا برای آینده است، نه بخشی از این پاس.

---

## ۴. رضایت و مجوز استفاده‌ی داخلی — سوال باز اصلی (نیازمند پاسخ رسمی مالک محصول، نه فرض توسعه‌دهنده)

طبق دستور صریح، این بخش تصمیم گرفته **نشده** — فقط سوالات دقیق طرح می‌شوند تا پاسخ رسمی از مالک محصول/حقوقی گرفته شود:

1. **Consumer در مدل `Data Type × Consumer` دقیقاً چیست؟** گزینه‌های ممکن: خودِ سازمان (کلینیک) به‌عنوان یک واحد؟ نقش خاص (مثلاً فقط `owner_manager`)؟ هر Actor به‌صورت مجزا؟ یا خودِ MLINO (برای مصارف تجمیعی)؟ — این سند هیچ‌کدام را انتخاب نمی‌کند.
2. **آیا استفاده‌ی داخلی کلینیک از داده‌ی بیمار (مثلاً نمایش یک Opportunity لغو-نوبت به منشی) نیازمند رضایت صریح بیمار است، یا در چارچوب رابطه‌ی درمانی/قرارداد سرویس از قبل مجاز فرض می‌شود؟** این می‌تواند به قوانین محلی (حریم خصوصی سلامت) هم مرتبط باشد — تصمیم/تایید حقوقی ممکن است لازم باشد.
3. **آیا مجوز معتبر دیگری از سیستم منبع (مثلاً فرم رضایت کاغذی/دیجیتال هنگام ثبت‌نام در همان کلینیک، خارج از MLINO) از قبل وجود دارد که بتوان به آن استناد کرد؟**
4. **اگر چنین مجوزی وجود دارد، MLINO چطور می‌تواند آن را به‌صورت قابل‌اثبات (Event-Sourced، طبق Kernel §۱۱) دریافت/ثبت کند؟** — این خودش یک یکپارچه‌سازی جدید (احتمالاً با سیستم موجود کلینیک) است، نه چیزی که AC-2 به‌تنهایی حل کند.

**تا پاسخ رسمی این بخش:** طبق قاعده‌ی ۶ بالا، هر داده‌ای که Subject آن یک `CoreEntity` با `entityType: 'Individual'` باشد (که طبق بخش ۰، امروز **تنها نوع موجود** است) بدون مبنای ثبت‌شده باید Fail-closed شود. عملاً یعنی تا این پاسخ داده نشود، سیاست حداقلی به‌تنهایی برای V1 امروز چیزی Allow نمی‌کند — این پیامد صادقانه در بخش ۲ هم ثبت شد.

---

## ۵. وضعیت کامل Consent — GAP آینده (بدون ساخت در این پاس)

موارد زیر عمداً **خارج از این پاس** می‌مانند، فقط Seam/قرارداد لازم حفظ می‌شود (مثلاً یک فیلد اختیاری/Interface که بعداً پر می‌شود، نه پیاده‌سازی کامل):

- تولید واقعی رویدادهای `ConsentGranted` / `ConsentRenewed` / `ConsentRevoked` / `ConsentExpired`.
- UI دریافت/مدیریت رضایت.
- Projection وضعیت رضایت (مشابه الگوی `opportunity_current_state`).
- تعریف رسمی و کامل تمام Data Typeها و Consumerها.
- فرآیند لغو و انقضای رضایت.

---

## ۶. برنامه‌ی تست الزامی (برای پاس پیاده‌سازی آینده، نه این پاس)

قبل از تایید هر Adapter واقعی، حداقل این سناریوها باید پوشش داده شوند:

1. Actor ناشناخته (بدون Token / Token نامعتبر)
2. عضویت غیرفعال (یا معادل امروز: Token منقضی/باطل)
3. نقش نامعتبر/ناشناخته
4. دسترسی بین دو سازمان (باید Deny شود، حتی با شناسه‌ی درست حدس‌زده‌شده)
5. Opportunity با مالکیت نامشخص → Deny
6. Evidence با مالکیت نامشخص → آن Evidence حذف یا کل Candidate Deny (طبق تصمیم بخش ۳)
7. داده‌ی سازمانی واقعی همان سازمان (وقتی مالکیت صریح ثبت شده) → Allow
8. داده‌ی فردی بدون Consent → Deny
9. Consent لغوشده → Deny
10. Consent منقضی‌شده → Deny
11. Consent برای Data Type اشتباه → Deny
12. Consent برای Consumer اشتباه → Deny
13. تزریق Evidence غیرمجاز در پاسخ Allow (این تست از قبل روی `evaluateAC2FailClosed` وجود دارد — باید روی Adapter واقعی هم تکرار شود)
14. خطا/Timeout در Policy Provider → Deply (fail-closed، از قبل پیاده‌شده در سطح Wrapper)
15. دست‌کاری/حدس‌زدن شناسه‌ی Opportunity توسط Actor غیرمرتبط
16. حفظ ترتیب اجباری AC-2 قبل از `intended_audience` (تست موجود `opportunity-read.spec.ts` را می‌توان پایه قرار داد)
17. عدم نشت اطلاعات از فیلدهای غیر از `evidence_refs` (مثلاً `materiality_basis`) وقتی Candidate باید Redact/Deny شود

تمام حالت‌های نامشخص/تعریف‌نشده باید Deny شوند — دقیقاً هم‌راستا با فلسفه‌ی fail-closed موجود در `evaluateAC2FailClosed`.

---

## ۷. خروجی‌های این مرحله‌ی طراحی (طبق درخواست دستور)

1. **Policy Matrix:** بخش ۲ (۱۰ قاعده) + بخش ۳ (تفکیک Opportunity/Evidence).
2. **تعریف Actor / Resource / Ownership / Consumer:**
   - **Actor** = `ActorContext` موجود (`organization_id`, `actor_id`, `role`) — تعریف‌شده، بدون تغییر.
   - **Resource** = یک `OpportunityAccessCandidate` (سطح Opportunity) به‌همراه هر عضو `evidence_refs` آن (سطح Evidence) — تعریف‌شده در این سند (بخش ۳).
   - **Ownership** = یکی از سه دسته‌ی Kernel §۱۰ (سازمانی/فردی/جمعی) — **دسته‌ها تعریف‌شده‌اند، اما نگاشت مشخص «کدام داده‌ی واقعی V1 در کدام دسته می‌افتد» تعریف‌نشده — GAP.**
   - **Consumer** = **تعریف‌نشده — سوال باز بخش ۴، منتظر پاسخ مالک محصول.**
3. **داده‌های لازم برای تصمیم:** جدول بخش ۱.
4. **داده‌هایی که اکنون در سیستم موجود نیستند:** نوع مالکیت Opportunity، نوع مالکیت Evidence، هر زیرساخت Consent، جدول عضویت مستقل از Token، نسخه‌ی Policy، نمای Redacted رسمی.
5. **اثر بر قراردادهای منجمد:** برای پیاده‌سازی کامل این سیاست، حداقل به فیلدهای جدید روی `EventLog`/`OpportunityCurrentState` (نوع مالکیت) و به‌طور بالقوه `EvidenceRef` (نوع مالکیت Evidence) در `shared-contracts/types.ts` نیاز است — **هر دو فایل منجمدند.** این یعنی پیاده‌سازی کامل بدون یک یا چند CCR رسمی ممکن نیست.
6. **تشخیص نوع کار:** این سند خودش «Implementation Design» است (فقط نحوه‌ی استفاده از قرارداد موجود را مشخص می‌کند، چیزی را تغییر نمی‌دهد). اما **گام بعدی (افزودن فیلد مالکیت/Consent) یک Implementation Design ساده نیست — نیازمند CCR رسمی (شبیه الگوی CCR «Evidence Namespace v1.2» قبلی) است**، چون به `prisma/schema.prisma` و `shared-contracts/types.ts` دست می‌زند. تشخیص من: با توجه به اینکه Kernel §۱۰/§۱۱ از قبل این مفاهیم را به‌رسمیت شناخته (این فیلدها معماری جدید اختراع نمی‌کنند، فقط مکانیزم از‌پیش‌مجاز Kernel را عملیاتی می‌کنند)، این محتمل‌ترین است که در سطح **CCR** حل شود، نه ACR کامل — اما تایید نهایی این تشخیص با بازبین مستقل/مالک محصول است.
7. **مدل Fail-closed:** بدون تغییر نسبت به `evaluateAC2FailClosed` موجود — هر ورودی ناقص/مبهم/خطا → Deny. این سند فقط یک لایه‌ی سیاست جدید *روی* همان مدل fail-closed تعریف می‌کند، جایگزینش نمی‌کند.
8. **برنامه‌ی تست:** بخش ۶.
9. **فهرست GAPهای تعمدی:**
   - نگاشت واقعی «کدام داده‌ی V1 در کدام دسته‌ی مالکیت می‌افتد» — تعریف‌نشده.
   - Consumer در مدل Consent — تعریف‌نشده، منتظر پاسخ مالک محصول/حقوقی (بخش ۴).
   - زیرساخت کامل Consent (بخش ۵) — عمداً به‌طور کامل به آینده موکول شد.
   - نمای Redacted رسمی برای Opportunity — تعریف‌نشده (بخش ۳).
   - جدول عضویت مستقل از اعتبار Token — تعریف‌نشده.
10. **پیشنهاد Scope دقیق پیاده‌سازی (برای پاس بعدی، منوط به تایید):**
    - **قدم اول واقع‌بینانه:** پاسخ رسمی به سوالات بخش ۴ (Consumer + آیا استفاده‌ی داخلی نیاز به Consent صریح دارد).
    - **قدم دوم:** یک CCR مشخص برای افزودن حداقلی «نوع مالکیت» به `EventLog`/`OpportunityCurrentState` (نه کل مدل Consent، فقط همین یک فیلد، دقیقاً به اندازه‌ی قاعده‌ی ۴ همین سند).
    - **قدم سوم (فقط اگر بخش ۴ آن را ایجاب کند):** طراحی حداقلیِ خودِ Seam رضایت (نه کل سیستم) — یک فیلد/Event ساده که بعداً قابل‌گسترش باشد.
    - **قدم چهارم:** پیاده‌سازی واقعی `AC2DecisionPort` بر پایه‌ی همین سه قدم + برنامه‌ی تست بخش ۶.
    - تا تکمیل این قدم‌ها: **Mock همیشه‌مجاز فقط در Test/Development باقی می‌ماند؛ استفاده از آن در Production صریحاً ممنوع است.**

---

## ۸. قیدهای صریح این پاس (بدون استثنا)

- هیچ کد Adapter واقعی نوشته نشد و نباید نوشته شود تا این سند بازبینی و مجوز کدنویسی صادر شود.
- هیچ Consent جعلی یا ضمنی برای بازکردن دسترسی در این سند پیشنهاد نشده.
- هیچ تغییری در Kernel، Shared Contract، `AC2DecisionPort`، یا هر فایل کد دیگری در این پاس انجام نشد — این یک سند طراحی خالص است.

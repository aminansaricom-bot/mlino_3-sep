# دفتر تصمیم‌های باز

**تاریخ به‌روزرسانی:** ۱۰ سپتامبر ۲۰۲۶

هر ردیف یا **باز** است (منتظر تصمیم مالک) یا **بسته** (تصمیم گرفته شده و مرجعش ثبت است). چیزی بی‌صدا حل نمی‌شود.

---

## تصمیم‌های بسته

| # | موضوع | تصمیم | تاریخ |
|---|---|---|---|
| D-01 | پروتکل حاکمیت | AI_HANDOFF حفظ می‌شود؛ MLINO BOOK **زیر** آن است، نه جایگزینش | ۹ سپتامبر ۲۰۲۶ |
| D-02 | ستون فقرات | V1 = هویت، سازمان، حاکمیت و Business OS. Content Studio = ماژول | ۹ سپتامبر ۲۰۲۶ |
| D-03 | مخازن | جدا می‌مانند + قرارداد صریح و نسخه‌دار. معماری به چیدمان مخزن وابسته نیست | ۹ سپتامبر ۲۰۲۶ |
| D-04 | هویت متعارف | `organization` در V1. `workspace` از راه نگاشت صریح وصل می‌شود — نه بر اساس نام | ۹ سپتامبر ۲۰۲۶ |
| D-05 | تکرار موتور اقدام | هیچ‌کدام حذف نمی‌شود. اول قرارداد مشترک، بعد Adapter | ۹ سپتامبر ۲۰۲۶ |
| D-06 | MLINO BOOK | لایه‌ی Product Memory بالای اسناد موجود؛ سند تحت چک‌سام کپی نمی‌شود | ۹ سپتامبر ۲۰۲۶ |
| D-07 | صدای مشتری | پیاده‌سازی DEFER تا تصویب سیاست R8-a؛ معماری طراحی می‌شود | ۹ سپتامبر ۲۰۲۶ |
| D-08 | AC-2 | سیاست حداقلی مصوب؛ Consumer = سازمان؛ رضایت فقط وقتی داده از سازمان خارج شود | پیش‌تر |
| D-09 | **ترتیب اجرا** | قرارداد → نگاشت → دیتابیس → API → UI. **دیتابیس پیش از قراردادها طراحی نمی‌شود** | ۹ سپتامبر ۲۰۲۶ |
| D-10 | سلسله‌مراتب هویت | `MLINO Business ID` بالای سه نمایندگی؛ Content Studio و V2 Business Twin مالک هویت نیستند | ۹ سپتامبر ۲۰۲۶ |
| D-11 | نگاشت | **ممنوعیت افزودن `organization_id` به دیتابیس Content Studio**؛ نگاشت یک‌طرفه در V1 | ۹ سپتامبر ۲۰۲۶ |
| D-12 | موتور اقدام روزانه | یکی از هسته‌های V1، **نه یک داشبورد**؛ هر اقدام پنج الزام دارد | ۹ سپتامبر ۲۰۲۶ |
| D-13 | Content Studio | اولین ماژول عملیاتی MLINO V1؛ **MLINO V1 فقط Content Studio نیست** | ۹ سپتامبر ۲۰۲۶ |
| D-14 | `target_role` | یازده نقش مصوب؛ اجباری. `owner` اختیاری می‌ماند و پیشنهاد به شخص وابسته نمی‌شود | ۹ سپتامبر ۲۰۲۶ |
| D-15 | `impact` | ساختاریافته: هشت `impact_type` × چهار `impact_level`؛ **هر اثر باید شواهد داشته باشد** | ۹ سپتامبر ۲۰۲۶ |
| D-16 | `effort` | مقیاس زمانی XS/S/M/L/XL | ۹ سپتامبر ۲۰۲۶ |
| D-17 | Stabilization | Content Studio پیش از هر یکپارچه‌سازی تثبیت شد — `f6946a8` | ۹ سپتامبر ۲۰۲۶ |
| D-18 | مخزن Content Studio | مخزن خصوصی **مستقل**؛ داخل MLINO نمی‌رود و ADR-0002 دست‌نخورده می‌ماند | ۹ سپتامبر ۲۰۲۶ |
| D-19 | قرارداد پیشنهاد | نسخه‌ی ۱٫۰ **IMPLEMENTED** به‌عنوان تعریف نوع و دامنه در V1؛ بدون ذخیره‌سازی | ۹ سپتامبر ۲۰۲۶ |
| D-20 | تفکیک دامنه | `Observation` · `Recommendation` · `Action` · `Outcome` · `Evaluation` پنج موجودیت جدا؛ ادغام ممنوع | ۹ سپتامبر ۲۰۲۶ |
| D-21 | ADR-0005 | تفکیک موجودیت‌های چرخه‌ی عمر پیشنهاد رسماً ثبت شد؛ تفسیر یونس تایید شد | ۹ سپتامبر ۲۰۲۶ |
| D-22 | `EventLog` و پیوند | رویداد پیوند در `EventLog` ثبت نمی‌شود؛ بدون `domainTag` جدید. موکول به معماری رویداد آینده‌ی پلتفرم | ۱۰ سپتامبر ۲۰۲۶ |
| D-23 | حذف `workspace` | بدون پیوند → حذف نرم مجاز · با پیوند فعلی یا گذشته → حذف فیزیکی ممنوع، فقط بایگانی | ۱۰ سپتامبر ۲۰۲۶ |
| D-24 | حالت پیوند | فقط `ACTIVE` و `REVOKED`؛ `SUSPENDED` حذف شد و حالت گمانه‌ای اضافه نمی‌شود | ۱۰ سپتامبر ۲۰۲۶ |
| D-25 | `basis` | فهرست بسته: `MANUAL` · `SYSTEM_MIGRATION` · `ADMIN_ACTION` · `INTEGRATION` | ۱۰ سپتامبر ۲۰۲۶ |
| D-26 | اجرای حذف `workspace` | `POLICY DEFINED` / `ENFORCEMENT DEFERRED` — بدون اجرای بین‌سیستمی در این مرحله | ۱۰ سپتامبر ۲۰۲۶ |
| D-27 | وصل دوباره | `REVOKED → ACTIVE` ممنوع؛ وصل دوباره = رکورد جدید. حفظ حقیقت تاریخی | ۱۰ سپتامبر ۲۰۲۶ |
| D-28 | ناوردای هویت تاریخی | پیوند لغوشده همیشه قابل پرس‌وجو؛ «در زمان T به کدام سازمان وصل بود؟» الزام هسته‌ای ممیزی | ۱۰ سپتامبر ۲۰۲۶ |
| D-29 | CCR ‏`ExternalWorkspaceLink` | **APPROVED** — تصویب معماری، جدا از وضعیت پیاده‌سازی | ۱۰ سپتامبر ۲۰۲۶ |
| D-30 | مرجع Business Brain | مدل زمینه = مرجع **معماری**؛ R8-b = مرجع **حاکمیت و سیاست**. R8-b حذف نمی‌شود و مدل دامنه تکرار نمی‌شود | ۱۰ سپتامبر ۲۰۲۶ |
| D-31 | نوع در برابر منبع | یازده **نوع** دانش · پنج **منبع**. `UserKnowledge` و `InferredKnowledge` منبع‌اند نه نوع | ۱۰ سپتامبر ۲۰۲۶ |
| D-32 | ارتقای استنتاج | استنتاج هرگز خودکار Fact نمی‌شود؛ ارتقا نیازمند اعتبارسنجی صریح | ۱۰ سپتامبر ۲۰۲۶ |
| D-33 | فازبندی پیاده‌سازی | فاز ۱: `Fact` `Goal` `KPI` `Capability` · فاز ۲: `Observation` `Signal` `Decision` · فاز ۳: تکامل دانش، یادگیری، استنتاج | ۱۰ سپتامبر ۲۰۲۶ |
| D-34 | `Relationship` | موکول شد. **انتزاع عمومی رابطه در V1 ساخته نمی‌شود** — پرهیز از هستان‌شناسی زودهنگام | ۱۰ سپتامبر ۲۰۲۶ |
| D-35 | `Capability` | V1 فقط توانمندی **اعلام‌شده**؛ استنتاج خودکار ممنوع. آینده: اعلام‌شده + مشاهده‌شده | ۱۰ سپتامبر ۲۰۲۶ |
| D-36 | `CUSTOMER_DATA` | در مدل مفهومی **می‌ماند** با وضعیت `DEFINED / ACCESS_BLOCKED`. محافظ زمان‌اجرا باقی می‌ماند، **بدون هیچ راه دور زدن**. ورود داده‌ی مشتری پیاده نمی‌شود | ۱۰ سپتامبر ۲۰۲۶ |
| D-37 | چهار نوع چرخه‌ی عمر | `Recommendation` `Action` `Outcome` `Evaluation` **ارجاع** هستند نه موجودیت تکراری؛ هیچ مدل ذخیره‌سازی موازی ساخته نمی‌شود. تفکیک ADR-0005 حفظ می‌شود | ۱۰ سپتامبر ۲۰۲۶ |
| D-38 | منشأ در برابر تایید | دو بُعد **جدا**: منشأ = از کجا آمد · `confirmedBy` = چه کسی تاییدش کرد. امتیاز اطمینان بالا هرگز جای تایید را نمی‌گیرد → ADR-0006 | ۱۰ سپتامبر ۲۰۲۶ |
| D-39 | منبع `Observation` | `AI_INFERRED` **رد می‌شود**. مجاز: `SYSTEM` · `HUMAN` · `INTEGRATION`. مشاهده نماینده‌ی واقعیتِ ثبت‌شده است؛ تفسیر استنتاجی به لایه‌ی `Signal` تعلق دارد | ۱۰ سپتامبر ۲۰۲۶ |
| D-40 | `Evidence` | مفهوم **مستقل**؛ به قرارداد پیشنهاد **وصل نمی‌شود**. ⚠️ عبارت «زنجیره» در نسخه‌ی اول این ردیف نادرست بود و با D-44 تصحیح شد | ۱۰ سپتامبر ۲۰۲۶ |
| D-41 | `Goal.createdByDecision` | **اختیاری**؛ هیچ هدفی را ملزم به داشتن تصمیم نمی‌کند. اهداف برخاسته از چشم‌انداز و استراتژی حفظ می‌شوند. فقط مستندسازی — بدون پیاده‌سازی | ۱۰ سپتامبر ۲۰۲۶ |
| D-42 | قدرت سیگنال | تفکیک `strength` (مقیاس ترتیبی) از `strengthBasis` (توضیح اجباری محاسبه یا منبع). **اطمینان از قدرت مشتق نمی‌شود** | ۱۰ سپتامبر ۲۰۲۶ |
| D-43 | تصمیم بیرونی | تصمیم سیستم بیرونی به‌عنوان موجودیت `Decision` در MLINO وارد **نمی‌شود** — بیرون از V1 | ۱۰ سپتامبر ۲۰۲۶ |
| D-44 | نقش `Evidence` | پشتیبان/اثبات برای مشاهده‌ها **و سایر ادعاهای دانش**. **جایگزین `Observation` نیست.** سیگنال از مشاهده‌ها **و/یا** شواهد مشتق می‌شود | ۱۰ سپتامبر ۲۰۲۶ |
| D-45 | `Evidence` نوع دانش **نیست** | `KNOWLEDGE_TYPES` دست‌نخورده یازده‌تایی می‌ماند. ساختار نهایی: `Knowledge Item + Evidence + Provenance + Confirmation` | ۱۰ سپتامبر ۲۰۲۶ |
| D-46 | `AI_INFERRED` و شاهد | هوش مصنوعی **نمی‌تواند `Evidence` بسازد**. می‌تواند تفسیر و سیگنال تولید کند، ولی نمی‌تواند منبع اثبات خودش شود | ۱۰ سپتامبر ۲۰۲۶ |
| D-47 | جایگاه `Decision` | دو منظر معتبر: `Decision → Recommendation` جریان عملیاتی · `Evaluation → Decision آینده` حلقه‌ی یادگیری. **معماری تغییر نکرد** | ۱۰ سپتامبر ۲۰۲۶ |
| D-48 | حلقه‌ی دانش (OD-30) | `Reality` = **مرز** (بیرون از سیستم، هرگز ذخیره نمی‌شود) · `Knowledge` = **لایه‌ی دانش پایدار** (`Fact` `Goal` `KPI` `Capability`، چارچوب تفسیر) · `Learning` = **فرایند** (حلقه‌ی D-47). **هیچ موجودیت جدیدی لازم نیست.** واژگان: «نوع دانش» / «دانش پایدار» / «دانش رخدادی» → `contracts/KNOWLEDGE_LOOP_ALIGNMENT.md` | ۱۰ سپتامبر ۲۰۲۶ |
| D-49 | استناد تصمیم به ارزیابی (OD-31) | **گزینه‌ی A:** `Decision.basedOn` می‌تواند به `EvaluationRecord` ارجاع دهد — فقط با شناسه. **نه** `OutcomeRecord` (انتساب قضاوت‌نشده) · **نه** از راه `Evidence`. ⛔ پیاده‌سازی مسدود تا OD-32 → `contracts/LEARNING_LOOP_ALIGNMENT.md` | ۱۰ سپتامبر ۲۰۲۶ |
| D-50 | مدل ارزیابی (OD-32) | `EvaluationRecord` **دو قضاوت مستقل**: اثربخشی و انتساب، هر کدام با سطح، مبنا و منشأ خودش؛ `effective: boolean` حذف. **انتساب هرگز بیرون از ارزیابی.** ارتباط زمانی به‌تنهایی حداکثر `possible`. شناسه برای `Action` · `Outcome` · `Evaluation`. ارتقای ادعای علّی به دانش پایدار فقط با انتساب ≥ `probable` + تایید انسانی. مسدودیت D-49 در سطح مفهومی برداشته شد → `contracts/EVALUATION_MODEL_ALIGNMENT.md` | ۱۰ سپتامبر ۲۰۲۶ |
| D-51 | مدل اقدام (OD-33) | **گزینه‌ی B:** `Action` مستقل؛ مبدأ: `recommendation` · `decision` · `automated` · `undeclared`. **منشأ ثبت ≠ مبدأ** — سیستم بیرونی منشأ است نه مبدأ. `authorizedBy` همیشه انسان؛ MLINO هرگز مجوزدهنده نیست. `undeclared` با دلیل؛ نه تصمیم پس‌نویسی‌شده، نه پیشنهاد جعلی. مبدأ معیار ارزیابی را عوض نمی‌کند. قاعده‌ی اقدام هم‌زمان. → `contracts/ACTION_MODEL_ALIGNMENT.md` · **ADR-0007** (جایگزین بخشی از ADR-0005) | ۱۰ سپتامبر ۲۰۲۶ |
| D-52 | مرز توانمندی (OD-29) | توانمندی = کاری که کسب‌وکار **واقعاً می‌تواند** انجام دهد — نه آفر، نه دسترس‌پذیری، نه دیده‌شدن. **چهار بُعد مستقل:** توانایی (`planned` · `active` · `retired`) · مخاطب (`internal` · `customer_facing`) · تایید (ADR-0006) · انتشار (`unpublished` · `published` · `withdrawn`). انتشار فقط با پنج پیش‌شرط و **عمل انسانی نقش مجاز** — رضایت سازمان (D-08). پس گرفتن خودکار مجاز، انتشار خودکار هرگز. داده‌ی منتشرنشده حتی روی رتبه‌بندی V2 اثر نمی‌گذارد. بدون طبقه‌بندی هسته. → `contracts/CAPABILITY_BOUNDARY_ALIGNMENT.md` | ۱۰ سپتامبر ۲۰۲۶ |
| D-53 | مالکیت آفر (OD-26) | **آفر مال V1.** V2 تطبیق، نمایش و ارکستراسیون؛ هرگز آفر نمی‌سازد و شرایط را عوض نمی‌کند. آفر = پیشنهاد مشخص و مشتری‌محور — چه چیزی · با چه شرایطی · در چه بازه‌ای. سه شکل: **قلم · بسته · کمپین**. هر آفر ≥۱ توانمندی؛ آفر هرگز شاهد توانمندی نیست. انتشار = مرز D-52 + انتشار همه‌ی ارجاع‌ها. اعتبار ≠ دسترس‌پذیری. واژه: «آفر» — «پیشنهاد» فقط `Recommendation`. → `contracts/OFFER_OWNERSHIP_ALIGNMENT.md` | ۱۱ سپتامبر ۲۰۲۶ |
| D-54 | قرارداد پیشنهاد v1.1 (OD-27) | پیشنهاد = توصیه‌ی موتور یا ماژول به یک نقش — **هرگز به V2 نمی‌رود**. اجباری: ≥۱ KPI · ≥۱ توانمندی `active` (مگر `develops`) · ≥۱ شاهد. `kpi_alignment` حذف. **`feedback` تا R8-a مسدود** — بستن راه دور زدن D-36. منشأ `SYSTEM`/`AI_INFERRED`/`INTEGRATION` — انسان پیشنهاد نمی‌سازد. چرخه‌ی عمر: draft · proposed · accepted · rejected · expired · superseded — **ADR-0008**. رد با دلیل. پالایش D-51 قاعده‌ی ۴. → `contracts/RECOMMENDATION_CONTRACT_ALIGNMENT.md` | ۱۱ سپتامبر ۲۰۲۶ |
| D-55 | مرز نقش و اختیار (OD-28) | **پنج مفهوم جدا:** نقش · مسئولیت · اجازه · مجوزدهی · دسترسی. **نقش هرگز اجازه یا دسترسی اعطا نمی‌کند**؛ اجازه به **شخص**، نقش فقط الگو (کپی، نه ارث) و مسیریابی. دسترسی سازمانی می‌ماند (AC-2). عمل‌های حاکمیتی در چهار خانواده. **هیچ اجازه‌ی ضمنی؛ مدیر پلتفرم هیچ اجازه‌ی کسب‌وکاری — هرگز.** یازده دسته به ماژول خنثی؛ نقش توکن منجمد گسترش داده نمی‌شود. **ADR-0009**. اجراشدنی پس از OD-36. → `contracts/ROLE_BOUNDARY_ALIGNMENT.md` | ۱۱ سپتامبر ۲۰۲۶ |
| D-56 | مرز دسترس‌پذیری (OD-34) | دسترس‌پذیری = **وضعیت عملیاتی** آینده‌نگر، فاسدشدنی و رقابتی — نه حقیقت پایدار، نه دانش رخدادی، نه داده‌ی تجربه؛ **نوع دانش نیست**. حقیقت در سامانه‌ی مرجع کسب‌وکار، نمایندگی در V1، نمایش در V2. به توانمندی یا آفر ارجاع. هرگز `AI_INFERRED` یا رفتار مشتری. **وعده نیست — فقط رزرو وعده است.** منقضی ⇒ نامعلوم. ساعت کاری = `Fact`؛ «در ساعت کاری اعلام‌شده است»، نه «الان باز است». هوش ظرفیت داخلی به V2 نمی‌رسد — پل، آفر منتشرشده است. fail-closed تا اولین یکپارچه‌سازی واقعی. → `contracts/AVAILABILITY_BOUNDARY_ALIGNMENT.md` | ۱۱ سپتامبر ۲۰۲۶ |
| D-57 | عضویت و اجازه (OD-36) | **ترکیبی الگو (C۲):** اجازه فقط در **اعطا به عضویت**؛ نقش فقط الگوی کپی در لحظه‌ی اعطا — **بدون پیوند زنده‌ی نقش به اجازه**. عضویت = شخص × سازمان، `active` · `ended`. مبنای اعطا `founding` · `member_grant` — **بدون `ADMIN_ACTION`**. دارندگان اجازه‌ی اعطا هم‌تراز و غیرقابل‌حذف توسط یکدیگر. **مدیر پلتفرم و مدل هرگز اعطا نمی‌گیرند؛ نیابت ماندگار به ماشین ممنوع.** ادعای سازمان در توکن مرجع عضویت نیست. حداقل V1: سازمان + عضویت + اعطا + شش اجازه — بدون نقش. مرتبط: `ROLE_BOUNDARY_ALIGNMENT.md` و ADR-0009 هنوز به «OD-36» ارجاع می‌دهند — این ردیف جانشین آن است. → `contracts/MEMBERSHIP_PERMISSION_BOUNDARY_ALIGNMENT.md` | ۱۱ سپتامبر ۲۰۲۶ |
| D-59 | بستن C-03 | پیشنهاد داخلی بر توانمندی معتبر منتشرنشده مجاز است؛ انتشار شرط خروج به V2 است | ۱۱ سپتامبر ۲۰۲۶ |
| D-60 | بستن C-04 | مجوز از اعطا به عضویت سازمانی حاصل می‌شود؛ نقش فقط الگو/زمینه است | ۱۱ سپتامبر ۲۰۲۶ |
| D-58 | بازیابی پلتفرم (OD-37 — بازیابی و اختلاف) | بازیابی = بازگرداندن **توان خودگردانی** به صاحب برحق، نه کارهای کسب‌وکار. **پنج لایه:** پیشگیری (D-57) · بازیابی **هویت** · **جانشینی پیشین** توسط خود کسب‌وکار · **نگه‌داشت ایمنی** (فقط بستن) · **بازیابی مالکیت** با شاهد بیرونی و چهارچشمی — آخرین پناه. **بازیابی می‌افزاید، کم نمی‌کند.** پلتفرم گیرنده را انتخاب نمی‌کند؛ فقط اعطای مؤسس؛ محتوای کسب‌وکار را نمی‌خواند. اختلاف: فقط سند بیرونی. هوش مصنوعی بیرون از مسیر تصمیم. پالایش D-57: مبنای `designated_succession` · `ownership_recovery`. D-55 قاعده‌ی ۶ به‌معنای لفظی برقرار. `MEMBERSHIP_PERMISSION_BOUNDARY_ALIGNMENT.md` هنوز به «OD-37» ارجاع می‌دهد — این ردیف جانشین آن است. → `contracts/PLATFORM_RECOVERY_BOUNDARY_ALIGNMENT.md` | ۱۱ سپتامبر ۲۰۲۶ |
| D-61 | هویت کسب‌وکار (OD-38) | **ساختن آزاد؛ پیش از اولین سخن با عموم، هویت راستی‌آزمایی می‌شود.** ساختن سازمان ≠ ادعای هویت؛ عضو مؤسس ≠ مالک حقوقی؛ آنچه راستی‌آزمایی می‌شود **اختیار نمایندگی** است، نه مالکیت حقوقی. هیچ نامی رزرو نمی‌شود. حداکثر یک ادعای راستی‌آزمایی‌شده‌ی فعال برای هر کسب‌وکار واقعی، بدون انتقال خودکار. **راستی‌آزمایی هویت هرگز اختیار حکمرانی نمی‌دهد.** هوش مصنوعی راستی‌آزمایی نمی‌کند؛ پلتفرم بین مدعیان انتخاب نمی‌کند. پالایش: D-52 پیش‌شرط ششم انتشار · D-57 اجازه‌ی هفتم. → `contracts/BUSINESS_IDENTITY_BOUNDARY_ALIGNMENT.md` | ۱۱ سپتامبر ۲۰۲۶ |
| D-62 | یکپارچه‌سازی و مالکیت (OD-39) | **پیوند workspace = نگاشت هویت** — نه مالکیت، نه اختیار، نه هویت کسب‌وکار. **سه کانال عبور نمی‌کنند:** داده (`INTEGRATION`) · اختیار (D-57 · D-58) · هویت (D-61). `ADMIN_ACTION` **کانال اجرا**ست، نه منبع اختیار — فهرست D-25 بدون تغییر. ساختن پیوند با هر مبنایی **رضایت دوطرفه** می‌خواهد؛ تا ثبت‌شدنی نشده، از هیچ API در دسترس نیست. Content Studio **ماژول** است و فقط داده می‌دهد؛ بدون سیستم هویت دوم. اجازه‌ی ماژول = فضانام ثبت‌شده روی سازوکار Core. V2 حقیقت نمی‌نویسد. **حکم بازبینی: B** — OD-40 · C-05. → `contracts/INTEGRATION_OWNERSHIP_BOUNDARY_ALIGNMENT.md` | ۱۱ سپتامبر ۲۰۲۶ |
| D-63 | مرز Core و ماژول‌ها (OD-40) | **آزمون سه‌شرطی Core:** بدون واژگان صنف معنا دارد · معنایش را Core تعریف می‌کند · زنجیره‌ی Core یا دو ماژول مستقل به آن نیاز دارند — «افقی» ≠ Core. **Core هیچ واژه‌ی صنفی را نام نمی‌برد** — نه import، نه فهرست بسته، نه بذر، نه برچسب نمایشی. «فرصت» Core است؛ **«خانواده‌ی فرصت» مال ماژول، ثبت‌شده**؛ `opportunity.interaction` سازوکار Core. **Core هیچ نام نقشی ندارد.** نوبت، بیمار، سابقه‌ی درمان، درمانگر، موتور ظرفیت، نقش‌ها و گردش‌کارهای درمانی ← **ماژول کلینیک**؛ خدمت فروختنی ← توانمندی و آفر Core. Core «رزرو» عمومی نمی‌سازد. مهاجرت: م۰ طبقه‌بندی منطقی (اکنون) · م۱ **یک CCR یک‌باره** روی قرارداد منجمد با مجوز جداگانه (فهرست بسته ← واژگان ثبت‌شده‌ی فضانام‌دار) · م۲ جابه‌جایی کد. **برچسب‌های موجود هرگز تغییر نام نمی‌دهند.** تا CCR هیچ واژه‌ی صنفی تازه در Core. → `contracts/OD-40_CORE_MODULE_BOUNDARY_ALIGNMENT.md` | ۱۱ سپتامبر ۲۰۲۶ |
| D-64 | Core · V1 · V2 (C-05) | با جداسازی صریح مالک: **Core** هوش و ارکستراسیون مستقل از کسب‌وکار · **V1** حقیقت و سیستم‌عامل (= Core + ماژول‌ها) · **V2** کشف، تطبیق، تعامل و تجربه. ارکستراسیون تجربه و زیربنای دستیار = **Core**؛ رابط دستیار، تطبیق، کشف و رندر = **V2**. جای‌گذاری بافت OD-26 جایگزین شد. **خروجی ارکستراسیون برای کاربر نهایی فقط از تصویر منتشرشده (D-52).** جریان‌ها و مهارت‌های صنفی را ماژول ثبت می‌کند. ابهام باقی‌مانده ← C-06. → `contracts/OD-40_CORE_MODULE_BOUNDARY_ALIGNMENT.md` بند ۶ | ۱۱ سپتامبر ۲۰۲۶ |
| D-65 | روشن‌سازی قاعده‌ی ۴ در D-63 (مصوب مالک) | **Core هیچ نقش دامنه‌ی کسب‌وکار ندارد.** ولی **دسته‌های مسئولیت عمومی D-55 مفاهیم Core‌اند** — `owner_founder` · `marketing` · `finance` · `specialist` و بقیه‌ی یازده دسته. **ماژول:** دندان‌پزشک · جراح · باریستا · سرآشپز · نقش‌های عملیاتی صنفی. پیامد: `TARGET_ROLES` در Core می‌ماند. **D-55 بازنویسی نشد**؛ D-63 فقط روشن شد. → `MLINO_ARCHITECTURE_CLOSURE_NEXT_STEPS.md` | ۱۱ سپتامبر ۲۰۲۶ |
| D-66 | مرز Session (مصوب مالک) | **Session مال Core است.** ماژول‌ها **نمی‌توانند Session احراز هویت بسازند.** Session **فقط بافت هویت** را حمل می‌کند. **اجازه از عضویت و اعطا می‌آید** (D-57). **Session اختیار نمی‌دهد.** پیامد: سازمانِ عمل در Session حمل نمی‌شود — هنگام درخواست با عضویت سنجیده می‌شود. JWT امروز (`organization_id` · `role`) وضع موقت است — OD-08 و CCR م۱. → `MLINO_ARCHITECTURE_CLOSURE_NEXT_STEPS.md` | ۱۱ سپتامبر ۲۰۲۶ |
| D-67 | دو دستیار (C-06 — مصوب مالک) | **دستیار مشتری:** V2 · **فقط حقیقت منتشرشده‌ی کسب‌وکار** · کشف و تجربه. **دستیار عملیاتی کسب‌وکار:** داخلی · **محافظت‌شده با AC-2** · **بیرون از دامنه‌ی مشتری فعلی V2**. هر دو روی زیربنای دستیار Core (D-64). D-52 دست‌نخورده. → `MLINO_ARCHITECTURE_CLOSURE_NEXT_STEPS.md` | ۱۱ سپتامبر ۲۰۲۶ |

---

## تصمیم‌های باز

### ⬜ OD-01 — سیاست رضایت (R8-a)

**چرا مهم:** بند ۴ و ۷ چشم‌انداز (صدای مشتری و CRM) بدون آن شروع نمی‌شوند. پردازش نظر، پیام و مکالمه‌ی مشتری یعنی داده‌ی شخصی افراد واقعی.

**وضعیت:** طرح آماده → [`policy/R8a_CONSENT_POLICY_PROPOSAL.md`](policy/R8a_CONSENT_POLICY_PROPOSAL.md)
**اثر تا تصمیم:** ۴ و ۷ 🔒 · **تصمیم‌گیرنده:** مالک محصول

---

### ⬜ OD-02 — مدل دانش Business Brain (R8-b)

**چرا مهم:** بدون تفکیک انواع دانش، Business Brain به یک vector database بی‌ساختار تبدیل می‌شود و `objective` در قرارداد پیشنهاد همیشه خالی می‌ماند.

**وضعیت:** طرح آماده → [`policy/R8b_BUSINESS_BRAIN_KNOWLEDGE_MODEL.md`](policy/R8b_BUSINESS_BRAIN_KNOWLEDGE_MODEL.md)
**اثر تا تصمیم:** ۱ و ۶ 📋 · **تصمیم‌گیرنده:** مالک محصول

---

### ⬜ OD-03 — استراتژی آینده‌ی مخازن

**پرسش:** چه زمانی و بر چه معیاری درباره‌ی monorepo، merge یا مرز سرویس تصمیم بگیریم؟

**وضعیت:** عمداً باز. D-03 فقط وضعیت **فعلی** را تعیین کرده.
**معیار پیشنهادی برای بازبینی:** وقتی بیش از دو قرارداد بین‌سیستمی فعال شد، یا وقتی یک تغییر مجبور شد هم‌زمان در هر دو مخزن اعمال شود.

---

### ⬜ OD-04 — مرزهای درآمدی

**پرسش:** چه چیزی رایگان است، چه چیزی پولی؟ سطح‌بندی روی کدام محور — تعداد workspace، استراتژی، حجم پیشنهاد، دسترسی به V2؟

**چرا الان مهم است:** اگر مرز درآمدی بعداً تعریف شود، ممکن است مجبور شویم مدل داده را عوض کنیم. یک اشاره‌ی زودهنگام (مثلاً `plan_id` روی organization) ارزان است؛ افزودن بعدی‌اش گران.

**وضعیت:** باز، طرحی وجود ندارد
**یادداشت:** در V2 مفهوم «پلن» به‌صورت سمت‌کلاینت وجود دارد و **صریحاً UX است نه امنیت**. این نباید با مرز درآمدی واقعی اشتباه شود.

---

### ⬜ OD-05 — قرارداد قابل‌انتشار V1/V2

**پرسش:** قرارداد `02` هنوز DRAFT است و یک CR باز دارد (`mlino2/HANDOFF/20260908_CR_AR_UI_CONTRACT_GAPS.md`) برای فیلدهایی که V2 می‌خواهد و وجود ندارند: لوگو، تصویر، ساعت کاری، امتیاز، آدرس، تلفن، وب‌سایت، اینستاگرام، چت.

**تصمیم لازم:** کدام‌یک از این‌ها به قرارداد اضافه شوند، و «چت با کسب‌وکار» کِی فاز خودش را بگیرد؟

**اثر تا تصمیم:** کارت جزئیات V2 محدود به فیلدهای موجود می‌ماند

**به‌روزرسانی (D-52):** **بخش توانمندی** این قرارداد تعریف شد — چه چیزی به V2 می‌رود (شناسه، نام، دسته، سطح اعتماد، تازگی، وجود شاهد) و چه چیزی نمی‌رود (داخلی، منتشرنشده، محتوای شاهد، هویت تاییدکننده). و یک الزام برای هر سازوکار انتقال: **باید پس گرفتن را منتقل کند، نه فقط افزودن را.** بقیه‌ی فیلدها باز می‌مانند. → `contracts/CAPABILITY_BOUNDARY_ALIGNMENT.md` بند ۶

**به‌روزرسانی (D-53):** **بخش آفر** هم تعریف شد. نگاشت برای CR قرارداد `02`: `products[]` ← آفر با شکل قلم یا بسته · `offers[]` ← آفر با شکل کمپین · **`appliesTo`** ← ارجاع موضوعی که امروز وجود ندارد · `discount_percent` ← `adjustment` کلی‌تر · `is_active` ← **حذف**؛ عضویت در تصویر = منتشرشده ∧ معتبر. → `contracts/OFFER_OWNERSHIP_ALIGNMENT.md` بند ۸

**به‌روزرسانی (D-56):** «ساعت کاری» در فهرست خواسته‌های V2 یک **`Fact` اعلام‌شده** است، نه دسترس‌پذیری — V2 می‌تواند «در ساعت کاری اعلام‌شده است» بگوید، هرگز «الان باز است» بی‌قید. **فیلد دسترس‌پذیری به قرارداد `02` افزوده نمی‌شود** تا اولین یکپارچه‌سازی واقعی. تلفن و لینک نوبت‌دهی منتشرشده‌ی کسب‌وکار راه **سپردن** کاربر به منبع حقیقت‌اند. → `contracts/AVAILABILITY_BOUNDARY_ALIGNMENT.md` بند ۶

---

### ⬜ OD-06 — رتبه‌بندی بین‌منبعی پیشنهادها

**پرسش:** «فرصت عملیاتی» و «پیشنهاد محتوایی» چطور با هم مقایسه شوند؟

**چرا باز است:** این یک قضاوت محصولی است، نه فنی. تا تعریف نشود، نمای واحد فقط گروه‌بندی می‌کند.
**اثر:** گام ۶ نقشه‌ی راه

---

### ⬜ OD-07 — یادگیری بین کسب‌وکارها

**پرسش:** آیا نتیجه‌ی اقدام یک کسب‌وکار می‌تواند پیشنهاد کسب‌وکار دیگری را بهبود دهد؟

**چرا حساس است:** این مستقیماً به مدل دو‌لایه‌ی داده/دانش برمی‌گردد که در سیاست AC-2 ثبت شده — داده‌ی خام متعلق به سازمان است، بینش تجمیعی می‌تواند جمعی باشد. ولی مرزش تعریف نشده.

**وضعیت:** باز · **وابسته به:** OD-01

**تحلیل (۱۰ سپتامبر ۲۰۲۶):** دامنه‌ی آینده می‌ماند · مفهوم جدید تقریباً قطعاً لازم است، چون بینش تجمیعی `businessId` واحد ندارد و طبق قاعده‌ی C.۱ نمی‌تواند `Fact` هیچ کسب‌وکاری باشد · معماری امروز V1 را عوض نمی‌کند، **با یک شرط: OD-32** — تجمیع ارزیابی‌های بی‌انتساب یعنی علیت قضاوت‌نشده در مقیاس. → [`contracts/LEARNING_LOOP_ALIGNMENT.md`](contracts/LEARNING_LOOP_ALIGNMENT.md) بند ۷

**به‌روزرسانی (بسته‌ی بستن معماری):** **شرط OD-32 با D-50 برآورده شد.** مرز هفت‌قاعده‌ای پیشنهاد شد: داده‌ی خام عبور نمی‌کند · بینش مالک واحد ندارد · رضایت سهم‌دهنده · شناسایی‌ناپذیری · ادعای علّی فقط با انتساب ≥ `probable` · بینش فقط ورودی پیشنهاد، هرگز به V2 · محتوا در فضانام ماژول (D-63). **توصیه: مرز امروز، توانمندی پشت OD-01.** محافظ: هیچ تجمیع بین‌سازمانی پیش از تصمیم. → `MLINO_ARCHITECTURE_CLOSURE_NEXT_STEPS.md` بند ۶

---

### 🔴 OD-09 — Remote برای Content Studio · وضعیت: **BLOCKED**

**تصمیم مالک:** مخزن خصوصی مستقل ساخته شود. Content Studio داخل مخزن MLINO **نمی‌رود** و ADR-0002 دست‌نخورده می‌ماند.

**آنچه تلاش شد:** ساخت مخزن خصوصی از راه GitHub API با توکن موجود.

**نتیجه:**

```
POST /user/repos  →  403 Forbidden
"Resource not accessible by personal access token"
```

توکن فعلی یک PAT ریزدانه (`github_pat_…`) است که فقط به `mlino_3-sep` دسترسی دارد و **اختیار ساخت مخزن ندارد**. احراز هویت خودش سالم است (`GET /user` → ۲۰۰، کاربر `aminansaricom-bot`).

**هیچ راه جایگزینی انجام نشد** — نه انتقال به مخزن MLINO، نه monorepo، نه تغییر مرز. طبق دستور صریح.

**دستور دقیق موردنیاز از مالک محصول — یکی از این دو:**

۱. **مخزن را خودت بساز** (خصوصی، مثلاً `mlino-content-studio`) و آدرسش را بده؛ سپس با همین توکن — اگر به آن مخزن دسترسی نوشتن داده شود — Remote وصل و Push می‌شود.

۲. **یک توکن با اختیار ساخت مخزن بده** (کلاسیک با scope ‏`repo`، یا ریزدانه با مجوز `Administration: write` روی حساب).

⚠️ **تا انجام یکی از این دو، Commit ‏`f6946a8` فقط روی دیسک این ماشین است.** هیچ Backupی وجود ندارد و **ادعای Backup هم نمی‌شود**.

**اولویت: بالا.**

---

### ⬜ OD-12 — راهبرد اجرای آینده‌ی سیاست حذف `workspace`

**قاعده تصویب و مستند شد** (D-26). ولی وضعیت رسمی‌اش این است:

```
POLICY DEFINED
ENFORCEMENT DEFERRED
```

**در این مرحله عمداً انجام نمی‌شود:** تغییر Content Studio برای اجبار، بررسی دوره‌ای پیوندهای یتیم، بررسی حذف بین‌سیستمی، یا افزودن همگام‌سازی صرفاً برای این هدف.

**دلیل مصوب:** اجرای این قاعده یک مرز بین‌سیستمی جدید باز می‌کند و تصمیم معماری جداگانه‌ای می‌خواهد.

**این مورد برای راهبرد اجرای آینده باز می‌ماند** — تا بعداً کسی فرض نکند که سیستم این قاعده را تضمین می‌کند.

---

### ⬜ OD-08 — تصمیم‌های Connector نسخه‌ی ۱

**پرسش:** صادرکننده‌ی JWT کیست؟ `MLINO_JWT_SECRET` کجا نگهداری می‌شود؟ زیرساخت دیپلوی چیست؟

**وضعیت:** باز از پیش‌تر · **اثر:** گام ۸ نقشه‌ی راه (اتصال واقعی V1 به V2)

**به‌روزرسانی (D-57):** این پرسش حالا **ساده‌تر** است. رکورد عضویت **مرجع تعلق** است، نه ادعای سازمان در توکن — پس صادرکننده‌ی توکن فقط باید **هویت** را تضمین کند، نه عضویت را. ولی OD-37 (ساختن سازمان و ریشه‌ی اعتماد) **به این تصمیم وابسته است**.

**به‌روزرسانی (D-58):** لایه‌ی ۱ بازیابی — **بازیابی هویت** برای حساب گم‌شده — در همین لایه‌ی هویت انجام می‌شود و هیچ اعطایی را تغییر نمی‌دهد. پس صادرکننده‌ی توکن باید **مسیر بازیابی هویت** هم داشته باشد. OD-38 (اثبات هویت کسب‌وکار) هم به این تصمیم وابسته است.

**به‌روزرسانی (D-61):** OD-38 بسته شد. D-61 دو هویت را جدا کرد: **هویت شخص** (اینجا — صادرکننده‌ی توکن) و **هویت کسب‌وکار** (اختیار نمایندگی، راستی‌آزمایی‌شده پیش از انتشار). این پرسش فقط درباره‌ی اولی می‌ماند.

---

### ⬜ OD-35 — جای آفر در طبقه‌بندی انواع دانش

**زمینه:** D-53 آفر را مفهوم V1 کرد. ولی `KNOWLEDGE_TYPES` یازده نوع مصوب دارد و آفر بینشان نیست.

**پرسش:** آفر نوع دانش **دوازدهم** در لایه‌ی پایدار است، یا موجودیت تجاری‌ای بیرون از آن طبقه‌بندی که همان ابعاد را دارد؟

**توصیه‌ی من: نوع دوازدهم، لایه‌ی پایدار.** آفر یک **ادعای کسب‌وکار** است — «این بسته X تومان است» — با هر چهار بُعد: منشأ، تایید، شاهد، تازگی. یعنی آزمونی را که `Evidence` در D-45 رد شد، می‌گذراند: شاهد **ماده‌ی پشتیبان** بود و ادعا نبود؛ آفر خودش ادعاست.

**چرا خودم تغییرش ندادم:** فهرست یازده‌تایی را مالک تصویب کرده (D-31). همان رفتار OD-25.

**اثر تا تصمیم:** فقط شکل پیاده‌سازی. آفر در هر حال **پس از** بُعد شاهد فاز ۲ پیاده می‌شود، چون انتشارش به شاهد نیاز دارد.

**به‌روزرسانی (بسته‌ی بستن معماری):** یک **گزینه‌ی سوم** از خود کد: `KNOWLEDGE_TYPES` از قبل `recommendation` و `action` را **با ارجاع** دارد، بدون ذخیره‌ی دوباره (ADR-0005). **توصیه‌ی به‌روزشده: نوع دوازدهم با ارجاع** — چون D-53 آفر را نسخه‌دار، با انقضا و انتشار کرد، و این چرخه‌ی عمر یک موجودیت است. هر دو گزینه‌ی نوع دوازدهم D-31 را تغییر می‌دهند ← تصمیم مالک. پیاده‌سازی به رکورد سازمان، عضویت و اعطا، و `Action` (ADR-0007) هم وابسته است. → `MLINO_ARCHITECTURE_CLOSURE_NEXT_STEPS.md` بند ۵

**مرجع:** [`contracts/OFFER_OWNERSHIP_ALIGNMENT.md`](contracts/OFFER_OWNERSHIP_ALIGNMENT.md) بند ۹

---


## تعارض‌های علامت‌گذاری‌شده

### ⚠️ C-01 — ترتیب اجرا: توصیه‌ی من در برابر تصمیم مالک *(حل‌شده)*

**تعارض:** نسخه‌ی ۰٫۱ نقشه‌ی راه، **اتصال دیتابیس را گام ۲** گذاشته بود — پیش از نهایی شدن قراردادها. استدلالم: بیشترین ارزش با کمترین ریسک، چون منطق تشخیص از قبل نوشته و تست شده و فقط لایه‌ی ذخیره‌سازی عوض می‌شود.

**تصمیم مالک:** رد شد. ترتیب مصوب: قرارداد → نگاشت → دیتابیس → API → UI، با قاعده‌ی «دیتابیس پیش از قراردادها طراحی نمی‌شود».

**چرا تصمیم مالک درست است:** ممیزی خودم نشان داد سه مفهوم اجباری قرارداد (`target_role`، `expected_impact`، `effort`) در **هیچ‌کدام** از دو موتور وجود ندارند. یعنی شِمای امروز ناقص است. اگر Repositoryها پیش از تثبیت قرارداد به دیتابیس وصل شوند، شکل جدول از مدل ناقص امروز می‌آید و مهاجرت بعدی گران‌تر از تأخیر امروز می‌شود.

**وضعیت:** حل‌شده به نفع تصمیم مالک · ثبت‌شده به‌عنوان D-09 · نقشه‌ی راه بازنویسی شد

---

### ⚠️ C-02 — جایگاه `Decision` در زنجیره *(حل‌شده)*

**پرسش:** زنجیره‌ی عملیاتی `Decision` را **پیش از** `Recommendation` می‌گذارد؛ نمودار بند B مدل زمینه آن را **پس از** `Evaluation` می‌گذارد.

**تصمیم مالک (D-47): هر دو منظر معتبرند و معماری تغییر نمی‌کند.**

```
جریان عملیاتی    Decision → Recommendation
حلقه‌ی یادگیری    Evaluation → Decision آینده
```

یک تصمیم هم از سیگنال‌های امروز تغذیه می‌شود و هم از ارزیابی اقدام‌های دیروز. اولی جریان عملیاتی است، دومی یادگیری.

**آنچه انجام شد:** هر دو منظر در مدل زمینه و طراحی فاز ۲ مستند شدند. **نمودار مصوب بازنویسی نشد.**

---


### ✅ C-03 — «توانمندی منتشرنشده» در قید پیشنهاد *(حل‌شده)*

**تعارض:** دستور OD-27 گفت پیشنهاد نباید «بر پایه‌ی توانمندی منتشرنشده» باشد. ولی D-52 (مصوب) انتشار را **رضایت برای خروج داده به V2** تعریف کرده، و پیشنهاد **هرگز از سازمان خارج نمی‌شود**.

**اگر قید لفظی اعمال شود، دو دسته پیشنهاد مشروع بسته می‌شوند:**
- هر پیشنهادی که روی توانمندی **داخلی** بنشیند — چون توانمندی داخلی طبق D-52 **هرگز** منتشر نمی‌شود
- «خدمت X هنوز در MLINO نیست — منتشرش کنید» — درست به‌خاطر اینکه منتشر نشده

**خوانش من:** منظور قید «توانمندی‌ای که کسب‌وکار واقعاً ندارد» است — یعنی بُعد **توانایی** (`active`)، نه **انتشار**. انتشار فقط وقتی شرط است که اقدام پیشنهادی **خودش** قرار دادن چیزی در V2 باشد. D-54 با این خوانش ثبت شد.

**آنچه انجام دادم:** خوانش سازگار با D-52 اعمال شد و این‌جا علامت خورد تا بی‌صدا حل‌شده تلقی نشود.

**تصمیم مالک — ۱۱ سپتامبر ۲۰۲۶ (D-59):** تایید شد. پیشنهاد داخلی می‌تواند بر توانمندی معتبر اما منتشرنشده تکیه کند. انتشار فقط برای خروج اطلاعات به V2 و تجربه کاربر لازم است. تولید پیشنهاد به‌معنای مجوز انتشار نیست.

**مرجع:** [`contracts/RECOMMENDATION_CONTRACT_ALIGNMENT.md`](contracts/RECOMMENDATION_CONTRACT_ALIGNMENT.md) بند ۶٫۱

---


### ✅ C-04 — جای «کاربر» در زنجیره‌ی عضویت *(حل‌شده)*

**تعارض:** دستور OD-37 مدل فعلی را این‌طور نوشت: `Organization → Membership → User → Permission Grant → Authorization` — یعنی **کاربر بین عضویت و اعطا**. ولی D-57 (مصوب با تفویض) اعطا را **به عضویت** وصل کرده، نه به شخص.

**چرا مهم است:** اگر اعطا به **کاربر** وصل شود، مشاوری که در کلینیک A اجازه‌ی انتشار دارد، مرزش با کلینیک B فقط با یک شرط اضافه حفظ می‌شود. روی **عضویت**، این مرز **ساختاراً** حفظ می‌شود — دلیل اصلی D-57.

**خوانش من:** نمودار دستور احتمالاً **فهرست موجودیت‌ها** است نه ترتیب وابستگی. D-58 با مدل D-57 نوشته شد: `Organization → Membership (شخص × سازمان) → Grant → Authorization`.

**آنچه انجام دادم:** D-57 حفظ شد و این‌جا علامت خورد تا بی‌صدا حل‌شده تلقی نشود.

**تصمیم مالک — ۱۱ سپتامبر ۲۰۲۶ (D-60):** تایید شد. مجوزدهی به عضویت سازمانی متصل است، نه کاربر مستقل. نقش فقط الگو/زمینه است؛ اجازه باید به عضویت اعطا شود. این تصمیم به‌معنای پیاده‌سازی یا تایید سایر ADRها نیست.

**مرجع:** [`contracts/MEMBERSHIP_PERMISSION_BOUNDARY_ALIGNMENT.md`](contracts/MEMBERSHIP_PERMISSION_BOUNDARY_ALIGNMENT.md) بند ۴٫۱

---


### ✅ C-05 — «ارکستراسیون تجربه» و «زیربنای دستیار»: Core یا V2؟ *(حل‌شده)*

**تعارض:** دستور OD-39 این دو را در فهرست مسئولیت‌های **Core** آورد، و در همان دستور V2 مالک «تجربه» و «رابط دستیار» است. بافت دستور OD-26 «ارکستراسیون تجربه» را صریحاً مال **V2** گذاشته بود.

**خوانش من:** **دو محور مستقل و عمود بر هم‌اند:**
- **Core / ماژول** — بین‌کسب‌وکاری در برابر صنفی
- **V1 / V2** — حقیقت کسب‌وکار در برابر تجربه

Core هر دو طرف را می‌پوشاند: ارکستراسیون تجربه **Core** است (بین‌کسب‌وکاری) **و** در **سمت V2** است؛ زیربنای دستیار Core است و رابطش V2. با این خوانش تناقضی نیست.

**چرا مهم است:** جای منطق V2 و اینکه ماژول‌ها می‌توانند بخش تجربه داشته باشند یا نه، به آن بستگی دارد.

**آنچه انجام دادم:** D-62 با خوانش دومحوری نوشته شد و این‌جا علامت خورد تا بی‌صدا حل‌شده تلقی نشود.

**تصمیم لازم:** تایید خوانش.

**تصمیم مالک — ۱۱ سپتامبر ۲۰۲۶ (D-64):** با جداسازی صریح دستور OD-40 حل شد — **Core:** هوش و ارکستراسیون مستقل از کسب‌وکار · **V1:** حقیقت کسب‌وکار و سیستم‌عامل · **V2:** کشف، تطبیق، تعامل و تجربه. ارکستراسیون تجربه و زیربنای دستیار = **Core**؛ رابط دستیار، تطبیق، کشف و رندر = **V2**. **خوانش من بخشی اصلاح شد:** ارکستراسیون تجربه مال Core است، **نه «در سمت V2»** — V2 فقط رندر می‌کند. جای‌گذاری بافت OD-26 جایگزین شد. محافظ: خروجی ارکستراسیون برای کاربر نهایی فقط از تصویر منتشرشده (D-52). ابهام باقی‌مانده ← **C-06**.

**مرجع تصمیم:** [`contracts/OD-40_CORE_MODULE_BOUNDARY_ALIGNMENT.md`](contracts/OD-40_CORE_MODULE_BOUNDARY_ALIGNMENT.md) بند ۶

**مرجع:** [`contracts/INTEGRATION_OWNERSHIP_BOUNDARY_ALIGNMENT.md`](contracts/INTEGRATION_OWNERSHIP_BOUNDARY_ALIGNMENT.md) بند ۶

---


### ✅ C-06 — رابط دستیار اعضای کسب‌وکار: V2 یا پشت AC-2؟ *(حل‌شده)*

**تعارض:** جداسازی مالک در دستور OD-40 «رابط دستیار» را مال **V2** می‌گذارد. ولی D-52 (مصوب) می‌گوید V2 **فقط تصویر منتشرشده** را دریافت می‌کند. دستیار **کاربر نهایی** با این سازگار است. دستیار **اعضای خود کسب‌وکار** روی حقیقت داخلی **منتشرنشده** کار می‌کند؛ اگر V2 آن را رندر کند، داده‌ی منتشرنشده به V2 می‌رسد.

**خوانش من:** «رابط دستیار = V2» یعنی دستیار **کاربر نهایی**. سطوح عملیاتی اعضا — feed، briefing و دستیار داخلی — پشت AC-2 می‌مانند. اگر V2 قرار است سطح احراز‌هویت‌شده‌ی اعضا هم باشد، **پالایش صریح D-52** لازم است.

**آنچه انجام دادم:** D-64 با این خوانش نوشته شد و این‌جا علامت خورد تا بی‌صدا حل‌شده تلقی نشود. **D-63 و D-64 به آن وابسته نیستند.**

**تصمیم لازم:** تایید خوانش، یا پالایش D-52.

**تصمیم مالک — ۱۱ سپتامبر ۲۰۲۶ (D-67):** دو دستیار جدا. **دستیار مشتری** — V2، فقط حقیقت منتشرشده، کشف و تجربه. **دستیار عملیاتی کسب‌وکار** — داخلی، محافظت‌شده با AC-2، بیرون از دامنه‌ی مشتری فعلی V2. خوانش من تایید شد و **D-52 پالایش نمی‌خواهد.**

**مرجع:** [`contracts/OD-40_CORE_MODULE_BOUNDARY_ALIGNMENT.md`](contracts/OD-40_CORE_MODULE_BOUNDARY_ALIGNMENT.md) بند ۶٫۳

---



هیچ تعارض حل‌نشده‌ی دیگری بین MLINO BOOK و اسناد دامنه‌ای ثبت نشده است.

اگر بعداً پیدا شد: اینجا ثبت می‌شود، سند دامنه‌ای تا تصمیم مالک معتبر می‌ماند، و **بی‌صدا حل نمی‌شود**.

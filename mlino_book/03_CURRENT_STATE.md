# گزارش وضعیت فعلی

**تاریخ:** ۹ سپتامبر ۲۰۲۶ · **مبنا:** ممیزی مستقیم کد و اجرای واقعی تست‌ها

---

## راهنمای وضعیت‌ها

| برچسب | معنا |
|---|---|
| ✅ **ALREADY IMPLEMENTED** | ساخته، تست‌شده، و بازبینی‌شده |
| 🟡 **PARTIALLY IMPLEMENTED** | بخشی کار می‌کند؛ محدودیت صریح دارد |
| 📋 **PLANNED** | طراحی یا قرارداد آماده است، کد نه |
| 🔒 **BLOCKED BY POLICY** | فنی شدنی است ولی سیاست لازم تصویب نشده |
| ⏸ **DEFERRED** | عمداً به بعد موکول شده |

---

## MLINO V1 Core

| قابلیت | وضعیت | جزئیات |
|---|---|---|
| Event Log و Projection | ✅ | ۷ مدل Prisma، معماری event-sourced |
| API خواندنی فرصت | ✅ | `node:http`، بدون وابستگی جدید |
| **AC-2 — حاکمیت دسترسی** | ✅ | fail-closed · existence oracle بسته در مرز HTTP · Adapter واقعی سازمانی |
| Composition Root | ✅ | یک‌بار در startup ساخته می‌شود |
| اجرای Docker | ✅ | Postgres + سرویس migrate + read-api؛ زنده از بیرون کانتینر تست شد |
| **موتورهای تشخیص فرصت** | 🟡 | منطق `cancellation`، `capacity`، `followup` نوشته و تست شده — **ولی هر سه Repository آن In-Memory است و به دیتابیس وصل نیست** |
| Connector و JWT | ⏸ | منتظر تصمیم مالک: صادرکننده‌ی JWT، محل `MLINO_JWT_SECRET`، زیرساخت دیپلوی |
| R4 — یکپارچگی شواهد | 🔒 | BLOCKED |
| R5 — همزمانی | ⏸ | OPEN by design |
| **تست‌ها** | ✅ | ۱۶۲/۱۶۲ |

## Content & Social Intelligence Module (Content Studio)

| قابلیت | وضعیت | جزئیات |
|---|---|---|
| چندمستأجری (`workspace`) | ✅ | ۲۶ جدول، ۲۷ migration |
| استراتژی `scrape_translate` | ✅ | runtime واقعی با APScheduler، dedupe، قفل همزمانی روی Postgres |
| استراتژی `before_after_compositor` | ✅ | با برچسب `service_slug` برای تحلیل per-service |
| استراتژی `ai_avatar_trend` | 📋 | فقط seam — عمداً پیاده نشده |
| انتشار (Buffer + Instagram) | 🟡 | لایه ساخته شده؛ credential واقعی و OAuth round-trip هنوز وصل نشده (نیازمند دیپلوی عمومی HTTPS) |
| Engagement Intelligence | ✅ | Poller واقعی Instagram Insights، adaptive polling، گزارش per-service |
| **حلقه‌ی یادگیری** | ✅ | `signals → recommendations → outcomes → evaluations` — **دارایی کلیدی** |
| Content Briefs و Planner | ✅ | تبدیل پیشنهاد به بریف محتوا |
| داشبورد | ✅ | Overview، Inbox، تقویم جلالی، Strategy Console، Channels، Before/After، تحلیل‌ها — همه به API واقعی |
| بومی‌سازی تصویر | 🟡 | هنوز وصل/تست نشده |
| **تست‌ها** | ✅ | ۳۹۸ backend + ۵۸ frontend (در آخرین Commit) |

### ⚠️ کار تثبیت‌نشده در Content Studio — یافته‌ی مهم

`git status` در `C:\CONTENT STUDIO` نشان می‌دهد **حجم زیادی کار Commit‌نشده** روی درخت کاری هست (تاریخ فایل‌ها ۳۰ اوت ۲۰۲۶):

| مورد | وضعیت |
|---|---|
| **ماژول Planner** (`service/app/planner/`) | Commit نشده — `candidate_builder`، `scoring`، `diversification`، `constraints` + هفت فایل تست |
| Migration `026_content_planner.sql` | Commit نشده — جدول‌های `content_plans` و `content_plan_items` |
| **ماژول Assistant** (`service/app/assistant/`) | تغییرات Commit‌نشده — `rules/registry`، `snapshot`، `planner_health` |
| `recommendation_quality` | گسترش Commit‌نشده |
| `AGENTS.md` | فایل جدید Commit‌نشده |
| رابط کاربری Planner و AssistantOrb | Commit نشده |

جمعاً ۳۵ فایل تغییریافته (~۷۰۰ خط افزوده) و ۱۲ مسیر جدید.

**چرا این مهم است:**

۱. **ماژول `assistant` مستقیماً به بند ۲ چشم‌انداز (دستیارهای نقش‌محور) مربوط است** و در ممیزی اولیه‌ی من — که بر پایه‌ی تاریخچه‌ی Commit بود — دیده نشد.
۲. **Planner یک «برنامه‌ریز مبتنی بر شواهد» است** که به حلقه‌ی یادگیری وصل می‌شود؛ یعنی بند ۳ چشم‌انداز از آنچه فکر می‌کردم جلوتر است.
۳. **ریسک از دست رفتن.** این کار فقط روی دیسک این ماشین است. همین الگو یک‌بار در V2 اتفاق افتاد و دو Commit تثبیت‌نشده برای همیشه از بین رفتند.

**توصیه:** پیش از هر کار یکپارچه‌سازی، وضعیت این کار تعیین تکلیف و Commit شود.

## MLINO V2

| قابلیت | وضعیت | جزئیات |
|---|---|---|
| نقشه (SDK نشان) | ✅ | خوشه‌بندی، مارکر دسته‌بندی‌شده، حالت خطای صادقانه |
| دستیار و تطبیق | ✅ | Edge-first: قاعده‌محور روی دستگاه، LLM اختیاری با fallback زنده |
| ویترین AR | ✅ | یک کارت اصلی + حداکثر ۵ حباب، شعاع انتخابی، بدون هم‌پوشانی |
| فضای من | ✅ | ذخیره/بعداً/مناسب من — فقط `localStorage`، بدون هویت پنهان |
| تم روشن/تیره | ✅ | راستی‌آزمایی زنده: کنتراست ۱۵٫۷۳:۱ و ۸٫۹۳:۱ |
| **منبع داده** | 🟡 | **دایرکتوری آزمایشی (Mock) با ده رکورد** — به V1 وصل نیست |
| اتصال زنده به V1 | 📋 | قرارداد ۰۲ آماده، اتصال عملیاتی وجود ندارد |
| **تست‌ها** | ✅ | ۱۰۴/۱۰۴ |

## لایه‌های چشم‌انداز — وضعیت واقعی

| لایه | وضعیت | مانع |
|---|---|---|
| ۱ Business Brain | 📋 | نیازمند CCR + طراحی مدل دانش |
| ۲ دستیارهای نقش‌محور | 📋 | نقش‌ها در V1 تعریف نشده‌اند |
| ۳ موتور اقدام روزانه | 📋 | قرارداد C آماده؛ Adapter نوشته نشده |
| ۴ صدای مشتری | 🔒 | **سیاست R8-a تصویب نشده** |
| ۵ اتوماسیون | ✅ | در Content Studio موجود |
| ۶ دانش سازمانی | 📋 | R8-b — نیازمند مدل دانش |
| ۷ CRM | 🔒 | وابسته به R8-a |
| ۸ لایه‌ی قابل‌انتشار | ✅ | AC-2 |

## سه چیزی که باید صریح گفته شود

**۱. موتورهای تشخیص فرصت به داده‌ی واقعی وصل نیستند.** منطق نوشته و تست شده، ولی هر سه Repository در حافظه است. یعنی V1 امروز فرصت واقعی از دیتابیس تولید نمی‌کند. این مهم‌ترین شکاف بین «آنچه هست» و «آنچه چشم‌انداز می‌خواهد» است.

**۲. V2 روی داده‌ی آزمایشی کار می‌کند.** هیچ اتصال عملیاتی بین V1 و V2 وجود ندارد. «حفظ مرزها» با «اتصال تایید شد» یکی نیست.

**۳. هیچ داده‌ی مشتری واقعی امروز پردازش نمی‌شود** — و تا تصویب سیاست R8-a هم نخواهد شد.

# گزارش تصویب — ADR-0010 · ADR-0011 · ADR-0012

**تاریخ:** ۱۱ سپتامبر ۲۰۲۶
**نقش:** بازبین مالک برای معماری MLINO (Claude Opus 5) — به دستور صریح مالک
**دامنه:** **فقط انتقال وضعیت مستندات.** بدون کد، شِما، API، قرارداد یا migration. **هیچ تصمیم تازه‌ای ساخته نشد، هیچ تصمیم بسته‌ای باز نشد، ADR-0007 تا ADR-0009 دست نخوردند. هیچ پیاده‌سازی‌ای با این تصویب آغاز نمی‌شود.**

---

## حکم نهایی: ✅ **تصویب شد**

**هر هشت معیار تصویب مالک برقرار است** (بند ۳). وضعیت سه ADR از «Draft — Pending Owner Approval — Not Approved» به **«Approved»** تغییر کرد.

## ۱. ADRهای مصوب

| ADR | عنوان | تصمیم‌های پایه |
|---|---|---|
| [**ADR-0010**](adr/ADR-0010-platform-executes-not-authorizes.md) | پلتفرم اجرا می‌کند، اختیار کسب‌وکاری اعطا نمی‌کند | D-58 · D-61 · D-62 |
| [**ADR-0011**](adr/ADR-0011-core-module-boundary.md) | مرز Core و ماژول | D-63 · D-65 · D-71 |
| [**ADR-0012**](adr/ADR-0012-experience-assistant-session-boundary.md) | مرز تجربه، دستیار و Session | D-64 · D-66 · D-67 · D-70 |

**مسیر:**
1. پیش‌نویس — `5f48ba4`
2. بازبینی — [`ADR_REVIEW_BEFORE_APPROVAL.md`](ADR_REVIEW_BEFORE_APPROVAL.md) · `47ae844`
3. اصلاح ر۱ تا ر۹ — [`ADR_REVIEW_CORRECTION_REPORT.md`](ADR_REVIEW_CORRECTION_REPORT.md) · `3dda649`
4. بررسی نهایی و تصویب — همین سند

## ۰. وضعیت زنده پیش از تصویب

همگام با remote (`3dda649`)، درخت کاری تمیز، `HANDOFF_STATE` برابر تحویل اصلاحات. **آخرین تغییر `adr/` همان اصلاحات من بود** — هیچ عامل دیگری از آن زمان ADRها را تغییر نداده بود. **متن فعلی هر سه ADR پیش از تصمیم دوباره به‌طور کامل خوانده شد.**

---

## ۲. بررسی سازگاری نهایی — ۱۶ تصمیم

✅ = سازگار · — = موضوع آن ADR نیست

| تصمیم | ADR-0010 | ADR-0011 | ADR-0012 | آنچه بررسی شد |
|---|---|---|---|---|
| **D-08** — AC-2؛ مصرف‌کننده = سازمان؛ رضایت هنگام خروج | ✅ | ✅ | ✅ | پلتفرم محتوای کسب‌وکار را نمی‌خواند · داده‌ی ماژول از همان دروازه · Session دسترسی را فردی نمی‌کند · خروجی برای کاربر نهایی فقط منتشرشده |
| **D-31** — یازده نوع · پنج منبع | ✅ | ✅ | — | ردیف داده‌ی ADR-0010 پنج منبع را نام می‌برد؛ هیچ نوع یا منبعی افزوده نمی‌شود |
| **D-43** — تصمیم بیرونی `Decision` نمی‌شود | ✅ | ✅ | ✅ | حکم بیرونی = شاهد ممیزی در صفحه‌ی پلتفرم · `Decision` نقطه‌ی گسترش ماژول ندارد · دستیار و ارکستراسیون تصمیم نمی‌سازند |
| **D-52** — انتشار انسانی؛ خودکار فقط پس گرفتن | ✅ | ✅ | ✅ | قاعده‌ی جهت خودکارسازی · بدون طبقه‌بندی Core · دستیار مشتری فقط منتشرشده |
| **D-55** — نقش ≠ اجازه؛ مدیر پلتفرم بدون اجازه | ✅ | ✅ | ✅ | پلتفرم بدون اجازه‌ی کسب‌وکاری · دسته‌ها Core، D-55 بازنویسی نشد · بدون Session جانشین · بررسی نقش AC-2 صادقانه به‌عنوان وضع موقت |
| **D-57** — اعطا به عضویت؛ مبنا بدون `ADMIN_ACTION`؛ بدون نیابت به ماشین | ✅ | ✅ | ✅ | `ADMIN_ACTION` مبنای پیوند، نه اعطا · اجازه‌ی ماژول روی عضویت · کار پس‌زمینه فقط پیش‌نویس |
| **D-58** — بازیابی پنج‌لایه؛ می‌افزاید | ✅ | — | ✅ | پنج لایه · مسیر حذف دارنده با سند بیرونی · D-70 اعطا را تغییر نمی‌دهد |
| **D-61** — ساختن آزاد؛ راستی‌آزمایی پیش از انتشار | ✅ | — | — | راستی‌آزمایی هرگز اختیار حکمرانی نمی‌دهد؛ بدون انتقال خودکار ادعا |
| **D-62** — پیوند = نگاشت هویت؛ رضایت دوطرفه | ✅ | ✅ | ✅ | «فقط داده حمل می‌کند» · بدون API تا ثبت دو رضایت · registry با قاعده‌ی ۱۰ سازگار · بدون سیستم هویت دوم |
| **D-63** — Core بدون واژگان صنفی؛ م۰ تا م۲ | — | ✅ | ✅ | آزمون سه‌شرطی · برچسب‌های موجود بی‌تغییر · م۱ با مجوز جداگانه · م۲ فقط کد |
| **D-64** — Core · V1 · V2 | — | — | ✅ | ارکستراسیون و زیربنای دستیار Core · V2 تجربه را می‌سازد · روشن‌سازی D-53 عیناً |
| **D-65** — بدون نقش صنفی؛ دسته‌ها Core | — | ✅ | ✅ | تصمیم ۴ ADR-0011 و خوانش سند OD-40 |
| **D-66** — Session فقط هویت | — | — | ✅ | هر پنج قاعده عیناً + «هیچ Sessionی بافت سازمانی حمل نمی‌کند» |
| **D-67** — دو دستیار | — | — | ✅ | دستیار مشتری V2، فقط منتشرشده · دستیار عملیاتی داخلی، AC-2 |
| **D-70** — بستن Session | ✅ | — | ✅ | هر چهار قاعده عیناً · سازگار با قاعده‌ی جهت |
| **D-71** — مرز دیتابیس ماژول | — | ✅ | — | هر پنج قاعده عیناً · «از نخستین ساخت جدا، نه مهاجرت» |

**نتیجه: هیچ تعارض معماری نیست. هیچ تصمیم منبعی تضعیف نشده است.**

---

## ۳. معیارهای تصویب مالک

| معیار | نتیجه | شاهد |
|---|---|---|
| **هیچ تعارض معماری** | ✅ | بند ۲ |
| **هیچ تصمیم منبعی تضعیف نشده** | ✅ | عبارت‌های مالک عیناً آمده‌اند: D-53، D-66، D-67، D-70 و D-71. D-55 بازنویسی نشد. ر۱ تا ر۹ فقط عبارت‌هایی را اصلاح کردند که **از** منبع فراتر رفته بودند |
| **هیچ ابهام منجر به واگرایی پیاده‌سازی** | ✅ | بند ۳٫۱ |
| **مرز Core / V1 / V2 سالم** | ✅ | ADR-0012 تصمیم ۱ · V1 = Core + ماژول‌ها |
| **جدایی ماژول سالم** | ✅ | ADR-0011 تصمیم ۲ و ۷ · Core به جدول ماژول ارجاع نمی‌دهد |
| **جدایی دستیار مشتری و عملیاتی سالم** | ✅ | ADR-0012 تصمیم ۳ · قیود Session فقط بر دستیار عملیاتی |
| **Session مال Core و بدون مجوزدهی سازمانی** | ✅ | ADR-0012 تصمیم ۴ · «هیچ Sessionی بافت سازمانی حمل نمی‌کند» · اجازه فقط از عضویت و اعطا |
| **پلتفرم اجرا می‌کند، هرگز اختیار نمی‌دهد** | ✅ | ADR-0010 تصمیم ۱ و ۲ و ۶ |

### ۳٫۱ چهار مورد باز — عمداً واگذارشده، نه مبهم

هر کدام **صریحاً** به یک تصمیم یا مرحله‌ی نام‌برده واگذار شده است، پس جای تفسیر آزاد ندارد:

| مورد | واگذارشده به | کجا |
|---|---|---|
| شکل فیزیکی جدایی ذخیره‌سازی — schema جدا یا دیتابیس جدا | برنامه‌ریزی پیاده‌سازی — **خود D-71** | ADR-0011 تصمیم ۷ |
| نحو فضانام و شکل registry | CCR م۱، با مجوز جداگانه | ADR-0011 تصمیم ۵ و ۸ |
| لزوم ورود برای دستیار مشتری | تصمیم آینده‌ی محصول — ADR آن را **الزامی نمی‌کند** | ADR-0012 تصمیم ۳ |
| سازوکار لغو Session | OD-08 — D-70 آن را به نیاز پیاده‌سازی تبدیل کرده | ADR-0012 پیامدها و اجرا |

---

## ۴. فایل‌های تغییریافته

| فایل | تغییر |
|---|---|
| `mlino_book/adr/ADR-0010-platform-executes-not-authorizes.md` | **فقط سطر وضعیت** ← `Approved` |
| `mlino_book/adr/ADR-0011-core-module-boundary.md` | **فقط سطر وضعیت** ← `Approved` |
| `mlino_book/adr/ADR-0012-experience-assistant-session-boundary.md` | **فقط سطر وضعیت** ← `Approved` |
| `mlino_book/CHANGELOG.md` | مدخل `[0.31.0]` |
| `mlino_book/README.md` | فهرست `adr/`: `ADR-0001 … ADR-0009` ← `ADR-0001 … ADR-0012` |
| `mlino_book/04_INTEGRATION_ROADMAP.md` | «ADR-0010 و ADR-0011 آماده‌ی نوشتن» ← «ADR-0010 · ADR-0011 · ADR-0012 مصوب» |
| `mlino_book/ADR_APPROVAL_REPORT.md` | همین سند |
| `AI_HANDOFF/*` | گزارش تحویل · `CLAUDE_LATEST_REPORT.md` · `HANDOFF_STATE.md` |

**محافظ‌های اسکریپت انتقال وضعیت:**
- درخت کاری پیش از اجرا تمیز بود.
- allow-list رعایت شد.
- **بدنه‌ی هر ADR پس از حذف سطر وضعیت بایت‌به‌بایت برابر ماند.**
- هیچ نشانه‌ی «Draft» یا «Not Approved» باقی نماند.
- این فایل‌ها بایت‌به‌بایت بررسی شدند: قرارداد منجمد، شِمای Prisma، دفتر تصمیم‌ها، ADR-0007، ADR-0008، ADR-0009 و سیاست AC-2.

**دفتر تصمیم‌ها تغییر نکرد** — ردیف‌های D-58 تا D-71 همان‌طور که ثبت شده‌اند می‌مانند.

## ۵. تایید عدم تغییر

| | |
|---|---|
| **کد** (`implementation/` · `mlino2/`) | ✅ **صفر تغییر** |
| **شِما** (`schema.prisma`) | ✅ **صفر تغییر** — بایت‌به‌بایت |
| **API** | ✅ **صفر تغییر** |
| **قراردادها** (قرارداد منجمد · اسناد `contracts/`) | ✅ **صفر تغییر** |
| **migration** | ✅ **هیچ** |
| **تصمیم تازه · بازگشایی تصمیم بسته** | ✅ **هیچ** |
| **ADR-0007 · ADR-0008 · ADR-0009** | ✅ **دست‌نخورده** — همچنان در انتظار بازبینی مالک |

**هیچ پیاده‌سازی‌ای با این تصویب آغاز نمی‌شود.** موانع پیاده‌سازی بدون تغییرند:
- OD-05 · OD-08
- مسیر انتشار V1 که هنوز ساخته نشده
- OD-01 — فقط برای داده‌ی شخصی
- مجوز CCR م۱ · CCR رضایت پیوند
- برنامه‌ریزی شکل فیزیکی D-71 پیش از گام ۳
- OD-09 🔴

**یک افشا:** نویسنده، بازبین و ویرایشگر این ADRها همین مدل بوده است. تصویب به دستور صریح مالک انجام شد. بازبینی مستقل، اگر بعداً انجام شود، هر یافته‌اش از مسیر ADR جانشین یا اصلاح ثبت‌شده اعمال می‌شود.
